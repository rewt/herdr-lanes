# Specification report — 2026-09-08

**Result: specification ready for operator review; no product implementation.**
Choose OpenSpec, with nine independently promotable product changes and one optional
research lane. Work is confined to this repository. Root configuration and account
rollout remain operator actions; no machine setup has been changed.

## Framework choice

OpenSpec fits this existing small tool because a change folder holds a proposal,
behavior deltas, design decisions, and a lane-sized task, while current specs and
archived changes stay distinct. This maps naturally to incremental promotion.
The framework's [documented layout](https://github.com/Fission-AI/OpenSpec/blob/main/docs/concepts.md)
supports that separation; its [default artifact schema](https://github.com/Fission-AI/OpenSpec/blob/main/schemas/spec-driven/schema.yaml)
supplies the Markdown forms used here.

GitHub Spec Kit is also viable. Its [constitution/specify/plan/tasks workflow](https://github.com/github/spec-kit)
provides stronger up-front planning structure. I studied the supplied local generic
installation's workflow instructions, command files, task template, and integration
metadata. It is agent-neutral and its feature directories need not dictate branch
names. The choice is therefore **not** based on an assumed Codex/Claude or branch
incompatibility. Here a second constitution and installed command/template/script
scaffold would duplicate AGENTS.md and add maintenance without improving these
small, separately promoted changes. OpenSpec's change/archive model is the better
fit for this initiative; neither framework needs to become a runtime dependency.

The scaffold is a [config](../openspec/config.yaml), a [portable workflow guide](../openspec/README.md),
an [existing safety spec](../openspec/specs/lane-safety/spec.md), and four standard
artifacts per product change. No agent-specific command registration, custom schema,
runtime package, root dependency, generated installation manifest, or empty archive
placeholder was added. The optional [mouse brief](tasks/board-mouse.md) is research
outside product changes. The installed OpenSpec 1.6.0 rejects spec-less changes even
though current upstream documents a skip_specs option; keeping this research
separate avoids inventing a product requirement or upgrading the machine.

## Goals and constraints

- One board shows every agent visible through accessible local Herdr servers, with
  its repository, goal/provenance, state, exact-commit gate, report/verdict, and a
  one-key jump to its tab. Unregistered, unnamed, and facilitator sessions count.
- A task plus explicit repository and route becomes an ignored template brief, one
  branch/worktree/child workspace, one agent, and one registry entry.
- Each development root can own shared config/routes and sibling worktrees at
  `<root>/.worktrees/<repo>/lane-<topic>`. Same-named repositories remain isolated;
  git, not lane configuration or a label, determines repository and author identity.
- The CLI remains built-ins-only and agent-agnostic. The optional UI calls CLI
  interfaces. There is no scheduler, job store, lifecycle replay, automatic
  promotion, remote gate, push, or identity setter.
- Existing cwd/clean-tree/conflict/archive/validation/fast-forward safeguards and
  Node 20/git 2.38 compatibility remain acceptance requirements.
- All tracked material is public-safe. The raw local briefs and review transcripts
  were read as inputs and were not copied into the repository.

"Machine-wide" means Herdr-discoverable local sessions; inaccessible servers and
agents outside Herdr cannot be promised. The board must show coverage, unknown
fields, and errors honestly. Production root defaults never choose temporary
worktrees; system-temp repositories remain mandatory test fixtures. Legacy paths
and the existing explicit environment override remain usable without migration.

## Phase specifications and acceptance

| Phase | Independently useful completion checkpoint |
| --- | --- |
| 1 — roots | Config sources/precedence are inspectable; two roots can open the same repo/topic without collision; unsafe paths refuse; the canonical checkout hosts the parent workspace; old lanes still operate. |
| 2 — registry and observation | Dispatch registers automatically, successful close marks done, all discoverable agents appear in grouped machine scope, finished metadata hides by default, current gates stay commit-bound, and supported agents show substantive message previews. |
| 3 — board UX | A responsive alternate-screen master/detail view exposes intent and evidence; Enter focuses the correct live tab; errors and exit restore terminal state; mouse research may conclude no-go without blocking delivery. |
| 4 — idea dispatch | Both CLI and composer create a literal-text brief and dispatch once into the selected repository/route; editor cancel and partial failures preserve work and never trigger automatic retries. |

The workplan is refined into subphases because registry mutation, CLI transport,
cross-repository discovery, text extraction, and terminal input have different
failure modes. **Each numbered subphase is one independent lane and one promotion**,
not a long-lived initiative branch or a set of unshippable infrastructure tasks.
Phase 4 CLI work can start after discovery; the UI composer waits for keyboard UX.
The mouse lane is optional and off the critical path.

Each linked task below is a complete brief with context, scope, non-goals, required
approval, failing-test expectations, acceptance, report path, and commit/check rules.
Effort is a suggested reasoning level and session boundary, not a model default.
If implementation discovers the scope cannot fit one focused session, return a
bounded spec revision before broadening the lane.

| Phase | Topic / dispatchable task | Route | Suggested effort | Depends on |
| --- | --- | --- | --- | --- |
| 1a | [roots](../openspec/changes/roots-config/tasks.md) | engineer | High; one focused session | None; supplied roots brief. |
| 1b | [roots-identity](../openspec/changes/roots-identity/tasks.md) | engineer | High; one focused session | roots-config promoted. |
| 2a | [session-registry](../openspec/changes/session-registry/tasks.md) | engineer | High; one focused session | roots-identity promoted. |
| 2b | [board-cli](../openspec/changes/board-cli/tasks.md) | engineer | High; one focused session | session-registry promoted. |
| 2c | [board-discovery](../openspec/changes/board-discovery/tasks.md) | engineer | High; one focused session | board-cli promoted. |
| 2d | [board-messages](../openspec/changes/board-messages/tasks.md) | engineer | High; one focused session | board-discovery promoted. |
| 3a | [board-ux](../openspec/changes/board-ux/tasks.md) | engineer | High; one focused session | board-messages promoted. |
| 3b (optional) | [board-mouse](tasks/board-mouse.md) | engineer | Medium; two hours maximum | board-ux promoted; optional. |
| 4a | [idea-cli](../openspec/changes/idea-cli/tasks.md) | engineer | High; one focused session | board-discovery promoted |
| 4b | [idea-composer](../openspec/changes/idea-composer/tasks.md) | engineer | High; one focused session | idea-cli and board-ux promoted |

Dispatch from the canonical checkout after prerequisites have promoted:

```sh
lane open roots
lane dispatch roots --route engineer @openspec/changes/roots-config/tasks.md
```

Substitute the topic and matching tasks path from the table. The mouse task uses
`@docs/tasks/board-mouse.md`. These are future operator commands; this planning
session dispatches no implementation agents.

The first roots task folds in the supplied Phase 1 engineering brief: key,
precedence, relative paths, nearest-parent layering, route merging, config output,
foreign-path refusal, documentation, and all requested offline cases. A compatibility
correction is explicit: today's LANE_WORKTREE_ROOT already names the final
per-repository directory; the new worktree_root key names the shared base that gets
a repository suffix. Canonical configuration resolution prevents a sibling lane
from inheriting its worktree directory's unrelated parent settings.

## Recommendations on the four open decisions

| Decision | Recommendation and rationale |
| --- | --- |
| Board default | Machine-wide, with --repo filter and visible local-server coverage. It satisfies the one-view goal; per-repository-only would preserve the current blind spot. No recursive disk scan or private process detection. |
| Mouse research | One optional, two-hour engineer lane after keyboard UX. Test the actual terminal-to-Herdr path and exit cleanup. Enter is sufficient for acceptance; no-go or unavailable-TTY evidence is a valid outcome. A positive result requires a separate implementation proposal. |
| Review records | Tracked per-target-repository `docs/reviews/<topic>/<head7>-r<N>.md`, with full reviewed SHA, public-safe findings, verdict and test evidence. This preserves durable review context without committing raw private transcripts. Reviews never become CLI promotion policy. |
| Workspace labels | Include root/repo with lane topic, disambiguating duplicate root labels by digest. Keep old labels and use verified paths/opaque IDs for operations. A basename label alone is not identity. |

The proposed UI package is `@inkjs/ui`, isolated with Ink/React under `board/`.
Its [upstream package manifest](https://github.com/vadimdemedes/ink-ui/blob/main/package.json)
currently identifies version 2.0.0, Node >=18, and an Ink >=5 peer requirement.
The engineering lane must verify/pin the compatible published release and measure
the dependency footprint. This spec does not assume current upstream Ink APIs
exist in this repository's installed Ink 5.

## Proposed contract amendments

The operator must approve these exact texts before their dependent implementation.
Keep the current root `AGENTS.md` unchanged in this planning lane. Approval is for a
concrete amendment; it is not required to finish the present specification.

**A1 — append to Product contract (roots-identity):**

> A repository is identified by its canonical git common directory, never by its
> basename, workspace label, remote, or author name alone. Development-root lane
> paths are outside the canonical checkout, with no automatic temporary-directory
> fallback. Existing registered worktrees remain usable without automatic migration.
> Lane commands do not set git identity; git configuration remains authoritative.

**A2 — append to Product contract (session-registry):**

> The session registry and completion markers are gitignored local display metadata.
> Dispatch may record a session and close may mark its sessions done; the board may
> discover sessions and explicitly mark a row done. This data must never authorize,
> schedule, retry, lease, validate, promote, push, or delete work. Concurrent metadata
> updates must not lose unrelated sessions. No daemon or background job coordination
> is added.

**A3 — replace the Herdr-optional bullet in Product contract (board-cli):**

> Herdr integration is optional for git-only operations and offline board snapshots.
> Dispatch, dispatch-from-idea, live discovery, and session focus require an available
> Herdr server. The optional board UI invokes lane CLI interfaces for observations
> and actions; it does not implement a second git or Herdr control path. Reconnection
> may restore display subscriptions only and must never replay lifecycle actions.

**A4 — replace the built-ins/UI-exception bullet in Change rules (board-ux):**

> Use Node.js built-ins only in lane.mjs, dependency-free board services, and the test
> suite. The isolated board/app.mjs entrypoint and UI-only modules under board/ui/
> may use Ink, React, and @inkjs/ui from board/package.json. Keep their dependencies
> and lockfile isolated; git-only commands, plain/JSON board output, and npm test
> must run without installing the UI package. Preserve Node.js 20 compatibility.

A2 clarifies the display-only exception already identified in the promoted board
reviews. A3 makes the requested CLI boundary explicit; it does not permit a job
system. A4 extends the existing Ink/React exception only as far as the chosen official
UI component package and UI modules. Any further dependency needs its own proposal.

## Operator rollout checklist — machine-local work, not product tasks

This checklist deliberately lives only in this report. It is not performed by
the spec lane or copied into the tracked change artifacts. Keep actual root paths,
identities, model choices, and private brief content in ignored/local configuration.

- [ ] Review the spec and approve or revise A1-A4 before the corresponding lanes.
  Dispatch engineering lanes only after their prerequisites have promoted.
- [ ] Inventory each intended top-level development root and its canonical
  repositories. Record existing lane paths and active sessions locally; do not
  move active worktrees or assume same-named repositories share identity.
- [ ] Confirm git identity using the existing repository/local/global configuration
  and any operator-owned conditional includes. Check both canonical and linked
  worktrees with git config --show-origin; adjust identities through git only.
- [ ] After Phase 1 promotion, place each root's shared .lane.json locally with its
  routes, use notes, and relative worktree_root of .worktrees (or use the root
  default). Keep model/account/env values out of tracked public examples.
- [ ] Keep project-specific validation/preparation in each canonical repository's
  config. Avoid partial dispatch/route objects that accidentally replace inherited
  ones. Run lane config and lane routes to inspect resolved sources before use.
- [ ] Inspect or remove stale LANE_CONFIG/LANE_WORKTREE_ROOT overrides in operator
  shells. Verify they are intentional and do not redirect work into a temporary,
  foreign-root, or repository-internal path.
- [ ] Ensure .lane/ and any configured registry array plus sidecar directory are
  untracked and ignored in each target repository. Use a normal repository change
  for shared ignore rules; never stage existing private runtime contents.
- [ ] Start or select each canonical repository's Herdr workspace and place its
  facilitator there. Do not launch nested clients from managed panes. Confirm new
  lanes become children of that canonical workspace.
- [ ] Pilot one lane in one root, then the same topic in a same-named repository in
  a second root. Confirm distinct sibling paths, labels, git identities, and
  verified cwd. Run builds/tests and promotions one at a time on this machine.
- [ ] Keep existing lanes at their registered paths until finished. If relocation
  is wanted later, plan it explicitly with recoverable refs; no migration command
  or automatic worktree move is part of this initiative.
- [ ] After Phase 2, dispatch a pilot and check automatic goal/brief/report metadata;
  verify unregistered/facilitator rows, local-server coverage, --repo, --all, a
  fresh post-commit gate, and done filtering after successful close.
- [ ] Adopt the public-safe reviews folder per target repository; keep raw reviews
  local. Record full reviewed SHAs and keep the final post-commit GATE in the
  conversation/ignored gate file so a report commit does not stale it.
- [ ] After Phase 3, install the optional board package once, serialized with other
  build/test work. Smoke-test layout, Enter focus, return, resizing, quit and Ctrl-C
  in an actual Herdr pane. Run the optional mouse study only if useful.
- [ ] After Phase 4, pilot lane new and composer using an explicit root/repo/route;
  test editor cancel, a deliberate preflight refusal, single dispatch, registered
  row selection, and close. Do not retry an ambiguous delivery without inspection.
- [ ] If a pilot fails, stop adopting new flows, retain branches/worktrees/briefs,
  and use known-good explicit open/dispatch/status paths as applicable. Use a
  reviewed fix or revert lane for tool rollback; do not rewrite history or delete
  runtime data to make a pilot appear clean. Legacy board versions do not read
  the new sidecar format; use git/Herdr directly until the compatible tool returns.

## Evidence used and remaining questions

The tracked [board report](LANE-BOARD-20260908.md), [check report](LANE-CHECK-20260908.md),
all three board review rounds, both check review rounds, and the drafted roots brief
were read. Current code confirmed the direct UI I/O, legacy environment-root
semantics, and match-any-output subscription. The installed Herdr help/schema
confirmed focus-by-name-or-pane, session enumeration, repo-scoped worktree listing,
and protocol 20's lack of a branch field in workspace metadata or a structured
last-assistant-message result. No live focus, agent prompting, or UI probe was run.

The prior review findings are carried forward explicitly: ignored runtime state
must not dirty promotion; git ownership must not come from workspace labels;
shutdown must remain permanent; asynchronous sampling and Unicode display-width
handling address the two deferred board nits; prepare failures produce no new gate.
New automatic report paths use `docs/reports/<topic>.md` so work crossing midnight
does not change the convention. Existing reports and explicit legacy paths remain
readable. Phase 1 retains the supplied dated roots report path.

Resolved design choices are in the adjacent specs, not left to implementers to
guess. These questions remain explicitly bounded:

- Does the operator approve A1-A4 and the canonical operational-config clarification?
  Until approval, dependent implementation cannot claim those contract changes.
- Which local Herdr server endpoints are accessible in the intended operator session?
  Discovery reports partial coverage; it must never silently start servers.
- Which installed Codex/Claude screen variants yield confident assistant previews?
  The message lane supplies sanitized fixtures and uses unavailable for unknown
  variants; it does not promise a full transcript.
- Is mouse input usable through the actual terminal/Herdr setup? The optional
  two-hour study answers that with go/no-go/defer evidence.
- Which exact compatible @inkjs/ui release should be locked at implementation?
  The UX lane verifies the published package under Node 20 without a major Ink upgrade.

Actual root names, routes, identity values and editor choices belong to operator
rollout and are intentionally absent here.

## Verification of this planning change

Baseline HEAD equaled local main at `c9d829cc2415e54ad1bf5c810e51625804c98714`.
One direct `npm test` passed **49/49**, zero failures/skips, in **5.290 s** before
edits. No CLI, board runtime, tests, package manifests, lockfiles, or product-contract
text were changed. The final commit must run `lane check` with its ordinary
`npm test` validator; its count must remain 49. This required commit-bound run is
separate from the one direct baseline invocation, and the final GATE is reported in
the conversation after the commit so recording it cannot stale itself.

OpenSpec 1.6.0 strict validation passed **10/10 items** (nine product changes plus
the existing safety spec), serialized with concurrency 1. Its initial rejection
of a spec-less mouse change exposed the installed/upstream difference; the final
scaffold uses the standalone research brief. No product behavior changed, so no
product regression or deliberate-failure suite was added/run for this prose task.
Document/link/source consistency and git diff --check are checked before commit.

Environment: Node.js 20.19.4, npm 10.8.2, git 2.54.0. Node 20.0/git 2.38 minimums,
live dispatch/focus, terminal restoration, mouse, alternative hosts, real cross-root
rollout, and future feature acceptance have not been exercised by this planning lane.
