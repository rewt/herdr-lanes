# Specification report — 2026-09-08

**Result: specification ready for operator review; no product implementation.**
Choose OpenSpec, with twelve independently promotable product changes and one optional
research lane. Work is confined to this repository. Root configuration and account
rollout remain operator actions; no machine setup has been changed.
The 2026-09-09 addendum adds foreground review and portable built-in configuration.
roots-config has since promoted; eleven changes remain proposed, and its delivered
source/precedence requirements are now the baseline for the configuration addendum.

## Framework choice

OpenSpec fits this existing small tool because a change folder holds a proposal,
behavior deltas, design decisions, and a lane-sized task, while current specs and
archived changes stay distinct. This maps naturally to incremental promotion.
The framework's [documented layout](https://github.com/Fission-AI/OpenSpec/blob/main/docs/concepts.md)
supports that separation; its [default artifact schema](https://github.com/Fission-AI/OpenSpec/blob/main/schemas/spec-driven/schema.yaml)
supplies the Markdown forms used here.

GitHub Spec Kit is also viable. Its [constitution/specify/plan/tasks workflow](https://github.com/github/spec-kit)
provides stronger up-front planning structure. I studied the supplied local generic
installation's workflow instructions, command files, task template, and integration
metadata. It is agent-neutral and its feature directories need not dictate branch
names. The choice is therefore **not** based on an assumed Codex/Claude or branch
incompatibility. Here a second constitution and installed command/template/script
scaffold would duplicate AGENTS.md and add maintenance without improving these
small, separately promoted changes. OpenSpec's change/archive model is the better
fit for this initiative; neither framework needs to become a runtime dependency.

The scaffold is a [config](../openspec/config.yaml), a [portable workflow guide](../openspec/README.md),
an [existing safety spec](../openspec/specs/lane-safety/spec.md), and a proposal,
design, tasks and one or more delta specs per product change. Promoted root
configuration also has a current spec. No agent-specific command registration, custom schema,
runtime package, root dependency, generated installation manifest, or empty archive
placeholder was added. The optional [mouse brief](tasks/board-mouse.md) is research
outside product changes. The installed OpenSpec 1.6.0 rejects spec-less changes even
though current upstream documents a skip_specs option; keeping this research
separate avoids inventing a product requirement or upgrading the machine.

## Goals and constraints

- One board shows every agent visible through accessible local Herdr servers, with
  its repository, goal/provenance, state, exact-commit gate, report/verdict, and a
  one-key jump to its tab. Unregistered, unnamed, and facilitator sessions count.
- A task plus explicit repository and route becomes an ignored template brief, one
  branch/worktree/child workspace, one agent, and one registry entry.
- Each development root can own shared config/routes and sibling worktrees at
  `<root>/.worktrees/<repo>/lane-<topic>`. Same-named repositories remain isolated;
  git, not lane configuration or a label, determines repository and author identity.
- The CLI remains built-ins-only and agent-agnostic. The optional UI calls CLI
  interfaces. There is no scheduler, job store, lifecycle replay, automatic
  promotion, remote gate, push, or identity setter.
- Existing cwd/clean-tree/conflict/archive/validation/fast-forward safeguards and
  Node 20/git 2.38 compatibility remain acceptance requirements.
- All tracked material is public-safe. The raw local briefs and review transcripts
  were read as inputs and were not copied into the repository.

"Machine-wide" means Herdr-discoverable local sessions; inaccessible servers and
agents outside Herdr cannot be promised. The board must show coverage, unknown
fields, and errors honestly. Production root defaults never choose temporary
worktrees; system-temp repositories remain mandatory test fixtures. Legacy paths
and the existing explicit environment override remain usable without migration.

## Phase specifications and acceptance

| Phase | Independently useful completion checkpoint |
| --- | --- |
| 1 — roots | Config sources/precedence are inspectable; two roots can open the same repo/topic without collision; unsafe paths refuse; the canonical checkout hosts the parent workspace; old lanes still operate. |
| 2 — registry and observation | Dispatch registers automatically, successful close marks done, all discoverable agents appear in grouped machine scope, finished metadata hides by default, current gates stay commit-bound, and supported agents show substantive message previews. |
| 3 — board UX | A responsive alternate-screen master/detail view exposes intent and evidence; Enter focuses the correct live tab; errors and exit restore terminal state; mouse research may conclude no-go without blocking delivery. |
| 4 — idea dispatch | Both CLI and composer create a literal-text brief and dispatch once into the selected repository/route; editor cancel and partial failures preserve work and never trigger automatic retries. |

The workplan is refined into subphases because registry mutation, CLI transport,
cross-repository discovery, text extraction, and terminal input have different
failure modes. **Each numbered subphase is one independent lane and one promotion**,
not a long-lived initiative branch or a set of unshippable infrastructure tasks.
Phase 4 CLI work can start after registry automation; the UI composer waits for
keyboard UX. Message previews can start after the read-interface lane (2b-i).
The mouse lane is optional and off the critical path.

Each linked task below is a complete brief with context, scope, non-goals, required
approval, failing-test expectations, acceptance, report path, and commit/check rules.
Effort is a suggested reasoning level and session boundary, not a model default.
The larger remaining changes have pre-agreed split points below. Choose the
combined or split delivery at dispatch, before implementation. Other unexpected
scope changes still require a bounded spec revision.

| Phase | Topic / dispatchable task | Route | Suggested effort | Depends on |
| --- | --- | --- | --- | --- |
| 1a | [roots](../openspec/changes/roots-config/tasks.md) | engineer | High; one focused session | None; supplied roots brief. |
| 1b | [roots-identity](../openspec/changes/roots-identity/tasks.md) | engineer | High; one focused session | roots-config promoted. |
| 2a | [session-registry](../openspec/changes/session-registry/tasks.md) | engineer | High; one focused session | roots-identity promoted. |
| 2b-i | [board-cli](../openspec/changes/board-cli/tasks.md) — read interfaces/services; UI untouched | engineer | High; one focused session | session-registry (2a) |
| 2b-ii | [board-actions](../openspec/changes/board-actions/tasks.md) — focus/done and UI process boundary | engineer | High; one focused session | board-cli (2b-i) |
| 2c | [board-discovery](../openspec/changes/board-discovery/tasks.md) | engineer | High; one focused session or agreed split | board-actions (2b-ii) |
| 2d | [board-messages](../openspec/changes/board-messages/tasks.md) | engineer | High; one focused session | board-cli (2b-i) |
| 3a | [board-ux](../openspec/changes/board-ux/tasks.md) | engineer | High; one focused session or agreed split | board-discovery (2c), board-messages (2d) |
| 3b (optional) | [board-mouse](tasks/board-mouse.md) | engineer | Medium; two hours maximum | board-ux promoted; optional. |
| 4a | [idea-cli](../openspec/changes/idea-cli/tasks.md) | engineer | High; one focused session | session-registry (2a) |
| 4b | [idea-composer](../openspec/changes/idea-composer/tasks.md) | engineer | High; one focused session | idea-cli and board-ux promoted |
| Addendum R | [review-cli](../openspec/changes/review-cli/tasks.md) | engineer | High; combined or [R-i/R-ii split](../openspec/changes/review-cli/design.md#pre-agreed-split-point): private validation, then public projection | None beyond current main; roots-config promotion now satisfies lane.mjs sequencing. |
| Addendum D | [default-config](../openspec/changes/default-config/tasks.md) | engineer | High; combined or [D-i/D-ii split](../openspec/changes/default-config/design.md#pre-agreed-split-point): routes/provenance, then template resolution | Full review-cli (R-ii if split) promoted; approved A5 in D-i if split. |

The dependency edges describe required capabilities. Message extraction needs the
2b-i read schema, not machine discovery; idea-cli uses an explicit repository and
needs root/config resolution plus registry automation, not a board. The complete
UX still needs discovery's grouping/history and the message parser, so both are
explicit prerequisites after those edges are relaxed. Shared-file overlap can
require sequencing/rebasing without adding a false capability dependency. The
machine's one-build/test rule applies to every path through this plan.

Pre-agreed split points (activate before implementation, with one brief per slice):

| Combined change | First independent lane | Second independent lane | Completion boundary |
| --- | --- | --- | --- |
| [board-discovery](../openspec/changes/board-discovery/design.md#pre-agreed-split-point) | 2c-i board-inventory: endpoint/repository/agent discovery, safe joins, scope/coverage/grouping/history with existing sampling | 2c-ii board-sampling: async sampling, freshness, rate/concurrency bounds and 100-session responsiveness evidence | Second depends on first; consumers of full discovery wait for both. |
| [board-ux](../openspec/changes/board-ux/design.md#pre-agreed-split-point) | 3a-i board-frame: fullscreen responsive frame, basic details/controls, keyboard focus and terminal cleanup | 3a-ii board-details: complete detail content, official UI selectors/status and badge semantics | Second depends on first; composer and mouse wait for full UX. |
| [review-cli](../openspec/changes/review-cli/design.md#pre-agreed-split-point) | R-i review-private: template, dispatch/wait/exit and validated private record with no public output | R-ii review-public: sanitization, public projection and after-check publication boundary | Second depends on first; default-config waits for full review-cli. |
| [default-config](../openspec/changes/default-config/design.md#pre-agreed-split-point) | D-i default-routes: routes/precedence/provenance under approved A5 | D-ii default-templates: repository/installation template resolution and inspection rows | Both wait for full review-cli; second depends on first; full defaults remain pending until both promote. |

Each design names the complete requirements and tests assigned to each slice. If
activated, partition the OpenSpec deltas and briefs before coding; sync only the
implemented slice and keep the rest pending. These are agreed delivery boundaries,
not an invitation to promote a partially completed combined task. All resulting
lanes use the engineer route, normal reports/checks, and independent promotion.

Dispatch from the canonical checkout after prerequisites have promoted and required
amendments are approved. For example, the next roots lane is:

```sh
lane open roots-identity
lane dispatch roots-identity --route engineer @openspec/changes/roots-identity/tasks.md
```

Substitute the topic and matching tasks path from the table. The mouse task uses
`@docs/tasks/board-mouse.md`. These are future operator commands; this planning
session dispatches no implementation agents.

The first roots task folds in the supplied Phase 1 engineering brief: key,
precedence, relative paths, nearest-parent layering, route merging, config output,
foreign-path refusal, documentation, and all requested offline cases. A compatibility
correction is explicit: today's LANE_WORKTREE_ROOT already names the final
per-repository directory; the new worktree_root key names the shared base that gets
a repository suffix. Canonical configuration resolution prevents a sibling lane
from inheriting its worktree directory's unrelated parent settings. Roots-config
(1a) needs no amendment: canonical-config resolution is a documented behavior
change inside it. A1 applies in roots-identity (1b) and retroactively describes
the path placement already delivered in 1a.

## Recommendations on the four open decisions

| Decision | Recommendation and rationale |
| --- | --- |
| Board default | Machine-wide, with --repo filter and visible local-server coverage. It satisfies the one-view goal; per-repository-only would preserve the current blind spot. No recursive disk scan or private process detection. |
| Mouse research | One optional, two-hour engineer lane after keyboard UX. Test the actual terminal-to-Herdr path and exit cleanup. Enter is sufficient for acceptance; no-go or unavailable-TTY evidence is a valid outcome. A positive result requires a separate implementation proposal. |
| Review records | Tracked per-target-repository `docs/reviews/<topic>/<head7>-r<N>.md`, with full reviewed SHA, public-safe findings, verdict and test evidence. The addendum's CLI generates this public projection from the private record; the operator controls its commit. This preserves durable context without raw private transcripts or review-based promotion policy. |
| Workspace labels | Include root/repo with lane topic, disambiguating duplicate root labels by digest. Keep old labels and use verified paths/opaque IDs for operations. A basename label alone is not identity. |

The proposed UI package is `@inkjs/ui`, isolated with Ink/React under `board/`.
Its [upstream package manifest](https://github.com/vadimdemedes/ink-ui/blob/main/package.json)
currently identifies version 2.0.0, Node >=18, and an Ink >=5 peer requirement.
The engineering lane must verify/pin the compatible published release and measure
the dependency footprint. This spec does not assume current upstream Ink APIs
exist in this repository's installed Ink 5.

## Proposed contract amendments

The exact A1-A5 texts live only in the
[OpenSpec guide](../openspec/README.md#proposed-contract-amendments--pending-operator-approval).
They remain proposals; this lane does not edit AGENTS.md.

| Amendment | Approval/application lane | Scope note |
| --- | --- | --- |
| A1 | roots-identity (1b) | Canonical identity and path guarantees; its placement clause describes 1a retroactively. roots-config needs no amendment for canonical-config resolution. |
| A2 | session-registry (2a) | Local display metadata and concurrent updates; no job authority. |
| A3 | board-actions (2b-ii) | CLI-only UI and explicit Herdr requirements; the 2b-i read interfaces leave the UI untouched and need no amendment. |
| A4 | board-ux (3a), or board-frame first if split | Isolated UI package/modules; built-ins-only core and tests remain mandatory. |
| A5 | default-config (Addendum D), or D-i first if split | Public vendor/model/effort profiles as overridable built-ins are explicitly distinguished from prohibited organization-specific defaults; ratify ordinary review's unpinned effort. |

Idea-cli (4a) composes existing open/dispatch behavior after A1-A2 and requires no
additional amendment. Git author identity stays in native git configuration; the
operator accepts that deliberate tool boundary through A1, not a lane identity key.
review-cli needs no new amendment. For default-config, public model identifiers
are repository-neutral, but packaged provider/model policy changes the prior
operator-owned default boundary. Requiring A5 makes that narrow permission explicit.

## Operator rollout checklist — machine-local work, not product tasks

This checklist deliberately lives only in this report. It is not performed by
the spec lane or copied into the tracked change artifacts. Keep actual root paths,
identities, model choices, and private brief content in ignored/local configuration.

- [ ] Review the spec and approve or revise A1-A5 before the corresponding lanes.
  Dispatch engineering lanes only after their prerequisites have promoted.
- [ ] Inventory each intended top-level development root and its canonical
  repositories. Record existing lane paths and active sessions locally; do not
  move active worktrees or assume same-named repositories share identity.
- [ ] Confirm git identity using the existing repository/local/global configuration
  and any operator-owned conditional includes. Check both canonical and linked
  worktrees with git config --show-origin; adjust identities through git only.
- [ ] After Phase 1 promotion, place each root's shared .lane.json locally with its
  routes, use notes, and relative worktree_root of .worktrees (or use the root
  default). Keep model/account/env values out of tracked public examples.
- [ ] Keep project-specific validation/preparation in each canonical repository's
  config. Avoid partial dispatch/route objects that accidentally replace inherited
  ones. Run lane config and lane routes to inspect resolved sources before use.
- [ ] Inspect or remove stale LANE_CONFIG/LANE_WORKTREE_ROOT overrides in operator
  shells. Verify they are intentional and do not redirect work into a temporary,
  foreign-root, or repository-internal path.
- [ ] Ensure .lane/ and any configured registry array plus sidecar directory are
  untracked and ignored in each target repository. Use a normal repository change
  for shared ignore rules; never stage existing private runtime contents.
- [ ] Start or select each canonical repository's Herdr workspace and place its
  facilitator there. Do not launch nested clients from managed panes. Confirm new
  lanes become children of that canonical workspace.
- [ ] Pilot one lane in one root, then the same topic in a same-named repository in
  a second root. Confirm distinct sibling paths, labels, git identities, and
  verified cwd. Run builds/tests and promotions one at a time on this machine.
- [ ] Keep existing lanes at their registered paths until finished. If relocation
  is wanted later, plan it explicitly with recoverable refs; no migration command
  or automatic worktree move is part of this initiative.
- [ ] After Phase 2, dispatch a pilot and check automatic goal/brief/report metadata;
  verify unregistered/facilitator rows, local-server coverage, --repo, --all, a
  fresh post-commit gate, and done filtering after successful close.
- [ ] Adopt the public-safe reviews folder per target repository; keep raw reviews
  local. Record full reviewed SHAs and keep the final post-commit GATE in the
  conversation/ignored gate file so a report commit does not stale it.
- [ ] After review-cli promotes, select an explicit round/source/review route and
  finite check budget; confirm the reviewer writes only the ignored private record
  and the CLI refuses moved/dirty targets or unverifiable sanitization. Inspect the
  generated public record before authorizing its commit and a fresh lane check.
  Configure review first until shipped defaults arrive. If split, private-only R-i
  is inspectable, while the full publication flow waits for R-ii.
- [ ] After approved default-config promotes, inspect lane config/routes and the
  template source rows on one target with no .lane.json. Confirm available agents
  support the selected models/efforts; override complete routes locally as needed.
  Review round 3 or later is an explicit tier choice, never an automatic fix loop.
  Ratify agent-default ordinary-review effort, or override the complete route to pin
  high. Audit partial LANE_CONFIG files because they will inherit undeclared built-ins.
- [ ] After Phase 3, install the optional board package once, serialized with other
  build/test work. Smoke-test layout, Enter focus, return, resizing, quit and Ctrl-C
  in an actual Herdr pane. Run the optional mouse study only if useful.
- [ ] After Phase 4, pilot lane new and composer using an explicit root/repo/route;
  test editor cancel, a deliberate preflight refusal, single dispatch, registered
  row selection, and close. Do not retry an ambiguous delivery without inspection.
- [ ] If a pilot fails, stop adopting new flows, retain branches/worktrees/briefs,
  and use known-good explicit open/dispatch/status paths as applicable. Use a
  reviewed fix or revert lane for tool rollback; do not rewrite history or delete
  runtime data to make a pilot appear clean. Legacy board versions do not read
  the new sidecar format; use git/Herdr directly until the compatible tool returns.

## Evidence used and remaining questions

The tracked [board report](LANE-BOARD-20260908.md), [check report](LANE-CHECK-20260908.md),
all three board review rounds, both check review rounds, and the drafted roots brief
were read. Current code confirmed the direct UI I/O, legacy environment-root
semantics, and match-any-output subscription. The installed Herdr help/schema
confirmed focus-by-name-or-pane, session enumeration, repo-scoped worktree listing,
and protocol 20's lack of a branch field in workspace metadata or a structured
last-assistant-message result. No live focus, agent prompting, or UI probe was run.

The prior review findings are carried forward explicitly: ignored runtime state
must not dirty promotion; git ownership must not come from workspace labels;
shutdown must remain permanent; asynchronous sampling and Unicode display-width
handling address the two deferred board nits; prepare failures produce no new gate.
New automatic report paths use `docs/reports/<topic>.md` so work crossing midnight
does not change the convention. Existing reports and explicit legacy paths remain
readable. Only roots-config retains the supplied dated roots report path;
roots-identity and mouse use docs/reports/roots-identity.md and
docs/reports/board-mouse.md.

Resolved design choices are in the adjacent specs, not left to implementers to
guess. These questions remain explicitly bounded:

- Does the operator approve A1-A5 for the lanes named above? Roots-config needs
  no amendment; canonical-config resolution is its documented behavior change.
  Until approval, only the named dependent lanes are blocked on those amendments.
- Which local Herdr server endpoints are accessible in the intended operator session?
  Discovery reports partial coverage; it must never silently start servers.
- Which installed Codex/Claude screen variants yield confident assistant previews?
  The message lane supplies sanitized fixtures and uses unavailable for unknown
  variants; it does not promise a full transcript.
- Is mouse input usable through the actual terminal/Herdr setup? The optional
  two-hour study answers that with go/no-go/defer evidence.
- Which exact compatible @inkjs/ui release should be locked at implementation?
  The UX lane verifies the published package under Node 20 without a major Ink upgrade.

Actual root names, routes, identity values and editor choices belong to operator
rollout and are intentionally absent here.

## Verification history

### Initial planning and review baseline

Baseline HEAD equaled local main at `c9d829cc2415e54ad1bf5c810e51625804c98714`.
One direct `npm test` passed **49/49**, zero failures/skips, in **5.290 s** before
edits. No CLI, board runtime, tests, package manifests, lockfiles, or product-contract
text were changed. The final commit must run `lane check` with its ordinary
`npm test` validator; its count must remain 49. This required commit-bound run is
separate from the one direct baseline invocation, and the final GATE is reported in
the conversation after the commit so recording it cannot stale itself.

OpenSpec 1.6.0 strict validation passed **10/10 items** (nine product changes plus
the existing safety spec), serialized with concurrency 1. Its initial rejection
of a spec-less mouse change exposed the installed/upstream difference; the final
scaffold uses the standalone research brief. No product behavior changed, so no
product regression or deliberate-failure suite was added/run for this prose task.
Document/link/source consistency and git diff --check are checked before commit.

The first post-commit check measured `46eceb6` and passed 49/49, but overlapped an
unrelated active build: the load probe returned nonzero and the orchestration
incorrectly continued. That run did not satisfy the machine-exclusive load rule.
The replacement check at e0cb38c passed 49/49 in 5.236 s after a clear load probe.
That result is also the unchanged-tree baseline for this revision, confirmed by the
review and existing gate. No other session's processes were stopped.

Environment: Node.js 20.19.4, npm 10.8.2, git 2.54.0. Node 20.0/git 2.38 minimums,
live dispatch/focus, terminal restoration, mouse, alternative hosts, real cross-root
rollout, and future feature acceptance have not been exercised by this planning lane.

### Facilitator round-two corrections

Resolved the approval wording; split 2b into read interfaces and actions/UI rewire;
recorded discovery/UX split boundaries and corrected dependency edges. Standardized
the two later report paths, required usage/README-table coverage, kept amendment
text in one place, and bounded workspace labels. Added docs/REVIEW-*.md to the
repository ignore rules while keeping public docs/reviews/ records trackable.

The ignore-path negative check returned nonzero before the edit; afterward the
private-review pattern matched and the public review path remained unignored.
No product code, test, package or AGENTS.md change was made. Per the facilitator's
run budget, use the existing 49/49 baseline and run lane check only once after the
final commit, following a clear load probe. The final GATE belongs in the response.
Strict OpenSpec validation passed 11/11 items with telemetry disabled and
concurrency 1: ten changes plus lane-safety. Document links, single-task briefs,
public-safe text, and git diff --check also passed inspection.

### 2026-09-09 — review protocol and built-in defaults addendum

The [review-cli change](../openspec/changes/review-cli/proposal.md) distills the
operator's local seed and supplied protocol refinement into portable requirements.
It uses explicit rounds, existing dispatch, a finite foreground timeout and a
machine-checkable PASS/NEEDS-WORK/FAIL schema. The reviewer must write the private
record. The CLI validates full SHA and clean-tree preservation, verifies sanitization
and writes the public projection. Re-executed tests-pass claims require observed
negative-control/mutation witnesses; Non-claims and Unverified remain mandatory.
No wrapper, fix loop, auto-escalation, record commit or promotion is included.

The [default-config change](../openspec/changes/default-config/proposal.md) ships
the eight requested role profiles under approved A5. Whole-route file overrides
retain precedence; lane config names built-in sources; generic templates resolve
from the target repository or installed tool. The shipped dispatch profile equals
engineer; explicit dispatch and route overrides remain independent as today.
Review-cli follows roots-config only to serialize lane.mjs edits; default-config
then follows review-cli. All interactive UI remains Ink in the terminal.

Sanitization verification is mechanical, with refusal on ambiguous or residual
private content; it cannot prove arbitrary prose contains no undeclared identity.
The operator still inspects generated public evidence. CLI help confirmed the
argument forms. The [Codex configuration reference](https://learn.chatgpt.com/docs/config-file/config-reference)
documents model_reasoning_effort but its value list stops at xhigh, while the
[Sol model page](https://developers.openai.com/api/docs/models/gpt-5.6-sol) includes
max; the engineering lane must verify installed-client support for the requested
routes. No live agent, account entitlement or review publication was exercised.

This addendum changes specification/report/handoff files only. The existing clean
55cd44c gate (49/49 tests) is the baseline; the single new lane check is reserved
for the final commit after a fresh clear machine load probe. Its exact GATE is
reported in the conversation so recording it cannot stale the commit.

Strict OpenSpec validation passed 13/13 items (twelve proposed changes and lane-safety)
with telemetry disabled and concurrency 1 after a clear load probe. Local links,
anchors, task structure, public-text review and git diff --check passed. No product
regression or negative-control suite was added/run for this specification-only change.

### 2026-09-09 — third review corrections and main rebase

Rebased onto roots-config's promoted main at 5da8868, preserving both handoff
histories in original commit-time order. The facilitator's committed r3 record
remains byte-for-byte unchanged and still reviews d0c4e53, not the rebased/new HEAD.
The promoted roots review supplies the 60/60 baseline; the final check will measure
this lane's final commit once, after a clear load probe.

Added agreed review/private-public and defaults/routes-template splits, with named
whole requirements and tests per slice. R-i returns a validated private verdict
without public output; R-ii adds sanitization and the after-check publication boundary.
Sanitization now enumerates OS/declared aliases, insensitive segment/word matching,
fixed placeholders/counts, and concrete ambiguity/ASCII-markup/encoding refusals.
The restrictions and inability to certify undeclared identities remain explicit.

The remaining review findings are reflected in the briefs: system-temp-only reviewer
fixtures and no lane writes; A5's explicit public-identifier exception; review-specific
exit 2 for every refusal; documented LANE_CONFIG inheritance plus matching MODIFIED
root-configuration deltas; CLI ownership of the public projection; A3's review dispatch
wording; repository-map maintenance; configuring review before shipped defaults;
late-publication/occupied-round recovery; and operator ratification of unpinned
ordinary-review effort. The terminal Ink/no-browser sentence remains unchanged.

Strict OpenSpec validation passed 14/14 items (twelve change folders and two current
specs) after a clear load probe, telemetry disabled and concurrency 1. Local links,
anchors, task structure, complete modified blocks and split assignments passed
inspection. Product code/tests/packages/AGENTS.md match main; no product negative
controls were added or run. The final GATE is reported outside git after the commit.
