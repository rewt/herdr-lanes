**PASS**
Schema: lane-review/v1
Reviewed commit: 50d684a2d641262a09b77a51dabe19fc9d5764ff
Topic: review-cli-fix-2
Round: 2
Review ID: lr-a437ddd0cbc9aa7b5500077f8641032d
Base branch: main
Base commit: 8f967cbc39096c17532ff0f46b34167a56b0afe4

## Findings
- [Minor] lane.mjs:1554 - decodeReviewRescanText expands only percent octets and numeric character references, so an absolute path written in the HTML named character reference form, ampersand name semicolon, survives the residual rescan and publishes with an absolute path count of zero even though Markdown and HTML render it as a real path; Fix: also expand the named references that decode to separator and path characters, or refuse a projected payload that still holds an undecoded named reference, and add a regression projecting a named reference absolute path.
- [Minor] docs/REFERENCE.md:461 - the publication paragraph still tells reviewers that encoded looking quoted text is not a refusal category, but lane.mjs:1621 now decodes before the residual check, so an ordinary command quoting a percent encoded absolute path is refused with exit 2 and the finished round is lost; the same URL published at c496e3c and refused at the reviewed commit; Fix: state the decoded exception in REFERENCE and the template, or narrow the decoded rescan to alias detection, where the literal pass cannot redact, and leave absolute paths to the literal pass that already replaces them.
- [Minor] test/lane.test.mjs:3341 - the documentation assertion added for the backslash half of round 1 finding 3 cannot fail, because the same sentence already contains the word backslash inside backslash escapes, so deleting backslash from the documented escape list in both REFERENCE and the template still passes while deleting backtick from that list fails; Fix: assert the ordered list text itself, for example escapes every backslash, backtick, so the omission is detectable.

## Re-executed
```json
[{"command":"npm test","cwd":"scratch","exit_code":0,"result":"114 of 114 tests passed with zero skips in 108.198 seconds, wall clock 109 seconds, in a system temporary copy of the reviewed commit whose lane.mjs and test file hash identically to the commit; exit status 0 was captured directly.","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"0 passed and 114 failed in 103.072 seconds, wall clock 103 seconds; exit status 1 was captured directly.","observed_failure":"With every deliberate control armed the suite reported 0 passes and 114 failures, including controls named joined board row and plain board rendering, so the green run is not vacuous."}},{"command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"0 passed and 114 failed in 103.072 seconds. Recorded as the deliberate negative control, not as a passing run.","tests_pass":false,"witness":null},{"command":"OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"scratch","exit_code":0,"result":"17 of 17 items passed strict validation, including change review-cli and spec foreground-lane-review; exit status 0 was captured directly.","tests_pass":false,"witness":null},{"command":"node --test --test-name-pattern \"^PROBE[12]\"","cwd":"scratch","exit_code":0,"result":"Underscore wrapped declared, user and host aliases were each redacted, counts user 1, host 1 and private 2, and a declared alias wrapped in underscores inside an unrelated word was redacted too. A user alias between percent encoded separators, a user alias holding one percent encoded letter, and a doubly percent encoded separator each exited 2. A numeric character reference separator redacted the alias normally. An absolute path written with named character references published unchanged with an absolute path count of zero.","tests_pass":false,"witness":null},{"command":"node --test --test-name-pattern \"^PROBE[3456]\"","cwd":"scratch","exit_code":0,"result":"A reviewer backslash before a backtick and before an underscore round tripped exactly through the documented reverse mapping. All four generated placeholders followed by an opening parenthesis were escaped. A command quoting a percent encoded absolute path exited 2, while a lone percent encoded separator and a percent literal followed by a space both published.","tests_pass":false,"witness":null},{"command":"node --test --test-name-pattern \"^PROBE7\"","cwd":"scratch","exit_code":0,"result":"At the reviewed commit a lone solidus published unredacted with an absolute path count of zero, a two character absolute path was still replaced with a count of one, a plain absolute path inside a URL query was replaced, and the same URL with percent encoded separators exited 2 with the residual path or alias diagnostic.","tests_pass":false,"witness":null},{"command":"LANE_TEST_CLI=../prevtree/lane.mjs node --test --test-name-pattern \"^PROBE7\"","cwd":"scratch","exit_code":0,"result":"Driving the identical fixtures at commit c496e3c, the lone solidus was replaced with a count of one and the percent encoded URL published verbatim with exit 0, so both differences are introduced by the reviewed commit.","tests_pass":false,"witness":null},{"command":"node --test --test-name-pattern \"documents normalized boundaries\"","cwd":"scratch","exit_code":0,"result":"Mutation copy A, with the word backslash deleted from the documented escape list in both REFERENCE and the review template, still passed the documentation test, so the added assertion cannot detect that omission.","tests_pass":false,"witness":null},{"command":"node --test --test-name-pattern \"documents normalized boundaries\"","cwd":"scratch","exit_code":1,"result":"Mutation copy B, with the word backtick deleted from the same list, failed with the diagnostic that the input did not match the regular expression for backtick, so the loop is effective for every listed character except backslash.","tests_pass":false,"witness":null}]
```

## Non-claims
- The gate recorded at the reviewed commit is inherited evidence, not a fresh test result produced by this review, and not a verdict.
- Nothing here establishes that the sanitizer detects arbitrary undeclared private names or secrets in reviewer prose.
- Every command ran in system temporary copies, so nothing here covers untracked or ignored state inside the reviewed lane worktree.
- No Markdown or HTML renderer was executed; the rendering conclusions follow from the published byte sequences and the CommonMark treatment of character references and escaped punctuation.
- The five round 1 findings are closed in the projection path, but that agreement is not evidence that the sanitization policy is complete.

## Unverified
- No live Herdr server, real agent dispatch or reviewer session was exercised; only the offline fake reviewer fixture was used.
- The three budgeted commands ran in system temporary copies of the reviewed commit rather than inside the lane, to satisfy the rule that nothing may be created in the reviewed lane.
- Another reviewer was running the same suite on this machine, so every run waited for a clear load probe and repeat variance, flakiness and timing spread were not assessed.
- Alternate operating systems, native drive letter and UNC inputs, concurrent private writers and permission denied filesystems were not exercised.
- Timeout, interruption, dispatch failure, schema validation and unchanged lane paths were not re exercised this round; the delta does not touch them and the existing suite covers them.
- The publication size and line bounds were not measured against maximally escaped prose, where escaping can nearly double a punctuation heavy payload.
- The historical board-cli refusal that motivated the production derived fixture was not independently reproduced; the implementation report states the same limit.
- No commit, push, promotion, archive, gate run or change under the tracked public review directory was performed.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
