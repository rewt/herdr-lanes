**PASS**
Schema: lane-review/v1
Reviewed commit: a2f7bacb4bad17624fb8945e33b6fd493793ac8c
Topic: board-cli
Round: 3
Review ID: lr-5664657a19514d681e9a13cfb9252e36
Base branch: main
Base commit: 8f967cbc39096c17532ff0f46b34167a56b0afe4

## Findings
- [Minor] docs/REFERENCE.md:210 - the watch is documented as printing one document per line "for the initial sample and observed changes", but the periodic refresh installed at lane.mjs:2210 emits a complete document every 5 seconds whether or not anything changed; an offline probe against an empty registry produced three full documents in 12 seconds that were byte-identical apart from captured\_at and host, so a consumer that treats each line as a change notification sees a continuous stream of false changes; Fix: say in that paragraph that a complete document is emitted for the initial sample, for each periodic refresh (currently every 5 seconds, matching the interactive board's own interval), and for each observed event.
- [Minor] docs/REFERENCE.md:250 - last\_message and tripwire are listed as ordinary row fields, but pane output is only accumulated by the event subscription path in board/board.mjs:133 and the one-shot reads at lane.mjs:2231 call collectBoardState without a runtime map, so --once and --json can never report anything but last\_message.available false with text and observed\_at null and tripwire null; the authorized live snapshot returned 11 rows with coverage.herdr connected and all 11 in that state, which reads as "no output has been observed" rather than "this mode never observes output"; Fix: note in the row-field list that last\_message and tripwire are populated only by --watch --json, the mode that subscribes to pane events.
- [Minor] test/lane.test.mjs:205 - tab\_id is the only documented schema v1 row field with no assertion anywhere in the suite, because the fake Herdr snapshot serves panes as an empty list, so the snapshot.panes fallback in board/board.mjs:78 is never exercised offline and a regression that always returned null would still pass npm test; the live read-only snapshot showed a non-null tab\_id on all 11 rows, so the fallback is a real production path; Fix: give boardSnapshotResult a pane entry whose pane\_id is pane-1 with a tab\_id, and assert row.tab\_id beside the existing row.pane\_id assertion at test/lane.test.mjs:2625.

## Re-executed
```json
[{"command":"npm test","cwd":"scratch","exit_code":0,"result":"Executed in a system-temp export of a2f7bac whose lane.mjs, board/board.mjs and test/lane.test.mjs hash to the reviewed blobs ae9cf6a0, b61b4575 and 4d091803. TAP totals: 1..115, tests 115, pass 115, fail 0, cancelled 0, skipped 0, todo 0, duration_ms 107838.19. Output was piped to tail, so the process status was not captured separately; zero failures and zero cancellations in the node --test summary correspond to exit 0.","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"TAP totals: 1..115, pass 0, fail 115, cancelled 0, skipped 0, duration_ms 102567.50. Exit status captured directly as 1.","observed_failure":"Every one of the 115 tests failed its deliberate control assertion with zero passes, so the green run is not vacuous and every test body reaches its control call."}},{"command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"TAP totals: 1..115, pass 0, fail 115, cancelled 0, skipped 0, duration_ms 102567.50. Recorded as the deliberate negative control, not as a passing run.","tests_pass":false,"witness":null},{"command":"node --test --test-name-pattern=\"board read modes reject contradictory\" test/lane.test.mjs","cwd":"scratch","exit_code":1,"result":"Targeted mutation probe in a separate system-temp copy of a2f7bac whose only edit reverts the round-3 change, setting the repository probe stdio back to ignore for git stderr. 0 pass, 1 fail, 92 skipped.","tests_pass":false,"witness":{"kind":"mutation","command":"node --test --test-name-pattern=\"board read modes reject contradictory\" test/lane.test.mjs","cwd":"scratch","exit_code":1,"result":"0 pass, 1 fail, 92 skipped.","observed_failure":"not ok 64 - board read modes reject contradictory, duplicate, unknown, extra, and missing options before observation, with an AssertionError on the malformed-gitfile stderr expectation, so that new regression genuinely depends on the captured git stderr and would catch a revert of the first round-2 fix."}},{"command":"node --test --test-name-pattern=\"plain and JSON board projections resolve the same session context\" test/lane.test.mjs","cwd":"scratch","exit_code":1,"result":"Targeted mutation probe in a separate system-temp copy of a2f7bac whose only edit makes boardSnapshotDocument resolve git state by branch instead of through the shared session context, simulating the projection drift the round-2 finding was about. 0 pass, 1 fail, 92 skipped.","tests_pass":false,"witness":{"kind":"mutation","command":"node --test --test-name-pattern=\"plain and JSON board projections resolve the same session context\" test/lane.test.mjs","cwd":"scratch","exit_code":1,"result":"0 pass, 1 fail, 92 skipped.","observed_failure":"not ok 61 - plain and JSON board projections resolve the same session context, with an AssertionError, so the rewritten behavioral regression detects divergence between the two projections, which the previous source-text version demonstrably did not."}},{"command":"OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"scratch","exit_code":0,"result":"Totals: 18 passed, 0 failed (18 items), including spec/board-cli-interface and change/board-cli. Exit status captured directly as 0. Recorded as artifact validation, not as a test claim.","tests_pass":false,"witness":null},{"command":"node ./lane.mjs board --json --repo \"$CANON\"","cwd":"scratch","exit_code":0,"result":"The single authorized live read against the running Herdr server, executed from the system-temp export with the shell variable set to the canonical checkout. Exit 0, empty stderr, exactly one stdout line. schema_version 1; top-level keys schema_version, captured_at, scope, coverage, repositories, rows, host, errors; scope.kind repository; coverage repository complete, registry available, herdr connected, messages pane-output; errors empty; 11 rows with 11 unique row_ids; every documented row key present, including a non-null tab_id on all 11 rows; gate states only pass or missing; git.available true on every row. The canonical checkout's porcelain status hashed identically before and after the run. No row content is reproduced here.","tests_pass":false,"witness":null},{"command":"node ./lane.mjs board --watch --json","cwd":"scratch","exit_code":0,"result":"Offline temporary repository with an empty registry and an absent Herdr socket, terminated by SIGTERM after 12 seconds. Exit status captured directly as 0, stderr 0 bytes, three complete JSON documents with captured_at at 05:48:53.230Z, 05:48:58.295Z and 05:49:03.293Z that were identical apart from captured_at and host; coverage.herdr unavailable with one localized errors entry. Evidence for the first finding and for a clean SIGTERM shutdown.","tests_pass":false,"witness":null}]
```

## Non-claims
- The green gate recorded at the captured HEAD is inherited evidence from the engineering session; nothing in this record treats it as a fresh test result or as a review verdict.
- No claim is made about live Herdr event streaming, reconnection against a real server, or the --watch mode beyond the offline fake server, the offline temporary-repository probe, and one live one-shot read.
- No claim is made about the interactive Ink board, which was neither installed nor started, nor about any behavior outside the range 8f967cb..a2f7bac.
- No claim is made about promotion, archival, or the public projection of this record; those remain operator actions outside this role.

## Unverified
- The full npm test process exit status was not captured separately because the run was piped to tail; the TAP summary reporting 115 passes with zero failures and zero cancellations is the evidence used, and the deliberate control run's exit status of 1 was captured directly.
- Live Herdr --watch --json framing, subscription installation against real panes, and reconnect after a real disconnect were not exercised; only the offline fake protocol-20 server in the suite and an offline probe with an absent socket covered the watch.
- Only macOS with Node.js v20.19.4, npm 10.8.2, git 2.54.0 and OpenSpec 1.6.0 was exercised; Linux, Windows, git 2.38, other locales for the git failure message, and a real TTY were not.
- Long-lived watch behavior beyond about 12 seconds, memory growth over many refreshes, and the 500 code point last\_message truncation path were not exercised.
- The board --repo flag combined with the interactive mode, and the LANE\_CONFIG interaction with a foreign --repo target, were not exercised; both are reachable through the accepted grammar.
- board/cli.mjs is no longer reached from lane.mjs and was only exercised indirectly through the board package tests inside npm test.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
