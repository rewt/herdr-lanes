**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: 6ac0067bd25f5338ecc71a26497396a665dcaa75
Topic: board-messages
Round: 1
Review ID: lr-4041d6c6ec0edb7a9f99dff41fea94e4
Base branch: main
Base commit: 3367cd53e6fa50d3212f0da32e2189900b9b6d21

## Findings
- [Major] board/message-preview.mjs:56 - a candidate block whose own first line is Codex status chrome is accepted as an assistant message, so an actively working pane presents its elapsed-time and esc-to-interrupt status line as LAST MESSAGE and hides the real answer above it; observed once on the live server and reproduced in a temp copy; Fix: also reject a candidate when the boundary predicate matches its captured first line, which the Codex boundary at line 32 already recognizes; that one-line change was verified in a temp copy where the real answer is returned and all five committed parser tests still pass.
- [Major] lane.mjs:2427 - board pane reads reuse the 500 millisecond snapshot request timeout and are issued for every registered pane at once, so on the live server 8 of 9 registered Claude panes returned pane.read timeouts and showed no message while all 8 Codex panes succeeded; Fix: give pane reads their own larger timeout instead of the snapshot budget, bound read concurrency at the Promise.all in the preview module line 190, and distinguish a timeout from an unsupported read in the limitation text; the watch client at line 2257 needs the same budget.
- [Major] board/message-preview.mjs:8 - trailing chrome outside the small boundary list is appended into the message text, so a shortcut bar without the middle-dot separator, a context-left bar, a token and cost bar, a Codex approval prompt, and the box-drawing composer frame each became part of an otherwise correct answer in temp probes covering both kinds; the committed fixtures pass only because every one of them interposes a plain horizontal divider or a shortcuts hint between the answer and the footer; Fix: add the box-drawing frame characters to the divider pattern, add explicit context, token, shortcut and approval boundary patterns, and end a block at the first line that is not plausible message content.
- [Moderate] board/message-preview.mjs:37 - a block is classified as a tool call from its first word alone, so common Codex answer openings such as Added, Updated, Applied, Opened, Read and Found are discarded and the preceding assistant block is presented as the current message with a fresh observation time, a fresh revision and stale false; the Claude list at line 49 does the same for openings such as Read, Search, Save and Edit; Fix: require corroborating tool evidence, such as a following indented result line or an argument-shaped target on the same line, before classifying a block as a tool call.
- [Moderate] board/message-preview.mjs:42 - the bare composer prompt boundary and the Codex block start both match at any indentation, so an answer containing its own indented quoted line is silently truncated at that line and an answer containing its own indented bullet list is split so only the trailing fragment survives, each still labeled available with truncated false; Fix: anchor the bare prompt marker and the block start marker at line 84 to the first column so indented quotes and nested bullets remain continuation text.
- [Moderate] docs/REFERENCE.md:274 - the retained sentence still tells consumers that last\_message is populated only by the live watch subscription, contradicting the delivered one-shot parity, the updated paragraph at line 365, and the committed parity test; Fix: narrow that sentence so it constrains tripwire alone.
- [Minor] lane.mjs:2432 - the plain one-shot renderer is never given the collected message errors, so a failed pane read is indistinguishable from a pane with no message in plain output even though the JSON read reports it; Fix: pass the collected message errors into the plain renderer beside the registry errors and print them as notices.
- [Minor] board/message-preview.mjs:143 - the fallback occupant identity mixes the mutable agent revision into pane occupancy, so for an agent lacking an agent session field a revision bump makes the stale-retention branch at line 226 miss and replace an otherwise retainable preview with unavailable; Fix: drop the revision element from the fallback identity tuple, because a changing output counter is not a change of occupant.
- [Minor] docs/reports/board-messages.md:64 - the final full-suite and negative-control figures of 133 were measured before the last rebase, while the reviewed commit's suite contains 134 tests, so the report and the matching HANDOFF entry do not describe the delivered commit; Fix: restate both counts as measured at the reviewed commit.

## Re-executed
```json
[{"command":"npm test","cwd":"lane","exit_code":0,"result":"134 tests, 134 pass, 0 fail, 0 skipped, duration_ms 113587.961167; run with the inherited GIT_CONFIG_COUNT, GIT_CONFIG_KEY and GIT_CONFIG_VALUE variables cleared","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"lane","exit_code":1,"result":"134 tests, 0 pass, 134 fail, 0 skipped, duration_ms 108626.128458","observed_failure":"every one of the 134 tests failed on its deliberate control assertion, so the green suite is not vacuous and each test carries a control"}},{"command":"OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"lane","exit_code":0,"result":"Totals: 20 passed, 0 failed (20 items); the new current spec validates alongside the existing changes","tests_pass":false,"witness":null},{"command":"./lane.mjs board --json","cwd":"lane","exit_code":0,"result":"17 rows against the live server; coverage.messages partial; 8 localized errors with source message, all of them pane.read timeouts, covering 8 of the 9 registered Claude panes; all 8 Codex panes produced a preview and every one reported truncated true; one Codex preview was a single status line rather than an answer","tests_pass":false,"witness":null},{"command":"git diff --check 3367cd5..6ac0067","cwd":"lane","exit_code":0,"result":"no whitespace or conflict-marker damage in the reviewed delta","tests_pass":false,"witness":null},{"command":"node probe1.mjs","cwd":"scratch","exit_code":0,"result":"a Codex answer opening with Added yielded the previous block instead; a Claude composer frame and a Codex approval prompt were both appended into an otherwise correct answer, each labeled assistant-preview with available true","tests_pass":false,"witness":null},{"command":"node probe2.mjs","cwd":"scratch","exit_code":0,"result":"a Claude answer containing an indented quoted line was truncated at that line; a Codex answer containing an indented bullet list was reduced to its trailing fragment; a Codex answer opening with Found yielded the previous block","tests_pass":false,"witness":null},{"command":"node probe3.mjs","cwd":"scratch","exit_code":0,"result":"a Codex shortcut bar without the middle-dot separator, a Claude context-left bar and a Claude token and cost bar were each appended into the message text","tests_pass":false,"witness":null},{"command":"node probe4.mjs","cwd":"scratch","exit_code":0,"result":"reproduced the live observation: a Codex status block displaced the real two-line answer and became the whole message","tests_pass":false,"witness":null},{"command":"node --test","cwd":"scratch","exit_code":0,"result":"5 tests, 5 pass, 0 fail against a temp copy of the module carrying the recommended one-line boundary rejection","tests_pass":true,"witness":{"kind":"mutation","command":"node check.mjs","cwd":"scratch","exit_code":0,"result":"the patched temp copy returns the real two-line answer where the reviewed code returns the status line","observed_failure":"a behavior-changing mutation of the extraction predicate left all five committed parser tests passing, so the committed suite does not constrain the status-line case at all"}}]
```

## Non-claims
- I do not claim a larger timeout would make the failing Claude pane reads succeed; I observed only that the client-side budget expired before a reply arrived.
- I do not claim the interactive board renders a multiline detail panel; the current UI has none, and the bounded multiline text reaches consumers only through the JSON text field.
- I do not claim the reviewer-authored probe shapes reproduce every real Codex or Claude screen; they are synthetic, and one of the failures they predicted was independently confirmed on the live server.
- I do not claim any defect in the tripwire subscription, the action boundary, or the child-process signal handling inherited from the base commit.
- I do not claim the message errors observed once on the live server are permanent or reproducible on other hosts.

## Unverified
- The interactive board was never launched; no terminal render, focus action, or done action was exercised, and no alternate host was used.
- Watch mode was exercised only through the committed fake-server test; the one-read-per-pane-per-second coalescing timer and the five-second sampling interval were not independently measured against the live server.
- The 16 KiB byte bound and the 200-line request were confirmed from the code and the committed tests, not by forcing an oversized live read.
- No mutation was applied inside the lane worktree; the candidate fix was verified only in a system temp copy, which was removed afterwards.
- Process exit statuses for the suite, the control and the validator were not captured separately because output was piped; their outcomes rest on the printed summary counts quoted above.
- Only one live board read was authorized, so the Claude pane read failures were observed once and could not be retried, bisected, or attributed to a specific cause.
- Herdr protocol semantics for the agent revision field were not verified, so the severity of the fallback identity finding is bounded by that uncertainty.
- No push, promotion, archive, close, dispatch, or public record write was performed or attempted.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
