**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: f2cb31b1a94d9e9fff55dfe2c56b318b9dad3c45
Topic: board-messages
Round: 6
Review ID: lr-b623d40ec334c58613641503d163a110
Base branch: main
Base commit: 3367cd53e6fa50d3212f0da32e2189900b9b6d21

## Findings
- [Major] board/message-preview.mjs:17 - the trailing rendered status form is now accepted only when the elapsed time is the final token, optionally followed by one closing round or square bracket, so a progress line whose duration is followed by any further status field is no longer chrome and is published as the current assistant message; five committed chrome fixtures reproduce this after a one-field edit that keeps their exact shape, namely a Working line with a duration then a separator then a token count, a Working line with a duration then a separator then a file count, a Worked line with a bracketed duration then the word total, a Running line with a duration then the word remaining, and a Thinking line with a duration then a separator then a tool count; both the round-4 and round-5 modules rejected all of them for both adapters, and driving refreshMessagePreviews with a fake protocol-shaped client publishes the status text with source \`assistant-preview\`, \`available\` true, \`stale\` false, \`truncated\` false, \`limitation\` null and a fresh observation time, replacing a genuine answer, while the same line indented under an answer is appended into that answer's text instead of ending the block; this reinstates the round-1 Major in which an actively working agent's status becomes the message, and the committed status corpus pins only durations that end the line so no test observes it; Fix: treat the trailing group as status when the elapsed time ends it, when the group is parenthesized or bracketed and contains an elapsed time anywhere, or when the group contains both an elapsed time and a rendered field separator such as a middle dot, a bullet or a vertical bar, and add paired fixtures for a duration followed by a token count, by a file count, by the word total and by the word remaining; a scratch copy carrying exactly that rule kept all 29 committed groups green, rejected eight of the nine probed leaking forms in both adapters, and still returned all eight progress-verb prose answers including the round-5 three-period sentences
- [Minor] test/message-preview.test.mjs:69 - the new corpus pairs only 30 hand-listed chrome forms, so the pairing rule the commit advertises is not enforced for the parser's remaining chrome patterns; the shortcut bar at board/message-preview.mjs:12, the context bar at line 13, the token or cost bar at line 14, the Claude spinner row and the Codex approval and footer rows are still consulted on a candidate block's own first line and have no twin answer fixture, so ordinary answers opening with the words Context left, with a percentage before those words, with Tokens are used, with Tokens cost, with esc to toggle, with a question mark, with a middle-dot bullet under the Claude adapter, or with the Codex trailer shape are discarded and the older superseded block is published as the current message with no staleness marker; deleting one whole twin entry from the array left the suite at 29 of 29, and adding a new unpaired boundary rule to the Claude adapter that discards an ordinary answer also left the suite at 29 of 29, so nothing observes an unpaired addition and the first-line rule lives only in the two boundary comments; this exposure predates the round-6 delta and the round-5 record disclosed and declined the context and token cases; Fix: add twin answer fixtures for each remaining chrome rule, require those patterns to match only indented or fully anchored rendered rows the way the approval pattern now does, and drive every boundary pattern through the corpus mechanically so a future unpaired rule fails the group
- [Minor] docs/REFERENCE.md:294 - the sentence stating that indented approval prompts are chrome now describes the Claude adapter only, because the Codex boundary at board/message-preview.mjs:77 still applies the unindented approval pattern, so a Codex answer opening with Would you like to run, Do you want to run, Do you want to allow, Do you want to approve, or Allow Codex to is discarded at any indentation and the older block is published as current, which the public documentation does not state and which the paired corpus records only by restricting its five approval pairs to the Claude adapter; Fix: say that approval-worded openings are excluded for Codex at any indentation while only indented approval prompts are chrome for Claude, and pin a Codex paired fixture that records the intended asymmetry

## Re-executed
```json
[{"command":"npm test","cwd":"scratch","exit_code":0,"result":"161 tests, 161 pass, 0 fail, 0 skipped, duration_ms 113586.102667, 114 s wall, against a fresh system-temp copy whose tree hash equals the reviewed commit's tree, with the inherited GIT_CONFIG_COUNT, GIT_CONFIG_KEY_n and GIT_CONFIG_VALUE_n variables cleared; this matches the report's declared 161 of 161 with zero skips","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"161 tests, 0 pass, 161 fail, 0 skipped, 109 s wall; 161 lines quoted the deliberate control message and no Unix-socket EPERM occurred, since this host permits the system-temp sockets that the engineer's sandbox refused","observed_failure":"every one of the 161 tests failed on its own deliberate control assertion with zero passes and zero skips, so the green suite is not vacuous and the new paired-fixture group carries an effective control"}},{"command":"OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"scratch","exit_code":0,"result":"Totals: 20 passed, 0 failed (20 items) with OpenSpec 1.6.0, matching the report; the process exit status was captured directly","tests_pass":false,"witness":null},{"command":"./lane.mjs board --json","cwd":"lane","exit_code":0,"result":"27 rows, coverage repository complete, registry available, Herdr connected and messages assistant-preview, zero errors; 13 rows produced an available preview and 14 were unavailable; no available preview contained an interrupt affordance, a context bar, a token or cost bar, or any progress verb carrying a duration, and the one preview opening with the word Running holds no digit, bracket or duration and is ordinary prose; the lane worktree reported zero changes immediately before and after the read","tests_pass":false,"witness":null},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"29 groups, 29 pass, 0 fail, 0 skipped in 0.04 s against the unmodified temp copy, matching the report's 29 groups and its 0.044 second figure; the array holds 30 tuple rows across four map groups, which expand to the reported 43 adapter pairs as 10 status pairs and 3 tool-summary pairs for both adapters, 12 tool-word pairs and 5 approval pairs for one adapter each","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"29 groups, 0 pass, 29 fail, 0 skipped, with 29 deliberate control messages including the paired chrome and answer invariant","observed_failure":"all 29 parser groups failed on their deliberate control assertion with zero passes, so the focused corpus controls are present and effective"}},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"28 pass, 1 fail against a copy whose parser module was replaced by the parent commit's blob while the reviewed test file was kept; the single failure is exactly the new paired chrome and answer group, reproducing the report's claim that the focused file passed 28 of 29 and failed on the new invariant","tests_pass":false,"witness":null},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"28 pass, 1 fail against a copy in which only the Claude approval indentation fix was reverted; the new paired group is the sole failure, so that half of the round-6 correction is independently observed","tests_pass":false,"witness":null},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"28 pass, 1 fail against a copy in which only the elapsed-time-at-end fix was reverted; the new paired group is again the sole failure, so both halves of the correction are independently observed by committed fixtures","tests_pass":false,"witness":null},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"29 pass, 0 fail against a copy of the reviewed tree with one whole twin entry removed from the paired array, namely the three-period Thinking pair; deleting a twin therefore does not fail the suite, so the invariant is a hand-maintained list rather than a self-enforcing rule","tests_pass":false,"witness":null},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"29 pass, 0 fail against a copy whose Claude boundary gained one new unpaired chrome rule; a direct call then showed that rule discarding an ordinary answer and publishing the older block, so an unpaired chrome addition is not observed by any committed test","tests_pass":false,"witness":null},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"29 pass, 0 fail against a copy carrying the finding-1 candidate rule, which adds a bracketed-group branch and a field-separator branch beside the elapsed-time-at-end branch; the committed corpus stays green under the proposed fix","tests_pass":false,"witness":null},{"command":"node p8-fix.mjs","cwd":"scratch","exit_code":0,"result":"under that candidate rule eight of the nine probed leaking status forms are rejected again in both adapters, while all eight progress-verb prose answers are still returned, including the four round-5 three-period sentences, the round-3 parenthesized Working answer and the two round-3 duration answers; the residual leak is a bare three-period Thinking line whose duration is followed by a single trailing word and no separator, so the proposal is a narrowing rather than a complete classifier","tests_pass":false,"witness":null},{"command":"node p1-findings.mjs","cwd":"scratch","exit_code":0,"result":"both round-5 findings are resolved: all five Claude approval-worded openings are returned as the current message where the parent commit published the older block, the five indented approval prompts remain chrome for both adapters, and all five progress-verb sentences carrying three periods or an ellipsis plus a later duration are returned for both adapters; the seven named status forms including the interrupt-advertising ones remain rejected; the Codex approval-worded opening is discarded in the round-4, round-5 and reviewed modules alike, so that asymmetry is unchanged by this delta","tests_pass":false,"witness":null},{"command":"node p2-holes.mjs","cwd":"scratch","exit_code":0,"result":"eight status forms whose duration is not final were compared across the three modules; seven are rejected by the round-5 module and leak as the message under the reviewed module for both adapters, and one form whose bracket closes before a trailing field leaked in all three and therefore predates this delta; separately, candidate openings beginning with the words Context left, a percentage then those words, Tokens are used, Tokens cost, esc to toggle, a question mark, or the Codex trailer shape are discarded in the reviewed module, while a leading run of six hyphens survives","tests_pass":false,"witness":null},{"command":"node p3-e2e.mjs","cwd":"scratch","exit_code":0,"result":"through refreshMessagePreviews with a fake protocol-shaped client, a Working line whose parenthesized group holds a duration then a separator then a token count is published as the current message with source assistant-preview, available true, stale false, truncated false, limitation null and the read revision, replacing the genuine answer above it, so the leak carries no staleness or limitation signal","tests_pass":false,"witness":null},{"command":"node p4-cont.mjs","cwd":"scratch","exit_code":0,"result":"the same four forms placed as indented continuations after a real answer are appended into the message text under the reviewed module for both adapters, where the round-5 module ended the block cleanly; the committed shape whose duration ends the group still ends the block","tests_pass":false,"witness":null},{"command":"node p6-diff.mjs","cwd":"scratch","exit_code":0,"result":"152 single-line bodies harvested from the committed corpus were driven through both modules in candidate, continuation and answer positions for both adapters, giving 912 comparisons and 54 classification differences; every difference is one of the two intended fixes, being five progress-verb prose bodies across three positions and both adapters and twelve Claude approval-worded bodies across two positions, so no other round-1 through round-5 answer or chrome example changed classification at the reviewed commit","tests_pass":false,"witness":null},{"command":"node p7-pin.mjs","cwd":"scratch","exit_code":0,"result":"five committed chrome fixtures were compared with minimal variants that keep their shape and replace or append one status field; each committed fixture is still rejected while every variant leaks as the message under the reviewed module and was rejected by both earlier modules, for both adapters; one further variant built from the Consolidating fixture leaked in all three modules because that verb is not in the progress list and was covered only by the interrupt affordance","tests_pass":false,"witness":null},{"command":"git diff --check 3367cd5 f2cb31b","cwd":"lane","exit_code":0,"result":"no whitespace or conflict-marker damage in the reviewed delta; board/app.mjs is byte-identical to main and to the base commit by blob hash; no file under docs/reviews changed between the parent record commit and the reviewed commit; the round-6 delta touches only the parser, its test file, the report and HANDOFF, leaves the 200-line request, the 16 KiB retention bound, the 500 code-point excerpt bound and the four-reader pool identical to the parent, changes no client, board, view or CLI file, and leaves the tripwire live-only sentence untouched","tests_pass":false,"witness":null}]
```

## Non-claims
- I do not claim the status leak occurs in a real render; it was reproduced from reviewer-authored text shaped like the committed chrome fixtures, and the single authorized live read showed no duration-bearing status line in any of the 13 available previews.
- I do not claim the proposed rule for finding 1 is a complete classifier; one probed form still leaks under it, and it exists to show that a narrow fix closes the regression without reopening the round-5 prose finding.
- I do not claim the exposure in finding 2 is new; the round-5 record disclosed the same class for the context and token rules and deliberately declined to raise it, and my deletion and unpaired-rule experiments only measure what the new group does and does not observe.
- I do not claim the Claude approval narrowing is wrong; it is the fix the round-5 record proposed, it is now pinned by five paired fixtures, and the indented prompt remains chrome.
- I do not claim any defect in the read bounds, the reader pool, the pane read timeout, occupant replacement, stale retention on read errors, the tripwire subscription, the action boundary or the interactive UI; the round-6 delta changes none of them.
- I do not claim the report or HANDOFF counts are wrong; both declare 161 tests and 29 focused groups, and my runs measured 161 with zero skips and 29 of 29, along with the claimed 28 of 29 pre-change failure and the 30 fixtures expanding to 43 adapter pairs.
- I do not claim a post-commit gate failure exists for this round; the report and HANDOFF say only that the gate remains conversation-only evidence and may meet the known sandbox socket restriction, and the supplied facilitator gate for this commit exited zero, which is inherited evidence rather than a fresh test claim.
- I do not claim the engineer's sandbox socket restriction is reproducible here; this host permits the system-temp sockets and all 161 negative-control cases reached their deliberate control.
- I do not claim the interactive board renders these previews correctly; no terminal UI was launched in this round.

## Unverified
- The interactive board, focus and done actions, alternate hosts and any real TTY render were not exercised.
- Only one live board read was authorized, and it returns parsed previews rather than raw rendered text, so the real frequency and exact shape of in-flight status rows on live panes remain unknown and the leak could not be hunted there.
- The report's untouched baseline of 160 of 160 for the parent commit was not re-run, because the single authorized full-suite run was spent on the reviewed commit.
- Watch-mode coalescing at one read per pane per second and the five-second sampling interval were not measured against the live server.
- The 200-line request, the 16 KiB retention bound, the four-reader pool and the pane read timeout were confirmed from the code and the committed read-state groups, not by forcing an oversized or slow live read.
- All budgeted suite runs used a fresh system-temp copy whose tree hash equals the reviewed tree, not the lane worktree, so behavior depending on the lane's own history was not exercised.
- Another project's suite was running when this review began; every budgeted run started only after a probe showed no build or test process, but the sub-second node probes were not serialized against other projects.
- No push, promotion, archive, close, dispatch, fix, commit or public record write was performed or attempted; the lane worktree reported zero changes and the same HEAD before and after every step, and all scratch fixtures were removed.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
