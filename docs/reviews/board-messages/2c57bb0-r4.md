**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: 2c57bb03532fd75368c935c0605e038b85f33f90
Topic: board-messages
Round: 4
Review ID: lr-87643f1850288ba024d66c7121eb67cc
Base branch: main
Base commit: 3367cd53e6fa50d3212f0da32e2189900b9b6d21

## Findings
- [Moderate] board/message-preview.mjs:22 - the in-flight tool-summary boundary matches only one fixed clause sequence, requiring the searched, read, listed and ran clauses all present in that order, so an otherwise identical summary that omits or reorders a clause is still appended into the published message; driving refreshMessagePreviews with a fake client, an indented line reading \`Searched for 2 patterns, read 3 files\` under a Claude answer produced source \`assistant-preview\`, \`available\` true, \`truncated\` false and \`limitation\` null with that tool line inside the message text for both adapters, which is the same class as the round-3 live observation, while the committed group at test/message-preview.test.mjs:293 pins only the four-clause form and a generalized clause pattern left all 28 committed groups passing; Fix: recognize an indented line built only from comma-separated count clauses, each a listed verb followed by an integer and a short noun phrase, instead of the single captured sentence, and add fixtures for a two-clause summary and a reordered summary; if the single-shape limit is kept deliberately, say so at docs/REFERENCE.md:289 instead of describing a covered shape in general terms
- [Minor] board/message-preview.mjs:92 - the Claude boundary list never consults the approval-prompt pattern that the Codex boundary uses at line 73, so an indented approval prompt under a Claude answer is appended into the message text with \`available\` true, \`truncated\` false and \`limitation\` null, contradicting docs/REFERENCE.md:294, which states without qualification that indented approval prompts are chrome, and docs/reports/board-messages.md:140, which presents the round-2 indented-prompt finding as resolved; the committed chrome group covers the indented prompt for Codex only, and adding the pattern to the Claude boundary left all 28 committed groups passing; Fix: add the approval-prompt test to the Claude boundary beside the context and token bars and extend the chrome group with an indented Claude approval prompt, or scope both documentation sentences to the Codex adapter
- [Minor] board/message-preview.mjs:18 - the trailing-status form accepts a parenthesized group, a bracketed group, or text following a single horizontal ellipsis character, but not the three-period spelling of the same ellipsis, so a status line reading Thinking, three periods, then 12s is accepted as an assistant message in both adapters and displaces the real prior answer, which commit 40d5382 rejected; the committed group at test/message-preview.test.mjs:191 pins only the ellipsis-character form, and accepting three periods wherever the ellipsis character is accepted restored the rejection while all 28 committed groups still passed; Fix: treat three consecutive periods as the ellipsis in the trailing-status form, or normalize a three-period run to the ellipsis character while cleaning pane text, and pin the three-period status line with a fixture

## Re-executed
```json
[{"command":"npm test","cwd":"scratch","exit_code":0,"result":"160 tests, 160 pass, 0 fail, 0 skipped, duration_ms 119472.069834; run against a fresh single-commit system-temp copy of the reviewed tree with the inherited GIT_CONFIG_COUNT, GIT_CONFIG_KEY_n and GIT_CONFIG_VALUE_n variables cleared; the wrapper lost the pipeline status, so exit zero was read from npm's own run log for that working directory, which recorded verbose exit 0 and info ok","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"160 tests, 0 pass, 160 fail, 0 skipped, duration_ms 115767.066833; 160 not-ok lines and 160 distinct deliberate control messages, and no Unix-socket EPERM occurred outside the engineer's sandbox","observed_failure":"every one of the 160 tests failed on its own deliberate control assertion with zero passes and zero skips, so the green suite is not vacuous and each test carries a control"}},{"command":"OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"scratch","exit_code":0,"result":"Totals: 20 passed, 0 failed (20 items); the synchronized current agent-message-preview spec validates alongside the existing changes; this one command overlapped another lane's focused node test run because the probe and the run were issued together, so its serialization discipline was breached even though the pass or fail outcome does not depend on timing","tests_pass":false,"witness":null},{"command":"./lane.mjs board --json","cwd":"lane","exit_code":0,"result":"23 rows, coverage messages assistant-preview, registry available, Herdr connected, zero errors and zero pane read failures; 9 rows produced an available preview and all 9 were genuine assistant answers with no status line, footer bar, approval prompt, composer frame or tool-summary line inside any message text; the lane worktree was clean immediately before and after the read","tests_pass":false,"witness":null},{"command":"git diff --check 3367cd5 2c57bb0","cwd":"lane","exit_code":0,"result":"no whitespace or conflict-marker damage in the reviewed delta; board/app.mjs is byte-identical to main by blob hash; no file under docs/reviews changed between abc12e5 and the reviewed commit; under openspec only the lane's own task checkbox and the verbatim synced current spec changed; the added lines contain no absolute path, account, host or socket path, only an elided board socket name in report prose and a fake socket name in fixtures","tests_pass":false,"witness":null},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"28 groups, 28 pass, 0 fail, 0 skipped, duration_ms 43.413666 against the unmodified temp copy, matching the report's 28 groups and its 0.043 second figure","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"28 groups, 0 pass, 28 fail; 28 deliberate control messages","observed_failure":"all 28 parser groups failed on their deliberate control assertion with zero passes, so the parser file's controls are present and effective"}},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"15 pass, 13 fail against a temp copy whose parser module was swapped for the pre-round-4 blob at 40d5382 while the reviewed test file was kept; the 13 failures are exactly the round-4 additions, being the ten cumulative answer-corpus entries plus the trailing partially rendered command, bounded live tool-summary and bare tree-marker groups, which reproduces the engineer's reported 15 of 28 and confirms the fixtures were failing before the parser change","tests_pass":false,"witness":null},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"28 groups, 28 pass against a copy patched so the tool-summary boundary accepts any indented run of comma-separated count clauses; a first attempt at that patch failed 1 of 28 because it did not allow a two-word noun phrase, and the corrected patch passes all 28","tests_pass":true,"witness":{"kind":"mutation","command":"node probe.mjs","cwd":"scratch","exit_code":0,"result":"on the corrected patched copy a two-clause in-flight summary is excluded from the message for both adapters while the committed four-clause fixture keeps its committed result","observed_failure":"a behavior-changing patch that excludes the two-clause summary left all 28 committed groups passing, so no committed test observes the variant shape"}},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"28 groups, 28 pass against a copy patched so the Claude boundary also consults the approval-prompt pattern","tests_pass":true,"witness":{"kind":"mutation","command":"node probe.mjs","cwd":"scratch","exit_code":0,"result":"on the patched copy the indented approval prompt no longer appears in the Claude message text, while the Codex result is unchanged","observed_failure":"a behavior-changing patch that removes the appended Claude approval prompt left all 28 committed groups passing, so no committed test observes that adapter gap"}},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"28 groups, 28 pass against a copy patched so the trailing-status form also accepts three periods in place of the ellipsis character","tests_pass":true,"witness":{"kind":"mutation","command":"node probe.mjs","cwd":"scratch","exit_code":0,"result":"on the patched copy the three-period status line is rejected again for both adapters and the real prior answer is returned, while the ellipsis-character and parenthesized forms keep their committed results","observed_failure":"a behavior-changing patch that restores the three-period rejection left all 28 committed groups passing, so no committed test observes that regression"}},{"command":"node p1.mjs","cwd":"scratch","exit_code":0,"result":"a 44-row by 4-commit matrix compared the reviewed module with the three earlier module blobs in this lane over every recorded answer and chrome example rendered for both adapters; at the reviewed commit all 30 answer shapes return the candidate answer, including the six round-3 tool-word answers, the four progress-verb duration answers, the nine round-1 tool-word openings, indented quotes and nested bullets, a dashed directory tree, a table and a bare hand-drawn tree run; every recorded chrome example is rejected or excluded except the three-period status form and the Claude indented approval prompt reported above","tests_pass":false,"witness":null},{"command":"node p2.mjs","cwd":"scratch","exit_code":0,"result":"reviewer-authored shapes were probed across the same four commits: a tool word followed by prose, by a colon-introduced list and by a path inside prose all remain messages, while a tool word followed only by a bare path, a bare filename, a bare command or nothing yields unavailable rather than an older block; progress durations in prose survive and durations in a parenthesized, bracketed or ellipsis-character trailing form are rejected; a framed approval prompt is excluded through the frame boundary for both adapters; the covered in-flight summary is excluded and three plausible variants are not","tests_pass":false,"witness":null},{"command":"node e2e.mjs","cwd":"scratch","exit_code":0,"result":"through refreshMessagePreviews with a fake protocol-shaped client, the two-clause in-flight summary and the indented Claude approval prompt are published as part of the message with source assistant-preview, available true, truncated false, stale false, limitation null and the read revision, while the covered four-clause summary is excluded","tests_pass":false,"witness":null},{"command":"node timing.mjs","cwd":"scratch","exit_code":0,"result":"pathological single-line inputs, being long word and dot runs, long slash runs, a long dotted tail, a thousand comma clauses and a 16 KiB line, were parsed in at most 1.29 milliseconds for either adapter, so the command-invocation and clause patterns show no catastrophic backtracking at the bounded read size","tests_pass":false,"witness":null}]
```

## Non-claims
- I do not claim the two-clause in-flight summary or the indented Claude approval prompt occur in a real render; both were reproduced in a temp copy from reviewer-authored text, and the single authorized live read showed no chrome inside any message.
- I do not claim the three-period status form is emitted by either agent; the recorded live status lines all carried the interrupt affordance, which remains independently sufficient evidence of chrome.
- I do not claim that a progress line whose elapsed time sits in no trailing form, such as the word Working followed by 45s alone, should be rejected; that widening is the deliberate narrowing round 3 requested and I treat it as an accepted trade-off, not a defect.
- I do not claim any defect in the read bounds, the reader pool, the pane read timeout, occupant replacement, stale retention, the tripwire subscription, the action boundary or the interactive UI; the round-4 delta changes only the visible-text classifier and documentation.
- I do not claim the report and handoff counts are wrong; both declare 160 tests and my runs measured 160 with zero skips, and the focused figure of 28 groups in 0.043 seconds also reproduced.
- I do not claim the engineer's post-commit gate result for this commit; the report states it is still outstanding and the supplied facilitator gate for this commit exited zero, which is inherited evidence rather than a fresh test claim.
- I do not claim the proposed fixes are complete classifiers; each was probed only against the shapes listed in this record, and the generalized clause pattern would also exclude an answer continuation line that happens to be a run of count clauses.
- I do not claim the interactive board renders these previews correctly; no terminal UI was launched in this round.

## Unverified
- The interactive board, focus and done actions, alternate hosts and any real TTY render were not exercised.
- Only one live board read was authorized, so no variant in-flight summary, no approval prompt and no status line could be hunted on live panes, and the observed 9 clean previews are a single sample.
- The live read returns parsed previews only, so the raw rendered text behind them was not inspected and the exact rendered form of any real in-flight summary remains unknown to me.
- The 200-line request, the 16 KiB retention bound, the four-reader pool and the two-second pane read budget were confirmed from the code and the committed read-state groups, not by forcing an oversized or slow live read.
- Watch-mode coalescing at one read per pane per second and the five-second sampling interval were not measured against the live server.
- All budgeted runs used a fresh single-commit system-temp copy of the reviewed tree rather than the lane worktree, so behavior depending on the lane's own history was not exercised.
- The strict OpenSpec validation overlapped another lane's focused node test run, so that one authorized command was not serialized; the parser probes are sub-second node runs and were also not serialized against other sessions.
- The engineer's reported sandbox socket EPERM in the round-4 negative run was not reproduced here and could not be, since this host allows the system-temp sockets; my negative run reached all 160 controls.
- Per-test outcomes rest on the printed runner summaries and the counted control assertions; process exit statuses were captured directly except for the full suite, whose exit zero came from npm's own run log.
- No push, promotion, archive, close, dispatch, fix, commit or public record write was performed or attempted, and the lane worktree was clean before and after every step.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
