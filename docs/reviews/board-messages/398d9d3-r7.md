**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: 398d9d36a4545dd5fb906ebb90c817a9a96c5e75
Topic: board-messages
Round: 7
Review ID: lr-a13229984690b3306e8b5d50b74cffbf
Base branch: main
Base commit: 3367cd53e6fa50d3212f0da32e2189900b9b6d21

## Findings
- [Major] board/message-preview.mjs:13 - the widened timed-status body now treats any parenthesized or bracketed group holding an elapsed time anywhere after a progress verb, and any three-period or ellipsis run followed later by both an elapsed time and a middle dot, bullet or vertical bar, as rendered status; because the timed-status rules at lines 28 and 47 admit the adapter marker, the full-row check at line 127 applies them to every marked candidate even though their scope flag is false, so ordinary current answers such as Running the full suite (about 2 minutes) confirmed the fix, Working through the parser (it took 30s) fixed the leak, Worked through the backlog \[took 12 minutes\] and closed it, and Thinking about caching... 200 ms per read, then a middle dot, then 3 files touched are discarded in both adapters and the older finished block is published as the current message, while the same sentence as an indented second line truncates the answer at its first line; all eight probed parenthesized or bracketed mid-sentence answers were messages under both 691cb6d and f2cb31b, all three probed ellipsis-and-separator answers were messages under f2cb31b, and driving refreshMessagePreviews with a fake protocol-shaped client publishes the older block with source assistant-preview where the parent published the new answer; this reopens the round-5 prose loss in a broader shape, defeats the agent-message-preview requirement that the board identify the last confidently bounded substantive message, and is not observed because no committed fixture places a duration mid-sentence after other words; Fix: require the elapsed-time group, or the three-period or ellipsis run carrying the elapsed time, to follow the progress verb directly, and add paired mid-sentence duration answers in candidate and continuation positions for both adapters; a scratch copy carrying exactly that rule kept the focused file at 32 of 32, returned all seven probed mid-sentence answers in both adapters, and still rejected all twelve probed status forms, including the four committed trailing-field forms and the round-6 residual form in which Thinking, an ellipsis and 30s are followed by the word left
- [Moderate] test/message-preview.test.mjs:189 - the generated table guard is real for rule shape but not for behavior: each twin keeps at most the first three word tokens of its example, cut at the first parenthesis, bracket, colon or separator, and appends a fixed ordinary tail, so it drops the elapsed-time groups, colons and separators the patterns actually key on, which leaves the timed-status twin as the bare progress verb followed by ordinary words and unable to observe the Major finding; twins are exercised only as a single marked first line and never as an indented continuation, although most rules act on continuation rows; and the asserted scope flag is declarative, because matchingChromeRule applies every rule regardless of its flag to the full marked row at the parser's line 127, so a flag-false rule whose pattern admits the adapter marker judges candidate text anyway; adding a flag-false marker-admitting Summary-row rule to both tables, or an indented Note-row rule, left the focused file at 32 of 32 while direct calls showed the first discarding a marked answer that opens with Summary and a colon and the second truncating an answer at an indented second line that opens with Note and a colon, in both adapters; flipping the Claude interrupt flag to false fails only the declarative scope assertion and changes no classification, because the same pattern already matches any full row carrying the affordance; a twin identical to its example is impossible by construction since every twin carries the fixed tail, but that is not the protection the report describes; Fix: generate each twin by embedding the example body inside an ordinary sentence, exercise it as both a marked first line and an indented continuation, and either skip flag-false rules when the row being judged is a marked candidate or assert that no flag-false pattern matches the adapter marker followed by an ordinary sentence that embeds its example
- [Minor] board/message-preview.mjs:24 - the Codex prompt-row rule now also matches the prompt glyph after two or more leading spaces, and the context-bar rules at lines 31 and 52 now accept a colon after the words context left, so an indented Codex answer continuation that begins with the single right-pointing angle quotation mark, such as a menu path written with that glyph between menu names, now ends the block, and an indented continuation reading Context left, a colon and a percentage now ends the block in both adapters, truncating the current answer at the previous line where the round-5 and round-6 parsers kept the whole answer; the report cites no rendered capture of an indented Codex prompt or a colon-bearing context bar, and the generated group never places a twin in continuation position, so nothing observes either widening; Fix: restore the column-zero Codex prompt anchor and the colon-free context wording unless a rendered capture shows otherwise, and pair each continuation-scoped rule with an indented ordinary continuation that opens with the same leading text
- [Minor] docs/REFERENCE.md:290 - the new text says every chrome classifier carries an explicit candidate-first-line scope and that only the interrupt-affordance status rule may also inspect a candidate's captured first line, but the timed-status rules are flagged false and still reject marked candidates through their marker-admitting rendered-row patterns, which is the mechanism behind the Major finding, and the report at lines 304 to 308 and HANDOFF at lines 952 and 953 present the 33 generated pairs as pairing every new rule automatically and as failing any unapproved first-line scope, which the Moderate finding shows holds only for the declared flag; Fix: state that a rule whose pattern admits the adapter marker judges the whole candidate row whatever its flag, or enforce the flag in the matcher, and describe the twin as a leading-words check until it embeds the example body

## Re-executed
```json
[{"command":"npm test","cwd":"scratch","exit_code":0,"result":"164 tests, 164 pass, 0 fail, 0 cancelled, 0 skipped, 0 todo, duration_ms 117721.08, 118 s wall, against a pristine system-temp extraction of the reviewed tree with no file newer than the extraction, all eight inherited GIT_ variables cleared and an empty isolated global and XDG git configuration, on Node.js 20.19.4, npm 10.8.2 and Git 2.54.0; this matches the declared 164 of 164 with zero skips, and the three new parser groups for the generated tables, the trailing status fields and the approval asymmetry each reported ok","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"164 tests, 0 pass, 164 fail, 0 skipped, duration_ms 115043.78, 115 s wall, on the same pristine extraction and isolated git configuration; 164 lines quoted the deliberate control message, no failure carried any other error text, and no Unix-socket EPERM occurred","observed_failure":"every one of the 164 tests failed on its own deliberate control assertion with zero passes and zero skips, so the suite is not vacuous and the new generated-table, trailing-status and approval groups carry effective controls"}},{"command":"OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"scratch","exit_code":0,"result":"Totals: 20 passed, 0 failed (20 items), matching the report, completed in under one second after a clear load probe on the same pristine extraction; the OpenSpec version was not separately queried","tests_pass":false,"witness":null},{"command":"./lane.mjs board --json","cwd":"lane","exit_code":0,"result":"36 rows in 3 s with empty stderr, coverage repository complete, registry available, Herdr connected and messages assistant-preview, zero errors; 22 rows carried an available preview, 16 Claude and 6 Codex, and 14 were unavailable; no row was stale, 11 were truncated and every limitation was null; no available preview contained an interrupt affordance, the words context left, a token usage or cost bar or a line opening with a prompt glyph, none opened with a progress verb, and no line in any preview began with a progress verb followed by an elapsed time; the one preview holding approval words carries them mid-sentence at offset 151 of a 387-character line; the lane HEAD and its porcelain listing, which shows only the pre-existing ignored gate file, were identical before and after, and no file in the lane worktree or in the canonical checkout's local lane directory was newer than a marker written just before the read","tests_pass":false,"witness":null},{"command":"node --test --test-reporter=tap test/old-f2cb31b.test.mjs","cwd":"scratch","exit_code":0,"result":"the focused parser test files from all six earlier lane commits, 6ac0067, 487c9f6, 40d5382, 2c57bb0, 691cb6d and f2cb31b, renamed but otherwise unmodified, each ran with this command form against a copy holding the reviewed parser and passed 5, 10, 14, 28, 28 and 29 groups with zero failures, as they also did against the parent parser, so every round 1 to 6 committed answer and chrome example keeps its committed classification","tests_pass":true,"witness":{"kind":"mutation","command":"node --test --test-reporter=tap test/old-f2cb31b.test.mjs","cwd":"scratch","exit_code":1,"result":"10 pass, 19 fail against a copy holding the round-1 parser from 6ac0067 in place of the reviewed parser","observed_failure":"the round-6 focused file rejected the round-1 parser on 19 of 29 groups, beginning with the cumulative answer corpus, so the old-file harness discriminates parser behavior rather than passing vacuously"}},{"command":"node --test --test-reporter=tap test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"mutation M1: 31 pass, 1 fail against a copy whose Codex footer-row rule was flagged as scoped to candidate text; the sole failure is the generated table group, on its scope-list assertion","tests_pass":false,"witness":null},{"command":"node --test --test-reporter=tap test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"mutation M2: 31 pass, 1 fail against a copy whose Codex footer-row rule was built without a scope flag field; the sole failure is the generated table group, on its rule-shape assertions","tests_pass":false,"witness":null},{"command":"node --test --test-reporter=tap test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"mutation M3: 31 pass, 1 fail against a copy whose Claude interrupt-status flag was set to false; the sole failure is the generated table group on its scope-list assertion, although the edit changes no classification because the same marker-admitting pattern already matches any full row carrying the affordance at the parser's line 127","tests_pass":false,"witness":null},{"command":"node --test --test-reporter=tap test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"mutation M4: 32 pass, 0 fail against a copy that adds one flag-false Note-row rule anchored to two or more leading spaces to both tables; a direct call on that copy then truncated an answer at its indented second line opening with Note and a colon in both adapters, so a new continuation over-match is not observed by its generated pair","tests_pass":false,"witness":null},{"command":"node --test --test-reporter=tap test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"mutation M5: 32 pass, 0 fail against a copy that adds one flag-false Summary-row rule whose pattern admits the adapter marker to both tables; its generated twin keeps only the word Summary and drops the colon, and a direct call on that copy discarded a marked answer opening with Summary and a colon and published the older block in both adapters, so a flag-false rule that judges candidate text is not observed","tests_pass":false,"witness":null},{"command":"node --test --test-reporter=tap test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"mutation M6: 31 pass, 1 fail against a copy whose timed-status body reverts to the round-6 elapsed-time-at-end form; the sole failure is the new trailing status fields group, so the round-6 finding-1 correction is independently pinned","tests_pass":false,"witness":null},{"command":"node --test --test-reporter=tap test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"mutation M7: 32 pass, 0 fail against a copy whose timed-status body requires the parenthesized or bracketed elapsed-time group, or the three-period or ellipsis run carrying the elapsed time, to follow the progress verb directly; direct calls on that copy returned all seven probed mid-sentence duration answers in both adapters and still rejected all twelve probed status forms in both adapters, including the four committed trailing-field forms and the round-6 residual form in which Thinking, an ellipsis and 30s are followed by the word left","tests_pass":false,"witness":null},{"command":"node probe.mjs harvest-diff","cwd":"scratch","exit_code":0,"result":"634 single-line bodies harvested from the focused test files of all seven lane commits were driven through the parent and reviewed parsers in candidate, continuation and inner positions for both adapters, giving 3804 comparisons with 57 classification differences: 26 marked candidate openings now returned as messages, which is the round-6 finding-2 correction; 24 changes that reject or end the block at the four committed trailing-field status forms, which is the round-6 finding-1 correction; 2 Claude continuation and inner-line differences for one indented row opening with a question mark, now kept; 1 doubled-marker ANSI harness artifact; and 4 Codex indented prompt-glyph rows that now end a block, which is the widening in the first Minor finding; no committed answer body moved from message to chrome","tests_pass":false,"witness":null},{"command":"node probe.mjs shapes shapes.json","cwd":"scratch","exit_code":0,"result":"reviewer-authored shapes compared across all seven lane parsers in both adapters: eight mid-sentence answers with a parenthesized or bracketed duration, messages under both 691cb6d and f2cb31b, are discarded as candidates and truncate the answer as inner lines at the reviewed commit; three ellipsis answers with a later duration and a separator, messages under f2cb31b, are discarded again as they were under 691cb6d; all nine probed status forms carrying trailing fields are now rejected and end the block; all fourteen round-6 finding-2 opening rows are now messages; an indented Codex continuation opening with the prompt glyph and an indented continuation reading Context left, a colon and a percentage now truncate the answer where the round-5 and round-6 parsers kept it; indented continuations reading a question mark then to toggle, or Tokens then a colon and a cost, are now kept; tool-label openings are unchanged from the parent","tests_pass":false,"witness":null},{"command":"node e2e.mjs","cwd":"scratch","exit_code":0,"result":"through refreshMessagePreviews with a fake protocol-shaped client returning an older finished block followed by a two-line current answer, the parent parser published each of three mid-sentence duration answers with its second line in both adapters, while the reviewed parser published the older finished block with source assistant-preview for all three in both adapters, so a consumer sees an ordinary current-looking preview","tests_pass":false,"witness":null},{"command":"git diff --check 3367cd5 398d9d3","cwd":"lane","exit_code":0,"result":"no whitespace or conflict-marker damage across the lane, and the same check over the round-7 delta from the parent record commit 1a2b40d also exited zero; main and the merge base equal the base commit; board/app.mjs has blob 56f8d64f at main, at the base and at the reviewed commit; the round-7 delta touches only the parser, its focused test file, REFERENCE, the report and HANDOFF, with no change under the public reviews directory, the README, the OpenSpec tree, lane.mjs or any other board file; every parser hunk ends by line 177, so the refresh path with its pane read request, the 16 KiB retention bound, the 500 code-point excerpt bound, the reader pool, occupant replacement and stale retention are identical to the parent, and the only REFERENCE hunk starts at line 290, leaving the tripwire live-only sentence at line 274 untouched","tests_pass":false,"witness":null}]
```

## Non-claims
- I do not claim the Major finding occurs in a real render; the discarded answers are reviewer-authored sentences, and the single live read cannot reveal a dropped current block because the older block it publishes looks like any other preview.
- I do not claim the scratch rule in the Major finding is the only acceptable fix or a complete classifier; it shows that the committed corpus, the round-5 prose sentences and every probed status form can be satisfied together.
- I do not claim the generated table group is worthless; it pins rule shape, anchors, a matching example, a rejected rendered example and the declared scope, and it failed on three of the seven scratch mutations.
- I do not claim the round-7 first-line change is wrong; the fourteen round-6 opening rows it now returns are the correction the round-6 record asked for, and every committed chrome example is still rejected in its committed position.
- I do not claim the Claude marker-plus-spinner shapes now returned as messages are a defect; a marked assistant line that begins with a spinner glyph is not a rendered form I could cite, and the column-zero spinner row is still chrome.
- I do not claim any defect in the read bounds, reader pool, pane read timeout, occupant replacement, stale retention, tripwire live-only rule, action boundary or interactive UI; the round-7 delta changes none of them.
- I do not claim the report's pre-change 29 of 31 or its 0.059 second focused figure; those intermediate states were not reproduced.
- I do not claim the suite counts in the report or HANDOFF are wrong; the Re-executed entries record totals equal to the declared ones.
- I do not treat the inherited facilitator gate file for the reviewed commit as fresh evidence; it is supplied metadata and is not part of any tests\_pass value here.

## Unverified
- The interactive board, focus and done actions, alternate hosts and any real TTY render were not exercised.
- The single live read returns parsed previews rather than raw pane text, so whether real Codex or Claude output ever places a duration group mid-sentence after a progress verb, or indents a Codex prompt glyph or a colon-bearing context bar, is unknown.
- How often real answers are affected by the Major finding is unknown; the probes establish classification only.
- The pane read request, the 16 KiB retention bound, the reader pool and the pane read timeout were confirmed unchanged from the diff, not by forcing an oversized or slow live read.
- Watch-mode coalescing, the five-second sampling interval and the live tripwire subscription were not measured.
- All budgeted suite runs used a pristine system-temp extraction of the reviewed tree rather than the lane worktree, so behavior depending on the lane's own git history was not exercised.
- Another session's suite held the machine at the first budgeted attempt, which exited on the busy probe without running; every budgeted run and probe then started only after consecutive clear load probes, but the sub-second node probes were not serialized against other projects beyond that check.
- No push, promotion, archive, close, dispatch, fix, commit or public record write was performed or attempted; the lane worktree kept the same HEAD and porcelain listing throughout, and all scratch fixtures were removed.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
