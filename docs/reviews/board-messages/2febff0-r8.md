**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: 2febff094ced83ab55747809cdda07dce28545ff
Topic: board-messages
Round: 8
Review ID: lr-d4ee1f2b481b5a5b843433fb730fbb3d
Base branch: main
Base commit: 03a5dc118cb396b11ed31f0fef3cd4fa0a8e81bc

## Findings
- [Major] board/message-preview.mjs:34 - the timed-status rules here and at line 53 now match only indented rows while keeping candidate-first-line scope false, and the marked-row check at line 133 now runs in candidate-first-line mode like the captured-body check at line 134, so the skip at line 76 never tests timed status against a marked row or its body; a marked progress status such as Working (2m 04s), Worked \[48s\] total or Running (45s) remaining after a real answer therefore becomes LAST MESSAGE, and a pane holding only such a row returns it instead of message unavailable; probe-f returns all 16 probed status forms as the message in candidate and lone placements for the Codex marker and both Claude markers (47 of 144 placements rejected, against 135 at 398d9d3), probe-e shows refreshMessagePreviews publishing the status row instead of the answer in both adapters, the harvest differential finds 9 committed status forms newly returned as candidates in both adapters, and the unmodified focused files of 2c57bb0, 691cb6d, f2cb31b and 398d9d3 fail six tests on exactly this; the round moved the marked-candidate status assertions committed at 398d9d3 to indented rows (now test lines 310 to 341 and 506), REFERENCE line 295 calls timed progress continuation-only chrome and report line 345 keeps interrupt-status as the sole first-line classifier without naming the lost rejection, and the result contradicts the agent-message-preview spec at lines 3 to 4 and 9 to 16 in both its main and board-messages change copies, line 17 of the board-messages change design, and the round-7 fix, whose scratch rule still rejected every probed status form; Fix: let each timed-status rule admit its adapter marker with candidate-first-line scope true, list timed-status beside interrupt-status in the scope assertion at test line 174, and restore marked-candidate and lone-row status assertions for both adapters; a scratch copy with exactly that rule and scope-list change passed the focused file 35 of 35 against a 0 of 35 negative control, kept all 10 mid-sentence answers, rejected 137 of 144 placements, published the answer end to end, and failed only the old scope-list assertion across the 146 earlier focused tests
- [Moderate] test/message-preview.test.mjs:113 - ordinaryTwinBody removes a leading prompt, fast-mode, question-mark or spinner glyph here and a leading result marker at line 114, so for 8 of the 33 rules (Codex prompt-row, result-marker and question-shortcut-bar; Claude prompt-row, result-marker, question-shortcut-bar, fast-mode-bar and spinner-row) the generated twins never contain the glyph those rules key on, and the full-body assertion at line 198 checks each twin against the same stripped body it was built from; a scratch copy that widened question-shortcut-bar in both tables to any indented row opening with a question mark passed the focused file 35 of 35 against a 0 of 35 negative control while truncating an ordinary continuation that opens with a question mark for all three markers, so report line 351 and HANDOFF line 1074, which say twins retain the complete lexical body and distinguishing groups, overstate the guard; Fix: add per-rule near-miss twins that keep the keyed leading glyph and change only a later keyed group, run them as indented continuations, and compare the full-body assertion with the literal rule example instead of the stripped twin
- [Minor] board/message-preview.mjs:18 - the first ellipsis alternative of the timed status accepts any text between the ellipsis and a final elapsed time, and the bracket group at line 17 accepts any bracketed text that contains an elapsed time, so ordinary indented continuations such as Running... it finished after 45s, Working... the first one timed out after 30s, Worked (from 09:00 to 12:30) and Running (see the 30s timeout) are truncated as status for the Codex marker and both Claude markers; this predates the round, since 398d9d3 dropped the same lines in both positions, and the Major fix brings back the first-line case, since the fix copy also dropped all five probed lines as marked candidates; Fix: limit the ellipsis alternative to optional whitespace, the elapsed field and the bounded status words, and the bracket group to elapsed and bounded status fields, with paired prose fixtures in both positions
- [Minor] board/message-preview.mjs:42 - the approval-prompt rules here and at line 63 now require the question mark to end the same rendered row, a round-8 narrowing that neither the report nor the HANDOFF round-8 entry mentions and that REFERENCE lines 307 to 308 still describe as excluding approval-worded row openings; the committed approval-worded prose sentence is now correctly kept, but an indented approval question that wraps before its question mark now stays in the preview where 398d9d3 truncated it, in both adapters, as probe-g shows for two wrapped questions; Fix: document the trailing question-mark requirement in REFERENCE and the report, and either treat an approval opening whose question mark ends the next indented row as chrome or pin the wrapped case as intended with a paired fixture

## Re-executed
```json
[{"command":"npm test","cwd":"scratch","exit_code":0,"result":"168 tests, 168 pass, 0 fail, 0 cancelled, 0 skipped, 0 todo, duration_ms 124605.69, 125 s wall, against a pristine system-temp extraction of the reviewed tree whose 131 files still hash to the 2febff0 blobs after all suite runs, with the seven inherited GIT_CONFIG variables cleared, on Node.js 20.19.4, npm 10.8.2 and Git 2.54.0, started only after three consecutive clear load probes; this matches the declared 168 of 168 with zero skips and no Unix-socket EPERM","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"168 tests, 0 pass, 168 fail, 0 skipped, duration_ms 118173.22, 118 s wall, on the same extraction after three clear load probes; all 168 error lines quoted the deliberate control message and none carried other error text","observed_failure":"every one of the 168 tests failed on its own deliberate control assertion with zero passes, so the suite is not vacuous and the round-8 spy, scope and generated-pair groups carry effective controls"}},{"command":"OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"scratch","exit_code":0,"result":"Totals: 20 passed, 0 failed (20 items) on openspec 1.6.0 against the same extraction after three clear load probes, with the agent-message-preview spec and the board-messages change among the passing items; this matches the declared 20 of 20","tests_pass":false,"witness":null},{"command":"./lane.mjs board --json","cwd":"lane","exit_code":0,"result":"one live read against the connected Herdr server after three clear load probes, 4 s, empty stderr: schema 1, coverage repository complete, registry available, herdr connected, messages assistant-preview, 0 errors, 39 rows, 22 available previews (17 Claude, 5 Codex), 17 unavailable, 0 stale, 10 truncated, 0 limitations, 0 raw excerpts and 0 non-null tripwire values; a count-only scan found 0 first lines opening with a progress verb, 0 lines pairing a progress verb with an elapsed time, 0 context-left bars, 0 token bars, 0 prompt-glyph lines and 1 preview containing the words esc to interrupt; preview text was neither printed nor retained, the lane HEAD and ignored-inclusive porcelain listing were identical before and after, and no lane file was newer than a stamp taken before the read","tests_pass":false,"witness":null},{"command":"node --test --test-reporter=tap test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"scope mutation copy of the reviewed tree with a flag-false Summary rule admitting both adapter markers and indented rows plus a flag-false indented Note rule injected into both tables: 35 tests, 34 pass, 1 fail, duration_ms 64.67; the generated group failed at its summary-row chrome assertion, expecting Earlier response. and receiving Summary: 3 files, so the focused file detects the injection","tests_pass":false,"witness":null},{"command":"node --test --test-reporter=tap test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"scope mutation copy with only the flag-false indented Note rule injected into both tables: 35 tests, 34 pass, 1 fail, duration_ms 65.59; the generated group failed at the note-row continuation twin, so an overbroad continuation rule is detected without a hand-maintained pair","tests_pass":false,"witness":null},{"command":"node --test --test-reporter=tap test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"matcher mutation copy with the scope skip at parser line 76 deleted: 35 tests, 33 pass, 2 fail, duration_ms 72.52; the review prose corpus failed because Would you like to run through the design together? became Superseded response., and the chrome-matcher scope test expected null and received summary-row","tests_pass":false,"witness":null},{"command":"node probe-q1.mjs . q1","cwd":"scratch","exit_code":0,"result":"consultation log over the Summary and Note injection copy after three clear load probes, for two Summary bodies and one Note body under the Codex marker and both Claude markers: all 9 marked candidates were returned unchanged with 0 consultations of an injected rule on the marked row or its captured body, and all 9 indented continuations were truncated after 2 or 4 injected-rule consultations","tests_pass":false,"witness":null},{"command":"node --test --test-reporter=tap test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"widening mutation copy in which question-shortcut-bar in both tables accepts any indented row opening with a question mark: 35 tests, 35 pass, 0 fail, duration_ms 61.38; the focused file does not detect a widening that probe-f shows truncating an ordinary question-mark continuation for all three markers","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 node --test --test-reporter=tap test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"35 tests, 0 pass, 35 fail, duration_ms 63.55, on the same widened copy after three clear load probes","observed_failure":"all 35 focused tests failed on their deliberate controls, so the 35 passes on the widened copy are real and the generated group is blind to the widening rather than vacuous"}},{"command":"node --test --test-reporter=tap test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"fix copy in which each timed-status rule also admits its adapter marker with candidate-first-line scope true and the scope assertion lists interrupt-status and timed-status, with no other change: 35 tests, 35 pass, 0 fail, duration_ms 70.19","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 node --test --test-reporter=tap test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"35 tests, 0 pass, 35 fail, duration_ms 66.33, on the same fix copy after three clear load probes","observed_failure":"all 35 focused tests failed on their deliberate controls, so the fix copy's 35 passes are not vacuous"}},{"command":"node --test --test-concurrency=1 --test-reporter=tap test/old-*.test.mjs","cwd":"scratch","exit_code":1,"result":"the unmodified focused files of 6ac0067, 487c9f6, 40d5382, 2c57bb0, 691cb6d, f2cb31b and 398d9d3 against the reviewed parser: 146 tests, 140 pass, 6 fail, duration_ms 371.58; every failure is a marked status row returned where Earlier response. was expected, in the cumulative status corpora of 2c57bb0, 691cb6d, f2cb31b and 398d9d3 (Working (2m 04s)), the 398d9d3 trailing status fields group (the three-period 2m 04s token-count form) and the f2cb31b same-opening pair group (Working (2m 04s))","tests_pass":false,"witness":null},{"command":"node --test --test-concurrency=1 --test-reporter=tap test/old-*.test.mjs","cwd":"scratch","exit_code":1,"result":"the same seven files against the fix copy's parser: 146 tests, 145 pass, 1 fail, duration_ms 389.98; the only failure is the 398d9d3 scope-list assertion at its line 151, which the fix changes by design","tests_pass":false,"witness":null},{"command":"node --test --test-concurrency=1 --test-reporter=tap test/old-*.test.mjs","cwd":"scratch","exit_code":1,"result":"harness witness pairing the 398d9d3 focused file with the round-1 6ac0067 parser: 32 tests, 10 pass, 22 fail, duration_ms 55.73, so the old-file harness discriminates between parsers","tests_pass":false,"witness":null},{"command":"node probe-f.mjs .","cwd":"scratch","exit_code":0,"result":"16 status forms, each as a marked candidate after an answer, a lone marked row and an indented continuation under the Codex marker and both Claude markers (144 placements): 398d9d3 rejected 135, the reviewed parser 47 and the fix copy 137; the reviewed parser returns all 16 forms as the message in candidate and lone placements for every marker and rejects them as continuations except one spinner-glyph token form in a Codex pane, where 398d9d3 leaked only the pre-existing Thinking three-period 30s left form; the fix copy's 7 leaks are all that spinner-glyph token form after a marker or in a Codex pane; the 10 committed mid-sentence answers are dropped by 398d9d3 and kept by the reviewed and fix parsers in both positions; five beyond-corpus lines such as Running... it finished after 45s and Worked (from 09:00 to 12:30) are kept as candidates and truncated as continuations by the reviewed parser and dropped in both positions by 398d9d3 and the fix copy; an indented ordinary line opening with a question mark is kept by the reviewed parser and truncated by the widened copy for all three markers","tests_pass":false,"witness":null},{"command":"node probe-e.mjs .","cwd":"scratch","exit_code":0,"result":"refreshMessagePreviews with a fake client whose pane text is a real answer followed by a marked status row, for Working (2m 04s), Worked [48s] total and a Thinking ellipsis 30s form in both adapters: 398d9d3 and the fix copy publish The migration finished and all checks passed., while the reviewed parser publishes the status row itself as messageText with source assistant-preview, each with 1 update and no errors","tests_pass":false,"witness":null},{"command":"node probe-h2.mjs . old/*.test.mjs tree/test/message-preview.test.mjs tree/docs/reviews/board-messages/*.md tree/docs/reports/board-messages.md","cwd":"scratch","exit_code":0,"result":"per-line harvest of 332 single-line bodies and 23 multi-line texts from the seven earlier focused files, the reviewed focused file, the committed public review records and the report, compared between 398d9d3 and the reviewed parser in candidate, continuation, inner and full positions for both adapters (2038 comparisons): 93 differences; 38 candidate differences are 9 committed status forms newly returned as messages plus the 10 intended mid-sentence answers now kept, each in both adapters; 50 continuation and inner differences newly keep the 10 mid-sentence answers, the colon-bearing Context left line and the approval-worded prose sentence in both adapters and the indented Codex prompt-glyph line in the Codex adapter; 4 newly reject the Thinking three-period 30s left form in continuation and inner positions; the single full-text difference is the committed indented menu-path answer, now kept whole","tests_pass":false,"witness":null},{"command":"node probe-g.mjs .","cwd":"scratch","exit_code":0,"result":"six approval-worded rows as indented continuations in both adapters after three clear load probes: 398d9d3 and the reviewed parser both truncate the three single-row approval questions; the reviewed parser now keeps the committed approval-worded prose sentence, as intended, and also keeps two approval questions wrapped onto a second indented row before their question mark, which 398d9d3 truncated","tests_pass":false,"witness":null},{"command":"git hash-object --no-filters --stdin-paths","cwd":"scratch","exit_code":0,"result":"hashed all 131 files of the extraction after all suite runs and compared them with git ls-tree -r 2febff0 read from the lane repository: every path and blob matched, so all scratch runs used the reviewed tree","tests_pass":false,"witness":null},{"command":"git diff --check 398d9d3 2febff0","cwd":"lane","exit_code":0,"result":"no whitespace errors across the 25 files changed since the round-7 reviewed commit","tests_pass":false,"witness":null},{"command":"git diff --check 03a5dc1 2febff0","cwd":"lane","exit_code":0,"result":"no whitespace errors across the full lane diff over the base commit","tests_pass":false,"witness":null}]
```

## Non-claims
- I do not claim marked progress rows without the interrupt hint appear in real Codex or Claude renders; the Major finding rests on the spec, the design, four earlier rounds of committed assertions and the round-7 fix expectation, not on a captured pane, and the single live read found no progress-verb first line.
- I do not claim the scratch fix in the Major finding is the only acceptable fix or a complete classifier; it still returns one spinner-glyph token form after a marker or in a Codex pane, a combination I could not cite as rendered.
- I do not claim the scope enforcement itself is wrong; flag-false rules are no longer consulted for the marked row or its captured body, all three scope mutations are caught, and the round-7 record offered matcher enforcement as one fix without warning that timed status would then need first-line scope.
- I do not claim the newly kept continuations are defects; the ten mid-sentence answers, the colon-bearing Context left line and the indented Codex prompt-glyph menu path are corrections the round-7 record asked for, and the approval-worded prose sentence is the intended effect of the approval narrowing.
- I do not claim the round-6 residual Thinking three-period 30s left form is resolved; it is now rejected as a continuation but is still returned as a marked candidate, as at 398d9d3.
- I do not claim the question-shortcut widening is a present defect; it is a scratch mutation that shows what the generated guard cannot see.
- I do not claim any defect in the read bounds, reader pool, pane read timeout, occupant replacement, stale retention, tripwire live-only rule, action boundary or interactive UI; every round-8 parser hunk ends by line 133, and board/app.mjs keeps its main blob.
- I do not claim the report's intermediate 29 of 35 focused figure or its 115.479 s suite time; the declared totals match, and the wall time differs with machine load.
- I do not rely on the lane's ignored gate file, which predates this review, for any result here.

## Unverified
- The interactive board, focus and done actions, alternate hosts and any real TTY render were not exercised.
- The single live read returns parsed previews rather than raw pane text, so whether real panes show marker-led progress rows without the interrupt hint, approval questions wrapped before their question mark, or free prose after a progress verb and an ellipsis is unknown.
- Whether the one live preview containing the words esc to interrupt is prose or a leaked status row was not determined, because preview text was deliberately not printed.
- How often real answers are affected by the findings is unknown; the probes establish classification only.
- The read bounds, reader pool, pane read timeout and occupant handling were confirmed unchanged from the diff hunk ranges and blob comparisons, not by forcing an oversized, slow or replaced live read.
- Watch-mode coalescing, the sampling interval and the live tripwire subscription were not measured.
- All budgeted suite runs used a pristine system-temp extraction of the reviewed tree with the user's global git configuration otherwise in effect, so behavior depending on the lane's own git history was not exercised.
- Another session's suite held the machine for several minutes; every run and probe here started only after three consecutive clear load probes, but the sub-second node probes were not serialized against other projects beyond that check.
- No push, promotion, archive, close, dispatch, fix, commit or public record write was performed or attempted; the lane worktree kept the same HEAD and ignored-inclusive porcelain listing throughout, its only ignored entry is the lane metadata directory holding one gate file that predates this review, and all scratch fixtures were removed before this record was written.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
