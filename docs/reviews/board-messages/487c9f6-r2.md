**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: 487c9f651a60488a6bea663da897ebe192818c5d
Topic: board-messages
Round: 2
Review ID: lr-5a28ef42264a6fb57f1b883494146ecb
Base branch: main
Base commit: 3367cd53e6fa50d3212f0da32e2189900b9b6d21

## Findings
- [Major] board/message-preview.mjs:13 - the status-chrome guard recognizes only the progress verbs Working, Worked, Thinking and Running, so any other progress line is still accepted as a candidate block and becomes the assistant message; the one authorized live board read at the reviewed commit returned a Codex row whose LAST MESSAGE was a context-compaction progress line carrying an elapsed time and the interrupt hint, and the same shape reproduces in a temp copy; Fix: treat any line advertising the interrupt affordance as status chrome in both adapters, in addition to the named verbs, and add a fixture for a progress verb outside the current list.
- [Major] board/message-preview.mjs:69 - Claude tool blocks are recognized only when the marker line begins with a known tool name, but the rendered pane names the tool by its human description and puts the invoked command on the following indented result-marker line, so a tool call and its shell command are presented as the assistant message with source assistant-preview; observed live at the reviewed commit on a Claude row and reproduced in a temp copy; Fix: accept an indented result marker on the line after a candidate as tool evidence on its own, independent of the tool-name list, and treat that marker as a block boundary so tool output cannot be appended to an answer.
- [Moderate] board/message-preview.mjs:13 - the same status pattern matches any line that opens with Working, Worked, Thinking or Running and later contains any parenthesized phrase, and such candidates are now discarded outright, so an ordinary answer opening with one of those words is dropped and the preceding block is presented as the current message with a fresh observation time and stale false; this is the round-1 stale-answer failure mode reintroduced through the new pattern; Fix: require the elapsed-time or interrupt form rather than any parenthesis before rejecting a candidate, and add a control fixture for an answer opening with a progress verb.
- [Moderate] board/message-preview.mjs:66 - requiring corroboration before classifying a block as a tool call now admits an uncorroborated tool line as an answer, so a Codex block reading Ran followed by a command whose result line has not yet rendered inside the bounded tail is presented as LAST MESSAGE; this composes with the first finding, because rejecting the status block above it exposes the tool line beneath; reproduced in a temp copy, not observed live; Fix: keep a block whose opening word is a tool label unavailable when it has neither corroboration nor sentence-shaped continuation text, and add a fixture for a tool line whose result has not yet arrived.
- [Minor] board/message-preview.mjs:14 - the approval-prompt boundary is the only chrome pattern anchored to the first column, so the same prompt rendered under an answer at two-space indentation is appended to the message text and still reported available with truncated false; Fix: allow optional leading whitespace exactly as the shortcut, context and token patterns do, and extend the chrome fixture with an indented approval prompt.
- [Minor] board/message-preview.mjs:9 - the divider pattern ends a block at any line whose first non-space character is a box corner, a vertical bar or a lower-left elbow, so a directory tree or table inside an answer silently truncates the message while it is still reported available with truncated false; Fix: restrict the frame alternative to lines built only from frame characters and padding, or record an explicit limitation when a block ends on a frame character.

## Re-executed
```json
[{"command":"npm test","cwd":"scratch","exit_code":0,"result":"142 tests, 142 pass, 0 fail, 0 skipped, duration_ms 128996.9975; run against a system-temp copy of the reviewed tree with the inherited GIT_CONFIG_COUNT, GIT_CONFIG_KEY_n and GIT_CONFIG_VALUE_n variables cleared","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"142 tests, 0 pass, 142 fail, 0 skipped, duration_ms 122747.800459","observed_failure":"every one of the 142 tests failed on its deliberate control assertion, so the green suite is not vacuous and each test carries a control"}},{"command":"OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"scratch","exit_code":0,"result":"Totals: 20 passed, 0 failed (20 items); the synchronized current message-preview spec validates alongside the existing changes","tests_pass":false,"witness":null},{"command":"./lane.mjs board --json","cwd":"lane","exit_code":0,"result":"19 rows, coverage.messages assistant-preview, zero errors and zero pane read failures; 5 rows had a readable supported agent and all 5 produced a preview; 3 were genuine answers, 1 Codex row was a context-compaction progress line and 1 Claude row was a tool description followed by its indented command line","tests_pass":false,"witness":null},{"command":"git diff --check 3367cd5..487c9f6","cwd":"lane","exit_code":0,"result":"no whitespace or conflict-marker damage in the reviewed delta; the same check limited to 92e94e6..487c9f6 also exits 0","tests_pass":false,"witness":null},{"command":"node p1.mjs","cwd":"scratch","exit_code":0,"result":"every named trailing chrome form is now excluded from the message for both adapters except an approval prompt indented by two spaces, which is still appended; a status candidate is rejected for the four named verbs but accepted for a progress line without parentheses or the interrupt hint","tests_pass":false,"witness":null},{"command":"node p2.mjs","cwd":"scratch","exit_code":0,"result":"indented quotes, dashes, numbers and nested bullets now stay inside the answer, and all fourteen Codex answer openings survive; an uncorroborated Codex tool line becomes the message, and an answer containing a tree listing is silently truncated at the elbow line","tests_pass":false,"witness":null},{"command":"node p3.mjs","cwd":"scratch","exit_code":0,"result":"the two shapes observed on the live server reproduce exactly against a temp copy of the reviewed module: a Codex compaction progress line becomes the whole message, and a Claude tool description plus its indented command line becomes the message","tests_pass":false,"witness":null},{"command":"node p4.mjs","cwd":"scratch","exit_code":0,"result":"answers opening with Running, Working, Worked or Thinking and containing any parenthesized phrase are discarded and the previous block resurfaces as the current message for both adapters; the same openings without parentheses are kept","tests_pass":false,"witness":null},{"command":"node --test test/","cwd":"scratch","exit_code":0,"result":"the 10 committed parser tests pass 10 of 10 against a temp copy of the reviewed module, and pass 10 of 10 again against a copy carrying both candidate fixes","tests_pass":true,"witness":{"kind":"mutation","command":"node p3.mjs","cwd":"scratch","exit_code":0,"result":"the patched temp copy returns the real prior answer for both shapes where the reviewed code returns the progress line and the tool block","observed_failure":"two behavior-changing mutations of the boundary and tool-evidence predicates left all ten committed parser tests passing, so the committed suite constrains neither live-observed case"}},{"command":"node probe-occupant.mjs","cwd":"scratch","exit_code":0,"result":"after a supported occupant is replaced by an unsupported one the cached preview is never read again but is also correctly withheld from the row, because the projection compares the cached occupant identity with the current agent before using it","tests_pass":false,"witness":null}]
```

## Non-claims
- I do not claim the two live anomalies are permanent or reproducible on another host; each was seen in the single authorized live read of five readable panes.
- I do not claim the new pane read budget fixes the round-1 live read failures; I observed only that no pane read failed in this one read, against far fewer readable panes than round 1 reported.
- I do not claim the reviewer-authored approval-prompt, uncorroborated-tool and tree shapes match every real screen; only the progress-line and Claude tool-block shapes were confirmed against the live server.
- I do not claim any defect in the tripwire subscription, the action boundary, the child-process signal handling, or the interactive UI, none of which this delta changes.
- I do not claim the report or handoff test counts are wrong; the reviewed commit declares 142 tests and both of my runs measured 142.
- I do not claim the interactive board renders a multiline detail panel; bounded multiline text reaches consumers only through the JSON text field.

## Unverified
- The interactive board was never launched; no terminal render, focus action or done action was exercised, and no alternate host or real TTY was used.
- Watch mode was exercised only through the committed fake-server tests; the one-read-per-pane-per-second coalescing timer and the five-second sampling interval were not measured against the live server.
- Only one live board read was authorized, so the two live anomalies could not be retried or bisected and the round-1 read failures could not be re-probed under comparable load.
- The 200-line request and the 16 KiB retention bound were confirmed from the code and the committed tests, not by forcing an oversized live read.
- The four-reader pool and the two-second pane read budget were confirmed from the code, the committed concurrency test and the delayed fake-server parity test; their effect on a live host with many slow panes, and the added worst-case latency of a one-shot read over many timing-out panes, were not measured.
- All authorized runs used a system-temp copy of the reviewed tree rather than the lane worktree, to keep that worktree untouched; the copy was a fresh single-commit repository, so any behavior depending on the lane's own history was not exercised.
- Another project's builds and test batches ran on this machine throughout; every authorized run started only after a probe showed no build or test process, but background load was present and the measured durations are higher than the report's.
- No mutation was applied inside the lane worktree; the candidate fixes were verified only in a system temp copy, which was removed afterwards.
- Process exit statuses were captured directly for every authorized run, but the per-test outcomes rest on the printed summary counts quoted above.
- No push, promotion, archive, close, dispatch, or public record write was performed or attempted.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
