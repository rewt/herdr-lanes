**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: 691cb6d7ae23bb2dbf441039c8851277bfdec905
Topic: board-messages
Round: 5
Review ID: lr-632e334e1f248085eb47420d4e15b0df
Base branch: main
Base commit: 3367cd53e6fa50d3212f0da32e2189900b9b6d21

## Findings
- [Moderate] board/message-preview.mjs:94 - the new Claude approval boundary is also consulted on a candidate block's own captured first line, so a Claude assistant message opening with the words Would you like to run, Do you want to run, Do you want to allow, or Do you want to approve is now discarded and the preceding superseded block is published as the current message; driving refreshMessagePreviews with a fake protocol-shaped client, the older block is stored with source \`assistant-preview\`, \`available\` true, \`stale\` false, \`truncated\` false, \`limitation\` null and a fresh observation time, which is the round-3 stale-answer failure mode reintroduced for this shape, and the round-4 module returned the newer answer for all four openings; the Codex adapter is unaffected in kind because a Codex bullet carrying that wording is Codex's own approval chrome, whereas the Claude assistant marker denotes authored prose; a patched copy whose Claude approval boundary requires two or more leading spaces returned all four answers, still excluded the round-5 indented-prompt fixture, left Codex unchanged, and kept all 28 committed groups passing; Fix: give the Claude boundary an indented-only approval pattern so the marker-captured first line cannot match it, and pin a Claude answer that opens with approval wording, or scope the unqualified approval sentence at docs/REFERENCE.md:290 to say that approval-worded openings are excluded for both adapters
- [Moderate] board/message-preview.mjs:18 - accepting three periods wherever the trailing-status form already accepted the single horizontal ellipsis character widened status rejection into ordinary prose, because line 30 asks only whether an elapsed time occurs anywhere after that ellipsis, so an answer opening with Working, Worked, Thinking or Running whose prose contains three periods followed later by a duration, such as a sentence beginning Working on the timeout, then three periods, then 30s is the current budget, is discarded in both adapters and the preceding superseded block is published with source \`assistant-preview\`, \`available\` true, \`stale\` false, \`truncated\` false and \`limitation\` null; this is the round-3 progress-verb finding reintroduced for the three-period spelling, and the round-4 module returned all four probed sentences as messages; a patched copy that requires the elapsed time to end the trailing status form returned those four sentences while still rejecting the parenthesized, bracketed, ellipsis-character, three-period and interrupt-advertising chrome forms, and kept all 28 committed groups passing; Fix: require the elapsed time to sit at the end of the trailing status form rather than anywhere inside it, and pin fixtures for progress-verb answers whose prose contains three periods and a duration

## Re-executed
```json
[{"command":"npm test","cwd":"scratch","exit_code":0,"result":"160 tests, 160 pass, 0 fail, 0 skipped, duration_ms 123132.754125, 123 s wall; run against a fresh single-commit system-temp copy whose tree hash equals the reviewed commit's tree, with the inherited GIT_CONFIG_COUNT, GIT_CONFIG_KEY_n and GIT_CONFIG_VALUE_n variables cleared; this matches the report's declared 160 of 160 with zero skips","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"160 tests, 0 pass, 160 fail, 0 skipped, 115 s wall; 160 lines quoted the deliberate control message and no Unix-socket EPERM occurred, since this host permits the system-temp sockets that the engineer's sandbox refused","observed_failure":"every one of the 160 tests failed on its own deliberate control assertion with zero passes and zero skips, so the green suite is not vacuous and each test carries a control"}},{"command":"OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"scratch","exit_code":0,"result":"Totals: 20 passed, 0 failed (20 items) with OpenSpec 1.6.0; the synchronized current agent-message-preview spec validates beside the existing changes; the shell pipeline lost the process status, so exit zero was taken from that printed totals line","tests_pass":false,"witness":null},{"command":"./lane.mjs board --json","cwd":"lane","exit_code":0,"result":"25 rows, coverage repository complete, registry available, Herdr connected and messages assistant-preview, zero errors and zero pane read failures; 11 rows produced an available preview and each was a genuine assistant answer with no status line, footer bar, approval prompt, composer frame or count-clause summary inside its message text; the 14 unavailable rows all carried a null agent kind, meaning no supported adapter rather than a parse failure; one available message legitimately retained a box-drawing data table as message content; the lane worktree was clean immediately before and after the read","tests_pass":false,"witness":null},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"28 groups, 28 pass, 0 fail, 0 skipped, duration_ms 44.205417 against the unmodified temp copy, matching the report's 28 groups and its 0.046 second figure","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"28 groups, 0 pass, 28 fail, 0 skipped, with 28 deliberate control messages","observed_failure":"all 28 parser groups failed on their deliberate control assertion with zero passes, so the parser file's controls are present and effective"}},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":1,"result":"25 pass, 3 fail against a temp copy whose parser module was replaced by the pre-round-5 blob while the reviewed test file was kept; the three failures are exactly the extended chrome group, the cumulative status corpus and the count-clause tool-summary group, reproducing the report's claim that the focused file passed 25 of 28 and failed exactly those three groups before the product change","tests_pass":false,"witness":null},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"28 groups, 28 pass against a copy patched so the Claude approval boundary requires two or more leading spaces","tests_pass":true,"witness":{"kind":"mutation","command":"node witness.mjs","cwd":"scratch","exit_code":0,"result":"on the patched copy the four Claude answers that open with approval wording are returned again instead of the older superseded block, the round-5 indented-prompt fixture stays excluded, and the Codex result is unchanged","observed_failure":"a behavior-changing patch that stops the newest Claude answer from being replaced by a stale block left all 28 committed groups passing, so no committed test observes the regression"}},{"command":"node --test test/message-preview.test.mjs","cwd":"scratch","exit_code":0,"result":"28 groups, 28 pass against a copy patched so a named progress verb is status chrome only when the elapsed time ends the trailing status form","tests_pass":true,"witness":{"kind":"mutation","command":"node witness.mjs","cwd":"scratch","exit_code":0,"result":"on the patched copy the four progress-verb sentences whose prose contains three periods and a later duration are returned again for both adapters, while the parenthesized, bracketed, ellipsis-character, three-period and interrupt-advertising chrome lines stay rejected","observed_failure":"a behavior-changing patch that stops ordinary three-period prose from being treated as status left all 28 committed groups passing, so no committed test observes that false positive"}},{"command":"node probe1.mjs","cwd":"scratch","exit_code":0,"result":"the generalized tool-summary boundary was probed over 16 bodies for both adapters against the reviewed and round-4 modules; the fixed four-clause sequence, five single-clause subsets and three reorderings are all excluded now and six of those nine were appended into the message by the round-4 module, so the first round-4 finding is resolved; seven near-miss prose bodies that mention a count and a noun, including a clause followed by more words, a clause preceded by a pronoun, a definite article before the count, a spelled-out number, a clause followed by a conjunction and a clause ending in a period, all remain part of the message in both adapters","tests_pass":false,"witness":null},{"command":"node probe2.mjs","cwd":"scratch","exit_code":0,"result":"the second and third round-4 findings are resolved: an indented Claude approval prompt under an answer is now excluded where the round-4 module appended it, and a three-period status line carrying an elapsed time is now rejected in both adapters where the round-4 module published it as the message; a polite variant addressed to the assistant and a plain question survive as answers, and a named progress verb with three periods but no duration is still ordinary prose","tests_pass":false,"witness":null},{"command":"node regress.mjs","cwd":"scratch","exit_code":0,"result":"a cumulative regression matrix drove every answer and chrome example recorded in rounds 1 through 4 against the reviewed module for both adapters; all 40 answer cases, being the six round-3 tool-word answers, eight round-1 tool-word openings, four progress-verb duration answers and two round-2 shapes, are returned verbatim as the current message, all eight multiline shapes covering indented quotes, nested bullets, a directory tree and a table are preserved whole, and all 30 chrome cases are excluded; the only differences from the round-4 module are the six intended round-5 fixes","tests_pass":false,"witness":null},{"command":"node probe3.mjs","cwd":"scratch","exit_code":0,"result":"six openings were compared across the reviewed and round-4 modules for both adapters; the four approval-worded Claude openings changed from being returned as the newest answer to being replaced by the older superseded block, while two control phrasings survive and the Codex results are unchanged in both modules","tests_pass":false,"witness":null},{"command":"node probe4.mjs","cwd":"scratch","exit_code":0,"result":"seven progress-verb lines were compared across the two modules for both adapters; the four sentences whose prose contains three periods and a later duration changed from answers to the older superseded block, while the two round-3 fixtures without an ellipsis and one three-period sentence without a duration are unchanged","tests_pass":false,"witness":null},{"command":"node e2e.mjs","cwd":"scratch","exit_code":0,"result":"through refreshMessagePreviews with a fake protocol-shaped client, both regressions publish the older superseded block with source assistant-preview, available true, stale false, truncated false, limitation null, the read revision and a fresh observation time, so neither carries any staleness or limitation signal; an ordinary newest answer is published correctly in the same harness","tests_pass":false,"witness":null},{"command":"node timing.mjs","cwd":"scratch","exit_code":0,"result":"seven pathological single-line inputs, being 400 two-word count clauses with a failing tail, 400 matching clauses, 800 one-word clauses with a failing tail, a 4000-character hyphen run, a 4000-character space run, 300 near-miss prose clauses and a 16 KiB clause line, were parsed in at most 9.24 milliseconds for either adapter, so the generalized clause pattern shows no catastrophic backtracking at the bounded read size","tests_pass":false,"witness":null},{"command":"git diff --check 3367cd5 691cb6d","cwd":"lane","exit_code":0,"result":"no whitespace or conflict-marker damage in the reviewed delta; board/app.mjs is byte-identical to main by blob hash; no file under docs/reviews changed between the round-4 record commit and the reviewed commit; the round-5 delta touches only the parser, its test file, REFERENCE, the report and HANDOFF, leaves every read bound, the pane read budget, the reader pool, the coalescing rule and the tripwire live-only sentence untouched, and its added lines contain no absolute path, account, host or socket path beyond an elided board socket name in report prose","tests_pass":false,"witness":null}]
```

## Non-claims
- I do not claim either regression occurs in a real render; both were reproduced in a temp copy from reviewer-authored text, and the single authorized live read showed no chrome and no stale answer inside any of the 11 available previews.
- I do not claim the count-clause boundary is wrong to exclude an indented answer continuation built entirely of that grammar; round 4 recommended this exact generalization and disclosed that consequence, the report repeats it, and my near-miss probes show ordinary prose survives, so I treat it as an accepted trade-off rather than a defect.
- I do not claim any defect in the read bounds, the reader pool, the pane read timeout, occupant replacement, stale retention on read errors, the tripwire subscription, the action boundary or the interactive UI; the round-5 delta changes none of them.
- I do not claim the report or HANDOFF counts are wrong; both declare 160 tests and my run measured 160 with zero skips, and the focused figure of 28 groups also reproduced along with the claimed 25 of 28 pre-change failure.
- I do not claim the engineer's own post-commit gate result for this commit; the report states it is conversation-only evidence, the supplied facilitator gate for this commit exited zero, and that is inherited evidence rather than a fresh test claim.
- I do not claim the round-3 report finding is reintroduced; the round-5 report and HANDOFF do not assert that no sandbox failure occurred, and both name the one board-focus socket EPERM in the negative run.
- I do not claim the two proposed patches are complete classifiers; each was probed only against the shapes listed in this record and exists to show that no committed test observes either regression.
- I do not claim the same candidate-first-line exposure is absent for the older context and token boundary entries; those predate this delta and were accepted in earlier rounds, so I did not raise them here.
- I do not claim the interactive board renders these previews correctly; no terminal UI was launched in this round.

## Unverified
- The interactive board, focus and done actions, alternate hosts and any real TTY render were not exercised.
- Only one live board read was authorized, so neither regression could be hunted on live panes and the 11 clean previews are a single sample.
- The live read returns parsed previews only, so the raw rendered text behind them was not inspected and the real rendered form of an in-flight count-clause summary remains unknown to me.
- The 200-line request, the 16 KiB retention bound, the four-reader pool and the two-second pane read budget were confirmed from the code and the committed read-state groups, not by forcing an oversized or slow live read.
- Watch-mode coalescing at one read per pane per second and the five-second sampling interval were not measured against the live server.
- All budgeted runs used a fresh single-commit system-temp copy of the reviewed tree rather than the lane worktree, so behavior depending on the lane's own history was not exercised.
- The OpenSpec validation's process exit status was lost through a shell pipeline and was read from its printed totals line; every other exit status was captured directly.
- Two other projects ran focused test batches on this machine during the review; each budgeted run began after a probe showed no build or test process, but the sub-second node probes were not serialized against them.
- The engineer's reported sandbox socket EPERM was not reproduced here and could not be, since this host allows the system-temp sockets; my negative run reached all 160 controls.
- No push, promotion, archive, close, dispatch, fix, commit or public record write was performed or attempted, and the lane worktree was clean before and after every step.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
