**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: f243562a2b096a7f48b683ad1690330a299b0665
Topic: board-inventory
Round: 2
Review ID: lr-8d41effe7d794f6fd7a1c6f62c426a26
Base branch: main
Base commit: 8d80ffae74aa05c4135bc159995e177f3ba2dcc1

## Findings
- [Major] board/inventory.mjs:594 - A matched registry row always takes its branch from session.lane, even when the agent's verified checkout now has a different branch or detached HEAD. Workspace verification compares live Git against live workspace mapping but never against the recorded lane, so the live row can display another checkout's Git, gate, and report evidence; Fix: require the recorded lane to agree with verified live checkout ownership before joining, otherwise retain the historical record separately and preserve the live occupant as unregistered, with regressions for registered branch switches and detached HEAD.
- [Moderate] board/inventory.mjs:337 - Registry matching checks name, pane, workspace, and repository but never compares a recorded immutable terminal or agent-session identity. A replacement that reuses the same name and pane in the same workspace can inherit the previous session's role, goal, tripwires, and done marker; an idle replacement is then hidden as completed history. The live identity hash and unique-claim count do not distinguish that replacement from the recorded occupant; Fix: retain and compare immutable occupant evidence for registry joins, reject replaced or unverifiable claims, and cover a same-name, same-pane replacement while preserving it as an unregistered live row.
- [Moderate] lane.mjs:2456 - A status-change event now emits only the updated status frame and never schedules a message-preview read. The previous watch scheduled a preview for every status change, so a status-only transition can leave the previous answer displayed until the periodic refresh despite the documented status-triggered sampling behavior; Fix: route status changes through the coalesced preview refresh after emitting their status frame and add a status-only event regression that verifies the resulting pane read.
- [Minor] board/board.mjs:489 - The shared projection copies session.goal into brief.excerpt for every row. Inventory assigns an unregistered agent's terminal title to that goal, so a title-only session now exposes a fabricated brief excerpt even though the inventory contract requires its brief to remain unknown; Fix: keep brief.excerpt null for an unregistered title-derived goal, preserve the separately labeled goal, and assert both fields in the unregistered-agent fixture.

## Re-executed
```json
[]
```

## Non-claims
- The captured gate and delivery measurements are inherited evidence only; this review makes no fresh execution claim.
- This verdict applies only to the fixed reviewed commit and does not authorize lifecycle actions or establish readiness for another commit.
- Deferred sampling performance and freshness, and the separate interactive frame and details work, are outside this verdict.

## Unverified
- No build, test, prepare, install, check, or mutation command was run because the supplemental review budget is zero; runtime reproductions and negative-control failures were not freshly observed.
- Live Herdr discovery and watch behavior, minimum-version compatibility, alternate hosts, and interactive rendering were not exercised; the findings follow from source inspection and committed fixture coverage.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
