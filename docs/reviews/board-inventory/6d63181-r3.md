**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: 6d63181ae36b6bce38f0209fb4c3d83b8a16aff6
Topic: board-inventory
Round: 3
Review ID: lr-605ae63621b1e8462d06ed3b739ea02c
Base branch: main
Base commit: 8d80ffae74aa05c4135bc159995e177f3ba2dcc1

## Findings
- [Moderate] lane.mjs:2514 - A status-triggered preview request is discarded if its one-second timer fires during an existing collection: the timer is cleared, refresh returns because refreshPending is true, and completion schedules no replacement. A status change after that pane was sampled can therefore leave its old preview until the next periodic tick; Fix: retain a coalesced pending preview request while collection is busy, drain it after completion without overlapping observations, and cover a status-only event during a delayed collection.
- [Moderate] board/inventory.mjs:252 - Inventory identity ignores terminal\_id whenever an agent-session value exists. When the same pane gets a different terminal with the same agent-session value, registry matching rejects the recorded terminal but runtime retention still treats it as the old occupant. The separate unregistered row can inherit the previous tripwire and, on a read error, its previous preview; Fix: include available terminal identity in the shared occupant key used for runtime and live identities, and cover a terminal replacement that retains the agent-session value.
- [Minor] test/board-inventory.test.mjs:551 - The workspace and ambiguous-claim fixture puts every live agent on main while every record names a lane branch. The new branch guard rejects the claims regardless of the workspace and duplicate-claim safeguards, so the current assertions no longer establish those acceptance behaviors; Fix: give the candidates matching verified lane checkouts and include a successful single-claim baseline before testing missing, stale, and duplicate workspace or occupant claims.
- [Minor] board/board.mjs:488 - An unregistered agent without a terminal title is normalized with goal null and goal\_source null, but the nullish fallback changes its goal\_source to registry. This invents registry provenance for a row with no registration or goal evidence; Fix: preserve an explicit null goal\_source and infer registry provenance only for registry goal metadata, with a no-title unregistered regression.

## Re-executed
```json
[]
```

## Non-claims
- The captured 226-test gate and reported OpenSpec 23-of-23 result are inherited evidence only; this review makes no fresh execution claim.
- This verdict applies only to the fixed reviewed commit and does not authorize lifecycle actions or establish readiness for another commit.
- Deferred sampling performance and freshness, and the separate interactive frame and details work, are outside this verdict.

## Unverified
- No build, test, prepare, install, check, or mutation command was run because the supplemental review budget is zero; runtime reproductions and negative-control failures were not freshly observed.
- Live Herdr dispatch, terminal replacement, overlapping watch events, multiple accessible endpoints, alternate hosts, and minimum-version behavior were not exercised; the findings follow from source inspection.
- Interactive rendering was not exercised, and the Ink entrypoint and UI modules are outside this review slice.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
