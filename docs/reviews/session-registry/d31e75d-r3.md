**PASS**
Schema: lane-review/v1
Reviewed commit: d31e75d596ead23d776aee52e65b04e30ea6877b
Topic: session-registry
Round: 3
Review ID: lr-abf06bffb128a418fdf944189c8e70ab
Base branch: main
Base commit: 4e55e4c4277b5a8dbb2a3413316930d6bc4af7a5

## Findings
- [Minor] lane.mjs:528 - a repository-local symlinked registry is a configuration policy refusal of exactly the kind that now warns, but assertNoRegistrySymlink still throws a plain Error, so a fully successful close exits 1 with lane closed; metadata update failed after the worktree was removed and the branch deleted, which is the residual instance of the exit-status defect rounds 1 and 2 raised; Fix: throw RegistryPolicyError from this check too, which is safe because an escaping symlink is already reported by the earlier containment check and dispatch still fails on any preflight error, and add a repository-local symlinked registry scenario to the close policy list in test/lane.test.mjs:1961.
- [Minor] lane.mjs:564 - the readability probe rethrows a whole-registry read failure, so an unparseable or non-array legacy sessions file also turns a fully successful close into exit 1 with cannot read lane board registry, while docs/REFERENCE.md:553 tells the operator that read errors are warnings and that a successful close remains successful; Fix: catch the registry-level read failure inside the close metadata block and print it with the same warning line used for per-record errors, or narrow the REFERENCE sentence to per-record read errors and state that an unreadable registry file remains fatal.
- [Minor] docs/reports/session-registry.md:6 - the headline measured implementation commit 25aeab38 and the round-2 correction commit 1c7129a3 cited at line 72 and in HANDOFF.md:630 are pre-rebase objects that are not ancestors of the reviewed head, so once this lane promotes and unreachable objects are collected, no reader can resolve the commits those measurements belong to; Fix: cite the post-rebase equivalents c4218f4 and 09481ba beside or instead of the pre-rebase identifiers in both files, keeping the existing sentence that records the rebase onto 4e55e4c.

## Re-executed
```json
[{"command":"npm test","cwd":"scratch","exit_code":0,"result":"TAP totals: 109 tests, 109 pass, 0 fail, 0 skipped, 0 todo, 104.842 s. The process exit status 0 was captured directly rather than inferred from the totals.","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"TAP totals: 109 tests, 0 pass, 109 fail, 0 skipped, 99.896 s. Exit status 1 captured directly.","observed_failure":"All 109 tests failed at their deliberate control assertions with zero passes. That set includes test 60, which carries the three new inline goal scenarios, test 61 for the shared socket helper, test 71 for the new close policy warning, test 72 for close completion ordering, and test 73 for the retained fatal marker-write boundary, so none of the round-3 regressions can pass vacuously."}},{"command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"TAP totals: 109 tests, 0 pass, 109 fail, 0 skipped, 99.896 s. This is the witness run above, recorded once as its own authorized command.","tests_pass":false,"witness":null},{"command":"OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"scratch","exit_code":0,"result":"Totals: 17 passed, 0 failed (17 items), including spec/session-metadata and change/session-registry. Exit status 0 captured directly.","tests_pass":false,"witness":null},{"command":"temporary-repository probe: close with the registry configured to an external absolute path holding one legacy entry for the closed topic","cwd":"scratch","exit_code":0,"result":"close exited 0, removed the worktree, deleted the merged branch, and printed only the stderr warning that automated session metadata requires a repository-local registry. No sidecar directory was created and the external array was left untouched. The round-2 Moderate is reproduced as fixed.","tests_pass":false,"witness":null},{"command":"temporary-repository probe: close with a repository-local ignored registry whose matching done-marker path is occupied by a directory","cwd":"scratch","exit_code":0,"result":"close exited 1 with lane closed; metadata update failed and an EISDIR rename error after the worktree removal and branch deletion completed, so a genuine marker write failure is still fatal and the new warning path did not swallow it.","tests_pass":false,"witness":null},{"command":"temporary-repository probe: close with a healthy repository-local registry, then with no registry at all","cwd":"scratch","exit_code":0,"result":"The healthy close exited 0, printed marked 1 session done, wrote exactly the expected legacy done marker, and left the legacy array file itself unmodified. The git-only close exited 0 and created no registry directory at all.","tests_pass":false,"witness":null},{"command":"temporary-repository probe: close with an unparseable legacy registry file, and close with a repository-local symlinked registry","cwd":"scratch","exit_code":0,"result":"Both closes removed the worktree and deleted the branch but exited 1 with lane closed; metadata update failed, one naming the registry read failure and one naming the symlinked component. These two runs are the evidence for the first two findings.","tests_pass":false,"witness":null},{"command":"temporary-repository probe: close with an ignored registry whose sidecar directory mode was set to read and execute only","cwd":"scratch","exit_code":0,"result":"close exited 1 with the not writable message after the git operations succeeded. This one is a real write-capability failure detected in preflight rather than a configuration policy refusal, so its fatal status is consistent with the delivered boundary and is not reported as a finding.","tests_pass":false,"witness":null},{"command":"temporary-repository probe: dispatch with Herdr absent from the probe PATH against external, canonically tracked, unignored, escaping-symlink and repository-local-symlink registries","cwd":"scratch","exit_code":0,"result":"All five refused with their own registry message, exited 1, created no sidecar, left the lane worktree clean, and never reached the herdr is unavailable path, including the tracked case invoked from the lane worktree. The round-1 preflight ordering and canonical-index safeguards survive the round-3 edits.","tests_pass":false,"witness":null},{"command":"temporary probe: the goal helpers extracted verbatim from the reviewed lane.mjs evaluated against fourteen prompts and briefs","cwd":"scratch","exit_code":0,"result":"A prose-first inline prompt with a later heading now yields the first prose line, a heading-first inline prompt yields the stripped heading, a fence-first inline prompt yields the first unfenced prose, and fence-only, unterminated-fence, empty and whitespace-only inputs yield the topic. File briefs still take the first unfenced heading and fall back to the first prose line, and the shipped review template still yields its heading text. All three round-2 goal expectations are met.","tests_pass":false,"witness":null},{"command":"read-only git inspection of the delta, the rebase, the amendment and the specification sync","cwd":"lane","exit_code":0,"result":"git diff --check over the whole delta is clean, the added-line scan for home directories, host paths, identities, addresses and secrets found nothing, the approved A2 paragraph appears verbatim in AGENTS.md and is the only contract change, the change delta spec equals the current capability spec once the ADDED heading is normalized, the round-2 public record is still blob ab07a630, both HANDOFF entries sit in author-time order around the main entry, and no line added by the review-cli-fix commit on main is removed by this lane.","tests_pass":false,"witness":null}]
```

## Non-claims
- I do not claim any live Herdr lifecycle behavior was exercised. Every dispatch, prompt, pane and workspace result came from the suite's fake Herdr script or from probes run with Herdr absent from the probe PATH.
- I do not claim the suite ran entirely without touching the local Herdr server. Herdr is present on this machine, so the one pre-existing verify-agent guard test executed a read-only agent query instead of skipping; nothing was started, prompted, closed or otherwise mutated.
- I do not claim the gate captured at the reviewed head is review evidence. It is inherited operator evidence and promotion revalidates independently of it.
- I do not claim to have reproduced the report's history of tests observed failing before each implementation change. I verified only that today's delivered tests bind their requirements, through one suite-wide negative control that reached all 109 controls.
- I do not claim the interactive Ink board entrypoint was executed. Its package dependencies are not installed here, so I read that file and exercised only the plain board modules through the suite.
- I do not claim to have run any command inside the lane worktree except read-only git and file inspection. Every authorized run and every probe used a system temporary copy verified byte-identical to the lane worktree's tracked content at the reviewed commit, and all fixtures were removed afterwards.
- I do not claim the three findings were fixed or retested; I did not modify the tree.

## Unverified
- No live Herdr dispatch, prompt, close, or daemon-backed concurrent dispatch was exercised, because the budget authorizes only offline commands and the protocol forbids lifecycle actions.
- No run with Herdr removed from PATH was performed this round, because the budget authorizes npm test exactly once; the report's claim of one explicit skip in that configuration rests on the engineer's measurement.
- No Linux or Windows run was performed. All evidence comes from macOS with Node 20.19.4, npm 10.8.2 and git 2.54.0, so the stated Node 20 and git 2.38 floors remain unverified on other platforms and versions.
- Filesystem fault injection was not performed. Unwritable, unreadable and unresolvable registry paths were simulated with a directory, a corrupt file, a dangling symlink and a mode change, not with quota, device or interrupted-write errors.
- Real filesystem races were not measured. Concurrency rests on the suite's parallel child-process writes plus the immutable-link and separate-marker design, not on contended timing.
- I did not verify that the pre-rebase commits named in the report are byte-identical to their post-rebase counterparts beyond matching subjects and the rebase's own equivalence; patch identifiers differ because the surrounding history differs.
- Report resolution across the lane and canonical checkouts was exercised only through the board module in the delivered test, not through a rendered board against a running Herdr snapshot.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
