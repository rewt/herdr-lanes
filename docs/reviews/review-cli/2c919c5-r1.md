**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: 2c919c59e780581cc050eb64eebed91a8f94d2b2
Topic: review-cli
Round: 1
Review ID: lr-66d7c0746ccb519f570239dd30c01c57
Base branch: main
Base commit: 655fdf46ca5c67ab460657a45ba57bee7b16a024

## Findings
- [Major] lane.mjs:1212 - Section slicing always drops the physical line immediately before the next heading, so when a reviewer omits the blank line after a section's last entry that entry disappears from both schema validation and the public projection; a private record holding two Major findings published a public record holding one and still exited 0; Fix: compute end as the next heading's own line index rather than one line above it, and let the existing trailing-newline trim remove the blank separator.
- [Moderate] lane.mjs:1414 - assertProtectedPublicValue checks only aliases, absolute paths and the ASCII range, so a finding location escapes the payload restrictions enforced on every other projected string; a location carrying inline-code marks, angle brackets, square brackets, emphasis sequences and a reserved USER placeholder was published verbatim while the Sanitization counts reported user 0, and a percent-encoded declared identifier passed the alias scan into the public record; Fix: apply the same markup, encoding and reserved-placeholder rejections that sanitizePublicString enforces to the location before emitting it, and correct the claim at docs/REFERENCE.md:404.
- [Moderate] lane.mjs:1163 - The unresolved-placeholder guard runs after SOURCE_MATERIAL and SUPPLEMENTAL_BRIEF are substituted, so any uppercase double-brace token inside the reviewed repository's AGENTS.md, openspec README, change artifacts, engineer report, latest HANDOFF entry or the supplied brief aborts the whole review with exit 2 under a diagnostic blaming the template; a brief token and a tracked AGENTS.md token each reproduced it; Fix: substitute and validate the metadata placeholders first, then insert the two literal source blocks without rescanning them.
- [Moderate] lane.mjs:1265 - The free-prose guard matches limitation wording as well as assertions, and also scans the private Analysis section, so a mandatory Unverified bullet recording that a witness was unavailable for a suite claim is refused with exit 2 even though the design asks for exactly that disclosure; Fix: drop Analysis from the scanned prose and narrow the pattern so negated or witness-absent phrasing in Unverified is accepted.
- [Minor] lane.mjs:1621 - The dispatch result is captured and then discarded, so the timeout message at lane.mjs:1521 and the interrupt message at lane.mjs:1506 name only the private path and omit the session identifiers the design requires for locating a reviewer that may still be writing; Fix: pass the agent name and tab id from dispatch into waitForPrivateReview and include them in both messages.
- [Minor] docs/REFERENCE.md:345 - The public-path paragraph still carries R-i wording saying the trackable public path is verified even though this first slice does not write it, which contradicts the shipped command and the later publication section; Fix: replace that clause with a statement that the same command creates the public record after the unchanged-lane check.

## Re-executed
```json
[{"command":"npm test","cwd":"scratch","exit_code":0,"result":"81 tests, 81 pass, 0 fail, 0 skipped in 33.7 seconds on a system-temp copy of the reviewed tree","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"81 tests, 0 pass, 81 fail in 35.8 seconds on the same copy","observed_failure":"Expected every case to fail once the deliberate control is enabled; observed 0 pass and 81 fail, so the green run exercises real assertions rather than reporting success vacuously."}},{"command":"OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"scratch","exit_code":0,"result":"Totals: 16 passed, 0 failed (16 items), including spec/foreground-lane-review and change/review-cli","tests_pass":false,"witness":null},{"command":"node --test --test-name-pattern 'PROBEA|PROBEB|PROBEC|PROBED|PROBEE|PROBEF'","cwd":"scratch","exit_code":0,"result":"Six added mutation probes ran against a system-temp copy and reproduced the published-markup, encoded-identifier, dropped-finding, brace-token and prose-refusal behavior described in the findings","tests_pass":false,"witness":null},{"command":"npm test in a throwaway two-case temporary package","cwd":"scratch","exit_code":1,"result":"Exited 0 with one succeeding case and 1 after adding one failing case, confirming that node --test propagates failure through npm","tests_pass":false,"witness":null}]
```

## Non-claims
- No live reviewer agent, real Herdr server mutation, or stalled real dispatch subprocess was exercised.
- No public record was produced for this lane; the projection behavior was observed only on temporary fixtures.
- The green gate at the reviewed commit is inherited evidence, not a fresh execution result and not a verdict.
- The mechanical sanitizer is not shown to remove every undeclared human name or secret from arbitrary prose.
- The findings describe the delta from the base commit through the reviewed commit only; earlier history was not audited.

## Unverified
- Exit statuses of the three budgeted commands were read from their summary output rather than captured directly; node exit propagation was confirmed by a separate temporary probe, and the OpenSpec status rests on its zero-failure total.
- Only local platform alias sources were exercised; Windows, Linux, a distinct nested full and short hostname, and a forced failure of the OS alias readers were not.
- Behavior when the lane worktree is deleted or detached mid-review was not exercised; the unguarded calls at lane.mjs:1585 and lane.mjs:1588 would throw as an unhandled rejection rather than reach the exit-2 boundary, and no top-level handler was found.
- Concurrent private writers, abbreviation collisions across differing full commits, permission-denied filesystems, and a maximum-size valid record were not exercised.
- The carried workspace-refresh correction was reviewed by reading and by its two added cases in the suite run above; no live Herdr snapshot race was reproduced.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
