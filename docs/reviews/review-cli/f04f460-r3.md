**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: f04f460295b272ea0db81b375bde8d2840d56f97
Topic: review-cli
Round: 3
Review ID: lr-2600bc2f5b695619c0677f3f6b3d2490
Base branch: main
Base commit: 655fdf46ca5c67ab460657a45ba57bee7b16a024

## Findings
- [Moderate] lane.mjs:1221 - waitForPrivateReview at line 1569 treats a record as complete using trimEnd, but validateReviewRecord strips only one trailing newline at line 1200 and then requires the marker to be the last line, so a private record ending with the completion marker plus one blank line is accepted as finished and immediately refused. A probe using the shipped fake reviewer with one extra trailing newline reproduced exit 2 and the message that the completion marker must be the final line, discarding a round after the reviewer had already finished the work, and leaving the round occupied so recovery needs the documented removal sequence. The same commit generalised the trailing-newline trim inside sections but left this outer boundary at a single newline; Fix: strip the trailing newline run before splitting, or compare the last non-empty line, so the validator tolerates exactly what the completion detector already accepts, and add a regression for a record ending with the marker and a trailing blank line
- [Moderate] lane.mjs:1912 - The review command is awaited at module top level with no error boundary, so a throw from a shared helper escapes the documented exit-2 refusal contract. A probe whose fake reviewer removed the lane worktree after writing a valid private record made the post-dispatch rev-parse throw, and lane review exited 1 with an uncaught Node stack trace and empty stdout rather than exiting 2. Exit 1 is the documented code for a completed NEEDS-WORK or FAIL review whose public record was written, so a wrapper keying on the exit code alone reads a crashed review as a finished one, and the trace also prints absolute worktree paths that the sanitizer never sees; Fix: wrap the awaited review call in a try/catch that writes one concise diagnostic and exits 2, and add a regression asserting exactly 2 with empty stdout when the lane worktree disappears after dispatch
- [Minor] lane.mjs:1228 - Section slicing now trims a run of trailing newlines but still strips only a single leading newline, so a record with two blank lines after a section heading keeps one blank as a section line. A probe with two blank lines after the Findings heading exited 2 with the new message that Findings spacing must not contain blank lines between entries, which names the wrong position because the blank precedes the first entry rather than separating two entries, repeating in mirror image the misattributed diagnostic this round set out to remove; Fix: trim leading newline runs symmetrically in the same replace, and extend the spacing regression to cover extra blank lines after a heading as well as before the next one
- [Minor] docs/REFERENCE.md:363 - The validator refuses any blank line inside the Findings section and this round added a dedicated diagnostic for it, but neither the REFERENCE schema paragraph nor the reviewer template states that finding entries must occupy consecutive lines. A reviewer who separates entries with a blank line, which is ordinary Markdown and which the shipped template never warns against, loses an entire round to an unstated rule; Fix: state in the REFERENCE schema paragraph and in the template required-output section that Findings entries occupy consecutive lines with no blank line between them

## Re-executed
```json
[{"command":"npm test","cwd":"scratch","exit_code":0,"result":"88 of 88 cases reported ok with 0 failures in 43.1 seconds, on a system-temp copy of the reviewed tree whose lane.mjs and test file digests match the lane byte for byte","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"0 pass and 88 fail in 42.9 seconds on the same copy, with exit status 1 captured directly","observed_failure":"Expected every case to fail once the deliberate control is enabled; observed 0 pass and 88 fail, so the green run exercises real assertions rather than reporting success vacuously."}},{"command":"node --test --test-name-pattern for the three round-three regressions, run against a system-temp tree holding the parent commit lane.mjs and the reviewed commit test file","cwd":"scratch","exit_code":1,"result":"All three round-three regressions failed against the pre-fix parent with 0 pass and 3 fail: the extra-spacing case expected exit 1 and observed 2, the spacing-diagnostic case observed the old message, and the spaced-location case expected exit 0 and observed 2. The same three pass at the reviewed commit, so both round-two findings are resolved and their regressions genuinely detect the old defects","tests_pass":false,"witness":null},{"command":"node --test --test-name-pattern zzprobe, one run of nine added probe cases on system-temp copies of the reviewed tree","cwd":"scratch","exit_code":0,"result":"Two blank lines before the next heading published both findings; a repository-relative location containing a space published intact while its description and fix still redacted an injected user alias; the same location carrying an injected alias still refused with the protected-location message; a trailing blank line in Unverified was accepted; a blank line inside Non-claims refused with its own correctly attributed message; two blank lines after the Findings heading refused with the misattributed spacing message reported above; a record ending with the completion marker plus one blank line was detected as complete and then refused with exit 2 and no public record; and removing the lane worktree after a valid private record was written produced exit 1 with an uncaught Node stack trace and empty stdout instead of the required exit 2","tests_pass":false,"witness":null}]
```

## Non-claims
- No live reviewer agent, real Herdr server, or stalled real dispatch subprocess was exercised; every execution used the offline fake fixture in system-temp copies.
- No public review record was created in the reviewed lane by this review; every projection result reported here came from system-temp fixtures.
- The green gate at the reviewed commit is inherited evidence, not a fresh execution result and not a verdict.
- The mechanical sanitizer is not shown to remove every undeclared human name or secret from arbitrary prose.
- The two Moderate findings describe boundaries the round-three commit did not touch; they are pre-existing gaps in the delta, not regressions introduced by that commit.
- These findings cover the delta from the base commit through the reviewed commit only; earlier history was not audited.

## Unverified
- The exit status of the passing suite was read from its zero-failure summary rather than captured directly; the deliberate control exit status 1 was captured directly.
- The two budgeted commands ran on a system-temp copy rather than inside the lane worktree, to honour the reviewer prohibition on creating anything there; the two files that determine behaviour were confirmed identical by digest, but no run inside the lane itself was made.
- The cross-commit control overlapped with another session's short-lived process on this shared machine; each budgeted run followed a probe showing no other build or test process above the threshold.
- Only local platform alias sources were exercised; other operating systems, a distinct nested full and short hostname, and a forced failure of the OS alias readers were not.
- Concurrent private writers, abbreviation collisions across differing full commits, permission-denied filesystems, and a maximum-size valid record were not exercised.
- An empty Findings section was reasoned about but never observed, because the shipped fixture treats an empty findings payload as absent and substitutes None.
- Windows and drive-letter path forms, the interactive board UI, and OpenSpec strict validation were not run in this round.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
