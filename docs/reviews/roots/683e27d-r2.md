# Review r2 — roots-config (confirmation pass)

Reviewed commit: 683e27d8cf5d23c6c2282864d03a6e1c7b038da9

Verdict: **READY**

Confirmation review of the corrections made after review r1, examined as the difference
between the review-record commit and this one; the intervening record commit is excluded
and was not altered. All eight r1 findings are resolved and nothing new rises above a
nit. No tracked file was modified by this review.

## Finding results

**minor-1 — nearest-parent test not discriminating: resolved.**
`test/lane.test.mjs:611-614` gives the grandparent a key nothing else defines, and
`test/lane.test.mjs:644` asserts it stays at the default. Merging any ancestor beyond the
nearest parent now fails the test.

**minor-2 — board bridge reused the public config override: resolved.**
`lane.mjs:770-786` passes `--repo`, `--main` and `--registry` to both board entrypoints;
there is no temporary file and no synthesized configuration variable. The child
environment is inherited unchanged, except that an operator-set explicit configuration
path is normalized to its resolved absolute form (`lane.mjs:775-777`) — correct, since
the child runs from the canonical checkout and a relative value would otherwise resolve
differently. Consumers: `board/args.mjs:11`, `board/cli.mjs:24-25`, `board/app.mjs:190-191`,
where the flags override file-derived values, with a "requires a value" guard per flag at
`board/args.mjs:3-9`. Lane commands started beneath the board again inherit ordinary
layered resolution, which was the point of the finding.

**minor-3 — temporary directory leaked on interrupt: resolved by removal.** No
temporary-directory machinery remains in `lane.mjs`; the imports were trimmed
(`lane.mjs:15-18`), so no cleanup path is left to miss.

**minor-4 — board retarget undocumented: resolved.** `docs/REFERENCE.md:185-187` states
that from a linked worktree the board retargets the canonical checkout and receives the
resolved main branch and inherited registry explicitly, while descendant lane commands
keep layered resolution.

**minor-5 — untested explicit-selection fallback: resolved.** `test/lane.test.mjs:811-816`
rewrites the selected file without the root key and asserts the legacy home default with
source `default`; the test now pins the home directory inside its fixture
(`test/lane.test.mjs:783,785`), so the assertion is hermetic.

**minor-6 — submodule identity: resolved as a documented limitation.**
`docs/REFERENCE.md:86-90` names the cause and both consequences and directs operators to
top-level repositories; repeated in the report's limits. No code guard, consistent with
deferring canonical identity to the next phase.

**nit-1 — unescaped record keys: resolved.** `lane.mjs:285-291` escapes backslash, tab,
carriage return and newline in the key; documented at `docs/REFERENCE.md:134-136`;
regression at `test/lane.test.mjs:677-693` asserts a single three-field row for a route
name containing all three control characters.

**nit-2 — raw stack on temp-file failure: resolved by removal** of that code path.

The r1 note on the report's preservation-control wording is also addressed: the report
now states that the registered-path test was the preservation control while the combined
occupied/foreign-path test failed on its new root behavior, matching its own arithmetic.

## Observations (nit level)

Round 2 edits four board files that are outside the change's declared scope list. This
follows directly from the requested fix, uses Node built-ins only — so the board
dependency rule still holds — and is recorded in the report and handoff; it is worth
naming so board files in a roots-config diff are not a surprise at promotion. The two new
flags are internal rather than a new public surface: `lane board` still rejects any
user-supplied argument other than `--once`, so no README entry is owed and the behavior
is documented in the reference. The new board regression is matched at the root level by
the pre-existing end-to-end linked-worktree board test, satisfying the shared rule that
new regressions also carry root `test/` coverage.

## Test evidence

- `npm test`: 60 tests, 60 passed, 0 failed, 0 skipped (~7.6 s).
- `LANE_TEST_NEGATIVE_CONTROL=1 npm test`: 60 tests, 0 passed, 60 failed (~7.6 s) — both
  new tests reach their negative-control assertion.
- Strict OpenSpec validation, telemetry disabled, concurrency one: 12 passed, 0 failed.
  Nothing under the specification directory changed in this round.
- `git diff --check` across the reviewed range: clean. An added-line scan for host or
  private text found no hits.
- The recorded gate names this commit as its head with a zero exit code — a fresh gate,
  though a gate is never a review verdict and promotion revalidates.
- Suites were run one at a time, with a clear machine-load probe beforehand.

## Limits

The Herdr-absent run was not repeated this round; the earlier pass verified the same
explicit-skip mechanism, and the new board regression forces the offline branch
deterministically. Only the single-shot board child was exercised end to end — the
interactive path passes identical arguments and applies them the same way, but was read
rather than run. Live dispatch, an actual submodule checkout, Linux and Windows hosts,
and concurrent destination races remain untested. No promotion, push, or archive was
performed here.
