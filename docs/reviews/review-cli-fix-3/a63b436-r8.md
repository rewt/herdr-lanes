**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: a63b436348e322ec95f202c37d13cf4b6901e336
Topic: review-cli-fix-3
Round: 8
Review ID: lr-09f05e4b122c3db632f8694d8137490f
Base branch: main
Base commit: 3367cd53e6fa50d3212f0da32e2189900b9b6d21

## Findings
- [Moderate] lane.mjs:1676 - The new ambiguity scan starts only when a space immediately follows the concrete match. A closing parenthesis between that match and a space therefore bypasses continuation scanning, allowing the remaining directories of a spaced path to publish in finding prose and commands at exit 0. The captured base refuses the same inputs at exit 2. Quote and bracket terminators expose analogous tails in the sanitizer sweep; Fix: inspect separator-bearing continuations after path terminators as well as directly after the match, preserve standalone closing delimiters when no ambiguous suffix follows, and add differential refusal fixtures.
- [Moderate] lane.mjs:1683 - A continuation token is treated as independently sanitizable merely because it contains any concrete absolute-path match. A relative path followed by a colon and another absolute path consequently bypasses the spaced-path refusal, and its relative directory prefix remains public in finding prose and commands. The captured base refuses the identical input; Fix: require the continuation token to have no unmatched separator-bearing prefix or suffix before treating its absolute matches as independent, otherwise refuse, and cover the case with a differential regression fixture.
- [Minor] test/lane.test.mjs:4154 - The new differential fixture reads a fixed historical Git object from the source checkout. In a depth-one clone of the reviewed commit it aborts before exercising the sanitizer, so npm test now depends on history absent from an otherwise complete checkout; Fix: make the historical comparison self-contained, for example with the historical matcher retained as fixture data, while keeping the current CLI refusal assertion independent of source-checkout history.

## Re-executed
```json
[{"command":"GIT_OPTIONAL_LOCKS=0 git diff --check 3367cd5 a63b436","cwd":"lane","exit_code":0,"result":"The fixed delta had no whitespace errors. HEAD and branch matched the requested target and the lane status was clean.","tests_pass":false,"witness":null},{"command":"env XDG_CONFIG_HOME=\"$review_scratch/xdg\" npm_config_cache=\"$review_scratch/npm-cache\" npm test","cwd":"lane","exit_code":1,"result":"Node 20.19.4 reported 149 successful cases, one failure, zero skips and zero cancellations in 280.066 seconds. All 50 review cases emitted successful results, including the settle rewrite, representative boundaries, timeout floor and closing-parenthesis differential. The sole failure was the known host-sensitive board completion case at test/lane.test.mjs:2113: Missing expected exception. XDG isolation did not override the explicit global core.excludesFile setting. This execution does not establish a green full suite.","tests_pass":false,"witness":null},{"command":"env REVIEW_PROBE_ROOT=\"$review_scratch\" node probe.mjs","cwd":"scratch","exit_code":0,"result":"Node 26.4.0 exercised 2247 public projections: 107 input shapes across seven payload fields against extracted base, round 7 and reviewed sanitizers. The requested earlier corrections redact or refuse, and plain regex prose publishes. Twenty-five command variants expose marker text at the reviewed commit where the base refuses. They reduce to the two spaced-path mechanisms in Findings. Each principal counterexample exposes the suffix in all seven projected field classes. The original contiguous closing-parenthesis case redacts at base, leaks at round 7, and refuses at the reviewed commit.","tests_pass":false,"witness":null},{"command":"env XDG_CONFIG_HOME=\"$review_scratch/xdg\" GIT_CONFIG_GLOBAL=\"$review_scratch/global.gitconfig\" node --test --test-name-pattern='^PROBE ' test/lane.test.mjs","cwd":"scratch","exit_code":0,"result":"Ten temporary full-command observations under Node 26.4.0 exercised five shapes in commands and finding prose. The reviewed CLI refuses the contiguous closing suffix and opening residue with exit 2, empty stdout and no public file. A standalone command-substitution delimiter publishes with the path redacted. Both spaced-path counterexamples instead publish the private suffix and emit PASS at exit 0. The observation harness records behavior; its own zero exit is not a safety verdict.","tests_pass":false,"witness":null},{"command":"env XDG_CONFIG_HOME=\"$review_scratch/xdg\" GIT_CONFIG_GLOBAL=\"$review_scratch/global.gitconfig\" LANE_TEST_CLI=\"$review_scratch/base/lane.mjs\" node --test --test-name-pattern='^PROBE ' test/lane.test.mjs","cwd":"scratch","exit_code":0,"result":"The identical ten observations against the fixed base CLI redact the original closing, standalone and opening shapes, while both spaced-path counterexamples refuse with exit 2, empty stdout and no public file in both fields. This confirms that the two disclosure findings are regressions relative to the captured base.","tests_pass":false,"witness":null},{"command":"env XDG_CONFIG_HOME=\"$review_scratch/xdg\" GIT_CONFIG_GLOBAL=\"$review_scratch/global.gitconfig\" npm_config_cache=\"$review_scratch/npm-cache\" LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"lane","exit_code":1,"result":"With Node 20.19.4, empty global Git configuration and isolated XDG configuration, all 150 cases failed through their intended deliberate-control diagnostics, with zero successful cases, skips or cancellations in 275.200 seconds. Every review control and the host-sensitive board control reached the deliberate failure. These end-of-body controls establish execution reachability, not sensitivity of every individual assertion.","tests_pass":false,"witness":null},{"command":"env XDG_CONFIG_HOME=\"$review_scratch/xdg\" GIT_CONFIG_GLOBAL=\"$review_scratch/global.gitconfig\" node shallow-probe.mjs \"$review_lane\"","cwd":"scratch","exit_code":1,"result":"A local depth-one clone reported the exact reviewed HEAD and shallow=true. Its new historical differential fixture aborted before exercising either CLI: git show exited 128 because lane.mjs was unavailable in the hard-coded base object. The selected test exited 1 in 0.346 seconds. Only the temporary clone was written.","tests_pass":false,"witness":null},{"command":"env XDG_CONFIG_HOME=\"$review_scratch/xdg\" OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"lane","exit_code":0,"result":"OpenSpec 1.6.0 running with Node 20.19.4 reported 19 valid items and zero invalid items. This is structural specification validation, not a runtime test claim.","tests_pass":false,"witness":null},{"command":"env XDG_CONFIG_HOME=\"$review_scratch/xdg\" GIT_CONFIG_GLOBAL=\"$review_scratch/global.gitconfig\" \"$review_node\" --test --test-name-pattern='^PROBE (closing spaced suffix|embedded concrete match)' test/lane.test.mjs","cwd":"scratch","exit_code":0,"result":"An explicitly selected Node 20.19.4 reproduced both spaced-path disclosures in commands and finding prose. All four reviewed-CLI invocations returned exit 0 with PASS and retained the private marker in the public record.","tests_pass":false,"witness":null},{"command":"env XDG_CONFIG_HOME=\"$review_scratch/xdg\" GIT_CONFIG_GLOBAL=\"$review_scratch/global.gitconfig\" LANE_TEST_CLI=\"$review_scratch/base/lane.mjs\" \"$review_node\" --test --test-name-pattern='^PROBE (closing spaced suffix|embedded concrete match)' test/lane.test.mjs","cwd":"scratch","exit_code":0,"result":"Under the same explicitly selected Node 20.19.4, the fixed base refused all four identical inputs with exit 2, empty stdout and no public file. The observation harness completed in 2.568 seconds. The regression is therefore reproduced on the supported Node baseline as well as the initial scratch runtime.","tests_pass":false,"witness":null}]
```

## Non-claims
- Question 1: the contiguous closing-parenthesis regression is closed by lane.mjs:1671. Full-command observations confirm refusal of that suffix, publication of the standalone delimiter, and retention of the opening-parenthesis refusal. This does not cover the separate spaced continuation in the first finding.
- Question 2: earlier flag, UNC, hosted URL, escaped-separator, regex-shaped and command-substitution corrections remain present. The suite exercised the settle rewrite at test/lane.test.mjs:3594, the five-case boundary matrix at test/lane.test.mjs:3641 and the timeout floor at lane.mjs:1278. The two new disclosure findings are within the full captured delta, not inherited matcher gaps.
- Question 3: docs/REFERENCE.md:552, docs/REVIEW\_TEMPLATE.md:97, openspec/changes/review-cli/design.md:268, the delta scenario at openspec/changes/review-cli/specs/foreground-lane-review/spec.md:76, and the current scenario at openspec/specs/foreground-lane-review/spec.md:78 agree on parenthesized residue. The current requirements match the change requirements. The report at docs/reports/review-cli-fix-3.md:229 and HANDOFF.md:979 distinguish historical baseline failures, isolated verification and conversation-only final gate evidence. Their consistency does not establish coverage of the new counterexamples.
- The supplied gate and local gate file both bind npm test exit 0 to the full reviewed commit. That is inherited evidence, was not regenerated, and is separate from the reviewer execution that exited 1.
- The known host excludes condition is not a lane finding. The normal run isolated XDG configuration and npm cache only; the control run additionally selected an empty global Git config. HOME was unchanged. All runs began after clear load probes and were serialized locally. Shell variables in the execution strings identify temporary directories, the reviewed lane, and the explicitly selected Node 20 executable; their exact private values appear only in Analysis.
- Follow-up candidates: the following pre-existing groups remain outside the verdict scope. They were compared with the captured base rather than treated as new findings.
- Follow-up candidate: a hosted file URL whose host includes a port remains unredacted at both commits.
- Follow-up candidate update: the previously reported drive-letter path with a parenthesized segment and a contiguous suffix now refuses under the new closing-parenthesis rule; the base exposes that suffix.
- Follow-up candidate: a three-separator file URL interrupted by an asterisk without whitespace, or by a backslash after a drive letter, still exposes its remainder at both commits.
- Follow-up candidate: an absolute path containing a comma followed directly by more path text exposes the suffix at both commits.
- Follow-up candidate: a long option attached to an absolute path, a short flag attached to a forward-separator UNC path, and a UNC server name with only its trailing separator remain unredacted at both commits.
- No claim is made that the mechanical sanitizer protects every undeclared identifier or path form. The full delta leaves dispatch, promotion and board production code unchanged. Temporary fake-reviewer runs exercised only isolated fixtures.

## Unverified
- The ordinary full-suite budget was used once and exited 1. It was not repeated with the additional global Git isolation used by the control run, so no fresh green full-suite claim is made. The inherited implementation timings and failure-first history were not exhaustively re-derived.
- Initial extracted-sanitizer, full-command and shallow-clone probes used Node 26.4.0 selected by the scratch shell. Both new disclosure findings were then reproduced with Node 20.19.4 for both reviewed and base CLIs. The suite and specification command used Node 20.19.4; Git was 2.54.0. Git 2.38 and alternate operating systems were not exercised.
- No live reviewer dispatch, live Herdr lifecycle action, real terminal UI, promotion, push or recovery operation was performed. No filesystem test targeted coarse timestamps or rewrites preserving both size and modification time. The settle check is not an operating-system lock.
- The extracted sweep covered seven payload field classes. The new disclosure counterexamples were driven through the full command in finding prose and command strings; other field classes were observed through extracted projection functions only.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
