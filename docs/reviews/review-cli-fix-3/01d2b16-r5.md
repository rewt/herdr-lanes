**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: 01d2b16e641918533bea1a9b0b362c73219ca6d6
Topic: review-cli-fix-3
Round: 5
Review ID: lr-99be992ce2c132f7583005e8af15987d
Base branch: main
Base commit: 3367cd53e6fa50d3212f0da32e2189900b9b6d21

## Findings
- [Moderate] lane.mjs:1646 - the POSIX alternative of the concrete matcher will not start a match when the preceding character is an ASCII letter, so an absolute POSIX path attached directly to a short command flag, meaning a hyphen, one or more letters, and then the leading separator with no space, matches no alternative and is published verbatim at exit 0 in finding prose, in a finding location and in a JSON command string; Fix: add a matcher alternative that permits the leading separator when the preceding whitespace-delimited token begins with a hyphen, keeping the flag text and replacing only the path, then add one fixture per affected field with its own deliberate negative control
- [Moderate] lane.mjs:1646 - a UNC path written with forward separators and a file URL that carries a host component match no alternative, because the file URL alternative requires three separators, the UNC alternative requires two backslashes, and the POSIX alternative rejects a doubled leading separator, so both shapes are published verbatim at exit 0 even though the synced capability spec names UNC paths and file URLs as covered categories with no syntax exemption; Fix: accept a doubled leading forward separator in the UNC alternative and an optional host in the file URL alternative, or refuse those two shapes explicitly, and cover each with its own controlled fixture
- [Moderate] docs/REVIEW_TEMPLATE.md:94 - the reviewer-facing template states that publication refuses prose only for non-ASCII or control characters, unresolved private identifiers, reviewer-written reserved placeholders, or residual paths and aliases, which omits the ambiguous spaced-path refusal and never mentions the parenthesized path residue refusal added by this very commit, so a reviewer following the shipped protocol can write a record the tool rejects with no guidance; Fix: extend that enumeration with both path refusals and add one sentence telling reviewers not to place an opening parenthesis immediately after an absolute path
- [Minor] docs/REFERENCE.md:547 - the lead-in sentence introducing the path refusal list is singular while the list now carries two entries, the ambiguous spaced path and the parenthesized path residue; Fix: make the lead-in plural so the reference matches the design and both capability specs

## Re-executed
```json
[{"command":"npm test","cwd":"scratch","exit_code":0,"result":"In a scratch export of the reviewed commit, 140 of 140 cases passed with zero failures and zero skips in 253.195 seconds. This reproduces the report's final suite claim.","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"On the same scratch export, zero cases passed and all 140 failed in 248.153 seconds.","observed_failure":"Every case was expected to fail through its deliberate control and every one of the 140 did, including the five cases added by this commit for the three escaped-separator fields, the parenthesized file URL residue and the relative-looking protected location, so the green run above is not vacuous."}},{"command":"OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"scratch","exit_code":0,"result":"19 items passed and zero failed, reproducing the reported strict validation total.","tests_pass":false,"witness":null},{"command":"LANE_TEST_CLI=../parent/lane.mjs node --test --test-name-pattern=\"escaped absolute path in|relative-looking protected location|parenthesized file URL residue|opening parenthesis after a concrete path match\" test/lane.test.mjs","cwd":"scratch","exit_code":1,"result":"Running the reviewed test file against an isolated export of the parent commit, five of the six selected cases failed and one passed. The three escaped-separator cases, including the finding-location one, plus both parenthesis cases failed, so they discriminate this commit. The relative-looking protected location case passed, exactly as the report and handoff disclose.","tests_pass":false,"witness":null},{"command":"LANE_TEST_CLI=../e1edc15/lane.mjs node --test --test-name-pattern=\"relative-looking protected location\" test/lane.test.mjs","cwd":"scratch","exit_code":1,"result":"The same case failed against an isolated export of the earlier tool copy named in the report, confirming that it discriminates the previous round's exemption removal rather than this commit.","tests_pass":false,"witness":null},{"command":"node --test --test-name-pattern=\"^PROBE publishes\" test/lane.test.mjs","cwd":"scratch","exit_code":0,"result":"Five reviewer-added end-to-end cases appended to the scratch test file drove the real review command through the offline fake reviewer at the reviewed commit. All five returned exit 0 and the published record contained the external path verbatim: a flag-attached POSIX path in a command string, in prose and in a finding location, a forward-separator UNC path, and a file URL with a host component.","tests_pass":false,"witness":null},{"command":"LANE_TEST_CLI=../base/lane.mjs node --test --test-name-pattern=\"^PROBE publishes\" test/lane.test.mjs","cwd":"scratch","exit_code":0,"result":"The same five cases against an isolated export of the base commit produced identical verbatim publication at exit 0, establishing all five shapes as pre-existing rather than regressions introduced by this lane.","tests_pass":false,"witness":null},{"command":"node probe.mjs","cwd":"scratch","exit_code":0,"result":"A harness importing the reviewed sanitizer functions from a truncated scratch copy, driven by environment variables carrying the scratch fixture root and a JSON case matrix, projected 294 then 69 adversarial fixtures across prose, findings, fixes, locations, commands and results. Contiguous POSIX, drive-letter, backslash UNC, three-separator file URL, quoted, bracketed, command-substitution, spaced, percent-encoded, numeric-reference and backslash-escaped shapes all redacted or refused. A plain regex literal in prose published as a placeholder rather than refusing, confirming the documented asymmetric rule. The same harness against the base commit showed the escaped-separator location shape publishing verbatim, which this commit closes.","tests_pass":false,"witness":null}]
```

## Non-claims
- The findings above describe pre-existing behavior that is identical at the base commit; this lane introduces no path-sanitization regression and strictly narrows the leak class.
- A green suite, a green strict validation and the inherited gate do not establish that the sanitizer redacts every possible private token, only the shapes exercised here.
- Running the three authorized commands in a scratch export of the reviewed tree does not establish that they would behave identically inside the lane worktree; the export was byte-identical to the reviewed commit.
- No claim is made that the reviewer-added probe cases belong in the delivered suite; they existed only in the deleted scratch copy.

## Unverified
- The three authorized commands ran in a cleaned scratch export rather than in the lane, because the reviewer role forbids creating anything inside the reviewed worktree, including ignored scratch and caches.
- No live Herdr server, real reviewer agent, real dispatch or real workspace was exercised; every review run used the offline fake reviewer fixture.
- No alternate operating system, Windows filesystem or non-POSIX path semantics were exercised; drive-letter and UNC behavior was observed only as text on this platform.
- The settle window was not tested against a rewrite that preserves both size and modification time, nor against a coarse filesystem timestamp resolution; the report already discloses that the metadata check is not an operating-system lock.
- The final commit's post-commit gate was not re-run; the disclosed gate is inherited evidence bound to the reviewed commit, not a fresh verdict.
- Promotion, rebase onto current main, push, archive, board behavior and the late-publication recovery path were not exercised.
- The declared-identifier ambiguity, alias precedence and non-ASCII refusal paths were exercised only indirectly through the delivered suite, not through new adversarial fixtures.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
