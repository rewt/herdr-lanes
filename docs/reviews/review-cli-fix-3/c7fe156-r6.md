**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: c7fe156d3b5ee7824aeeb419dee915e4d0bdc9cb
Topic: review-cli-fix-3
Round: 6
Review ID: lr-8f850ed1fb9815983e5216dbe88a57e4
Base branch: main
Base commit: 3367cd53e6fa50d3212f0da32e2189900b9b6d21

## Findings
- [Moderate] lane.mjs:1646 - the new flag branch starts only when whitespace or the string start precedes the hyphen, so the round 5 shape of an absolute POSIX path attached directly to a short flag, meaning a hyphen, letters and then the leading separator with no space, still matches no alternative when the flag follows a backtick, a double or single quote, an equals sign, a comma, an opening parenthesis or an opening square bracket; such paths publish verbatim at exit 0 in finding prose, including the backtick quoting the template recommends for identifiers, in command strings such as a quoted include flag or a CFLAGS assignment, and in a finding location that begins with a double quote; Fix: replace the whitespace lookbehind before the flag hyphen with one that rejects only letters, marks, numbers, underscore, dot, hyphen and separators, which a scratch mutation showed redacts the nine probed prose and command shapes and refuses the quoted location while leaving the other 73 probe cases unchanged, then add fixtures for backtick-quoted prose, a quoted or assigned command flag and a quoted location, each with its own deliberate negative control
- [Minor] docs/REFERENCE.md:582 - the reference still says publication refuses prose only for non-ASCII or control characters, unresolved private identifiers, reviewer-written reserved placeholders, and residual paths or aliases, which omits the ambiguous spaced-path and parenthesized-residue refusals listed at line 547 of the same file and now named in the corrected template enumeration, so the round 5 template defect survives in the reference; Fix: add ambiguous spaced paths and parenthesized path residue to that sentence so it matches the template, and extend the asymmetric path-redaction documentation test to assert the reference enumeration too

## Re-executed
```json
[{"command":"for v in head base r5; do node probe.mjs $v/lane.mjs cases.json $v; node probe.mjs $v/lane.mjs cases2.json $v; done","cwd":"scratch","exit_code":0,"result":"A harness importing the review sanitizer extracted from scratch exports of the reviewed, base and round 5 commits, with injected user and host aliases, projected 83 adversarial cases across finding prose, fix, location, command, result and non-claim fields. At the reviewed commit every shape named in rounds 1 to 5 redacts or refuses, including the round 5 flag-attached, forward-separator UNC and hosted file URL shapes in every field, and plain regex literals in prose publish with placeholders instead of refusing. Nineteen cases still publish the marker text at the reviewed commit, one of them the documented literal non-file URL, and all nineteen behave identically at the base commit; ten are flag-attached paths whose flag follows a non-space delimiter. No case exposes text at the reviewed commit that the base commit redacted or refused.","tests_pass":false,"witness":null},{"command":"node probe.mjs mut/lane.mjs cases.json mut; node probe.mjs mut/lane.mjs cases2.json mut","cwd":"scratch","exit_code":0,"result":"In a scratch copy of the reviewed sanitizer whose flag lookbehind was changed to reject only letters, marks, numbers, underscore, dot, separators and hyphen, the ten delimiter-prefixed flag cases redacted the path, or refused for the quoted finding location, and the other 73 cases produced output identical to the reviewed commit.","tests_pass":false,"witness":null},{"command":"node --test --test-name-pattern=\"^PROBE\" test/lane.test.mjs","cwd":"scratch","exit_code":0,"result":"Six reviewer-added end-to-end cases appended to a scratch export of the reviewed commit drove the real review command through the offline fake reviewer. Five shapes exited 0 with a PASS verdict and published the external path verbatim: a backtick-quoted flag in finding prose, an equals-prefixed and a double-quoted include flag in command strings, a comma-prefixed linker flag in a command string, and a double-quoted flag in a finding location. The whitespace-prefixed control flag in prose published with the path replaced by the placeholder.","tests_pass":false,"witness":null},{"command":"LANE_TEST_CLI=../base/lane.mjs node --test --test-name-pattern=\"^PROBE\" test/lane.test.mjs","cwd":"scratch","exit_code":0,"result":"The same six cases against an isolated export of the base commit all exited 0 and published the path verbatim, including the whitespace-prefixed control, so the five delimiter-prefixed shapes are pre-existing and only the whitespace-prefixed form is closed by the reviewed commit.","tests_pass":false,"witness":null},{"command":"LANE_TEST_CLI=../r5/lane.mjs node --test --test-name-pattern=\"flag-attached absolute path|redacts a forward-separator UNC|redacts a hosted file URL\" test/lane.test.mjs","cwd":"scratch","exit_code":1,"result":"Running the reviewed test file against an isolated export of the round 5 commit, all five new behavior fixtures failed with zero passes: the flag-attached path in finding prose, finding location and command string, the forward-separator UNC path and the hosted file URL. The new fixtures therefore discriminate the round 6 matcher change, matching the report's failure-first claim.","tests_pass":false,"witness":null},{"command":"npm test","cwd":"scratch","exit_code":0,"result":"In a cleaned scratch export of the reviewed commit, started after a clear load probe at 18:01, 145 of 145 cases passed with zero failures, cancellations or skips in 267.8 seconds, reproducing the report's final suite count; no Unix-socket permission error occurred.","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"On the same scratch export, zero cases passed and all 145 failed in 268.6 seconds; 144 failures carried the deliberate-control message, including the five round 6 fixtures, the settle window, the timeout floor and the asymmetric path-redaction documentation controls.","observed_failure":"Every case was expected to fail through its deliberate control. 144 did. The output-path preflight case failed earlier, at its first refusal assertion, because the user-level default git excludes file gained the lane state directory at 18:06, between the two runs, and the suite fixtures still read that file. An isolated rerun of that case now fails the same way without the control variable, so its own control was not observed and the green run above predates the change."}},{"command":"OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"scratch","exit_code":0,"result":"Strict validation of the scratch export reported 19 items passed and zero failed, reproducing the reported 19 of 19 result.","tests_pass":false,"witness":null},{"command":"LANE_TEST_NEGATIVE_CONTROL=1 node --test --test-name-pattern=\"review preflight requires ignored private\" test/lane.test.mjs; node --test --test-name-pattern=\"review preflight requires ignored private\" test/lane.test.mjs; LANE_TEST_NEGATIVE_CONTROL=1 node --test --test-name-pattern=\"review preflight requires ignored private\" test/lane.test.mjs","cwd":"scratch","exit_code":1,"result":"After the control run, the output-path preflight case was run alone three times, twice with the control variable and once without. Every run failed at its first refusal assertion with exit 0 where 2 was expected, because a fixture repository whose own ignore rules omit the lane state directory is now ignored through the user-level default git excludes file, which changed at 18:06. The case body and the preflight check are unchanged from the base commit, so an environment change exposed a suite hermeticity gap; this is not a regression in the reviewed commit.","tests_pass":false,"witness":null}]
```

## Non-claims
- Follow-up candidates: the matcher gaps in the next five entries behave identically at the base commit 3367cd5, are not among the round 1 to 5 findings, and are outside this lane's scope.
- Follow-up candidate: a file URL whose host carries a port, meaning a colon and digits after the host name, matches no alternative and publishes verbatim.
- Follow-up candidate: a drive-letter path with backslash separators and a parenthesized segment ends its match at the closing parenthesis, so the backslash-separated remainder after that parenthesis publishes.
- Follow-up candidate: a three-separator file URL stops at the first character outside its URL character class, such as an asterisk, or at a backslash separator after a drive letter, so the remainder of the path publishes.
- Follow-up candidate: an absolute path containing a comma ends its match at the comma and publishes the remainder of the path.
- Follow-up candidate: a double-hyphen long option attached directly to an absolute path, a short flag attached directly to a forward-separator UNC path, and a UNC server name followed only by one trailing separator all publish verbatim.
- Observation, not a finding: because flag-attached paths are now concrete matches, a command string with a flag-attached absolute path followed later by any relative path, such as an include flag followed by an output file under a subdirectory, now refuses under the documented spaced-path rule where the base published it with the path exposed; this fails closed but can cost a reviewer a round.
- Observation, not a finding: a regex literal with a capture group directly after literal text refuses under the documented parenthesized-residue rule where the base published it with a placeholder; REFERENCE, the design and both capability specs document that the residue rule can refuse regex-like text.
- The round 5 recommended fix wording was itself narrower than the shape that finding described, so the Moderate finding concerns completeness of the requested correction rather than a regression; every leaking shape found here leaks identically at the base commit.
- The probe matrix exercised extracted copies of the sanitizer with injected user and host aliases; only the shapes listed in the end-to-end entries were driven through the complete review command.
- No claim is made that the sanitizer redacts every possible private token beyond the exercised shapes, and neither a green suite nor the inherited gate establishes that.
- Observation, not a finding: the suite's hermetic git environment replaces global and system configuration but git still reads the user-level default excludes file; when that file gained the lane state directory during this review, the unchanged output-path preflight case began to fail on any commit, so a fresh suite run or promotion validation on this machine now fails that case until the fixtures also neutralize the default excludes file, for example with an empty core.excludesFile in the shared fixture configuration, or that entry is removed. The gap is identical at the base commit.

## Unverified
- No live Herdr server, real reviewer agent, real dispatch or real workspace was exercised; every end-to-end run used the offline fake reviewer fixture.
- No alternate operating system, Windows filesystem or non-POSIX path semantics were exercised; drive-letter and UNC behavior was observed only as text on this platform.
- The settle window was not tested against a rewrite that preserves both size and modification time, nor against coarse filesystem timestamps; the report already discloses that the metadata check is not an operating-system lock.
- The inherited post-commit gate was not re-run; it is disclosed evidence bound to the reviewed commit, not a fresh verdict.
- Promotion, rebase onto current main, push, archive, board behavior and the late-publication recovery path were not exercised.
- Authorized commands ran in cleaned scratch exports of the reviewed tree rather than in the lane, because the reviewer role forbids creating anything inside the reviewed worktree, including ignored caches.
- The output-path preflight case's own deliberate control was not observed, and the green suite run predates the 18:06 change to the user-level default git excludes file; a fresh suite run on this machine now fails that case independently of the reviewed code, and the only runs after the change were the isolated reruns of that case.

## Sanitization
- absolute-path: 0
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: None

<!-- lane-review-complete -->
