**NEEDS-WORK**
Schema: lane-review/v1
Reviewed commit: 3ef82307e125fc6c78bea9ee8cb1ef0d7fc719ff
Topic: board-actions
Round: 1
Review ID: lr-880ac550b7b48e0e44e2f278d78ddb7b
Base branch: main
Base commit: a7b0ac79aedd0ecf0e28ee290f1789047c72bc8f

## Findings
- [Moderate] board/app.mjs:57 - SIGINT, SIGTERM and SIGHUP terminate the interactive board before this teardown runs, so the \`lane board --watch --json\` observer child and any in-flight focus/done child outlive the UI; Ink's signal-exit handler calls unmount and re-raises the signal synchronously while React 18 flushes useEffect destroy functions asynchronously, and docs/REFERENCE.md:337 states that signals permanently stop the observer, reconnect timer and pending action children; Fix: hold the client in a module-scope reference and register process.on handlers for SIGINT, SIGTERM and SIGHUP that call client.close() synchronously before exiting, matching the explicit close the q handler already performs.
- [Minor] board/view.mjs:53 - the rewired table shows a session's recorded pane ID even when no agent is live, and line 51 shows the bare topic where the table previously showed the \`lane/\<topic\>\` branch, so the interactive view now disagrees with \`lane board --once\` for identical data; Fix: derive pane from live status only (for example \`row.status === "offline" ? "-" : row.pane\_id ?? "-"\`) and map lane from \`row.branch\`, keeping the promoted plain projection as the reference.
- [Minor] test/lane.test.mjs:3238 - the import and process-boundary regression inspects only static from-clauses plus a fixed identifier denylist, so a git control path reintroduced into a UI action handler through a dynamic import of \`node:child\_process\` still passes; Fix: also assert \`doesNotMatch(appSource, [ABS_PATH]\([ABS_PATH])\` and add \`execFileSync\`, \`spawnSync\`, \`node:fs\` and \`node:child\_process\` to the denylist.
- [Minor] lane.mjs:2221 - focus reports success for any exit-zero herdr response and discards \`run.error\` and \`run.stderr\`, unlike \`herdrJson\` at lane.mjs:761 and the dispatch guard at lane.mjs:1167, which both treat an error document as a failure; Fix: parse \`run.stdout\` the way \`herdrJson\` does, fail when the document carries \`error\` or cannot be parsed, and name \`run.stderr\` or \`run.error.code\` in the message.
- [Minor] docs/REFERENCE.md:270 - the JSON schema v1 row list documents \`last\_message\` and \`tripwire\` without stating that they are populated only by the watch subscription; Fix: add one sentence saying both are unavailable outside \`lane board --watch --json\`, matching the existing live-only note for the TRIPWIRE and LAST OUTPUT columns.
- [Minor] test/lane.test.mjs:2482 - the plain-versus-JSON projection parity test still asserts no \`tab\_id\`, although its own fixture already supplies \`panes\[0\].tab\_id\`, so the \`tabIdFor\` pane fallback stays uncovered; Fix: add \`tab\_id\` to the compared object so the fallback is asserted.
- [Minor] board/args.mjs:15 - \`main\` and \`registry\` option parsing is dead now that lane.mjs passes only \`--repo\` and app.mjs ignores both values; Fix: delete the two fields and their \`optionValue\` calls.

## Re-executed
```json
[{"command":"npm test","cwd":"scratch","exit_code":0,"result":"TAP summary: 126 tests, 126 pass, 0 fail, 0 skipped, 112.139 s; run in a clean system-temp clone checked out at the reviewed commit","tests_pass":true,"witness":{"kind":"negative-control","command":"LANE_TEST_NEGATIVE_CONTROL=1 npm test","cwd":"scratch","exit_code":1,"result":"TAP summary: 126 tests, 0 pass, 126 fail, 0 skipped, 106.857 s","observed_failure":"every one of the 126 deliberate controls failed and no test passed, so the green run is not a vacuous pass"}},{"command":"env PATH=\"$PATH_WITHOUT_HERDR\" npm test","cwd":"scratch","exit_code":0,"result":"with the herdr executable removed from PATH: 126 tests, 125 pass, 0 fail, 1 skipped, 113.990 s; the single skip is the herdr-dependent verify-agent control with the explicit reason recorded at test/lane.test.mjs:4411; recorded as a skip-behaviour observation, not as an independent pass claim","tests_pass":false,"witness":null},{"command":"node --test --test-name-pattern=\"cross only the lane CLI process boundary\"","cwd":"scratch","exit_code":1,"result":"after reintroducing a static `import { HerdrClient } from \"./herdr-client.mjs\"` into board/app.mjs the selected boundary test reported 0 pass and 1 fail; the mutated copy was restored afterwards","tests_pass":false,"witness":{"kind":"mutation","command":"node --test --test-name-pattern=\"cross only the lane CLI process boundary\"","cwd":"scratch","exit_code":1,"result":"0 pass, 1 fail, 125 filtered skips","observed_failure":"the boundary regression fails when a direct Herdr socket import is put back into the UI, so it is not vacuous for the static-import case"}},{"command":"node --test --test-name-pattern=\"cross only the lane CLI process boundary\"","cwd":"scratch","exit_code":0,"result":"after reintroducing a git control path in runAction through `await import(\"node:child_process\")` plus execFileSync, the same boundary test still reported 1 pass and 0 fail; this is the evidence for the boundary-regression finding, not a pass claim; the mutated copy was restored afterwards","tests_pass":false,"witness":null},{"command":"OPENSPEC_TELEMETRY=0 openspec validate --all --strict --no-interactive --concurrency 1","cwd":"scratch","exit_code":0,"result":"Totals: 19 passed, 0 failed (19 items), including change/board-actions and spec/board-session-actions; openspec 1.6.0","tests_pass":false,"witness":null},{"command":"./lane.mjs board --json","cwd":"lane","exit_code":0,"result":"one schema_version 1 document against the live Herdr server: 14 rows, coverage repository=complete registry=available herdr=connected messages=pane-output, zero errors, empty stderr; every row carried a non-null tab_id, every row carried last_message.available=false and tripwire=null outside watch mode; the lane worktree stayed clean","tests_pass":false,"witness":null},{"command":"script -q [ABS_PATH] node probe.mjs","cwd":"scratch","exit_code":0,"result":"pseudo-terminal probe: an Ink 5.2.1 component whose useEffect starts LaneCliClient and closes it on cleanup; after SIGTERM to the Ink process the cleanup marker was never written and the spawned observer child was still alive","tests_pass":false,"witness":null},{"command":"script -q [ABS_PATH] node probe-unmount.mjs","cwd":"scratch","exit_code":0,"result":"control for the probe above: on an explicit instance.unmount() the same component wrote its cleanup marker and its observer child was gone, so the probe does detect a working teardown","tests_pass":false,"witness":null},{"command":"node orphan-probe.mjs","cwd":"scratch","exit_code":0,"result":"a real `lane board --watch --json` child of a killed parent stayed alive for about four seconds and then exited on its own, bounding the leak at roughly one five-second refresh tick through the existing EPIPE handler","tests_pass":false,"witness":null},{"command":"node row-compare.mjs","cwd":"scratch","exit_code":0,"result":"same registry session and empty Herdr snapshot through joinBoardRows and through boardSnapshotDocument plus rowsFromBoardDocument: LANE differed (lane/board-actions versus board-actions) and PANE differed (- versus the recorded pane) while every other compared cell matched","tests_pass":false,"witness":null}]
```

## Non-claims
- No claim that the shipped board/app.mjs was launched: the teardown evidence comes from a synthetic Ink component that reuses the delta's LaneCliClient, not from the installed board application.
- No claim about behaviour on any host, platform or git version other than the one measured here.
- No claim that the seven findings are exhaustive for the Ink render path, which was reviewed by reading only.
- No claim that a green gate, a green suite here, or this record makes the commit promotable; promotion revalidates independently.

## Unverified
- Real-TTY interactive use of the installed board against a live Herdr server was not exercised; the lane's own report already records this as an explicit skip and board dependencies are not installed in the reviewed worktree.
- Live focus and done actions were not run against real sessions because the brief forbids it, so only the offline fake-Herdr and fake-CLI paths were executed.
- Process exit statuses for the piped runs above were read from their TAP or totals summaries rather than captured separately; only the board JSON read had its status captured directly.
- Whether any herdr build reports a focus refusal with exit zero was not established; herdr 0.8.2 answered a read for a missing agent with an error document and a nonzero status, so the focus finding is a consistency and defence-in-depth concern rather than a currently reproducible false success.
- EOF on standard input was not exercised; the entrypoint refuses a non-TTY start, so no realistic EOF path was available.
- git 2.38 compatibility was not exercised; the measured toolchain was Node.js v20.19.4, npm 10.8.2, git 2.54.0, herdr 0.8.2 and openspec 1.6.0.
- No alternate host, Linux or Windows run, push, promotion, archive or public record was performed, and nothing was created or modified inside the reviewed lane worktree.

## Sanitization
- absolute-path: 4
- user: 0
- host: 0
- private: 0
- relative-path: 0
- redacted execution fields: 6.command, 7.command

<!-- lane-review-complete -->
