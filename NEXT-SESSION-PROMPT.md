Read AGENTS.md, HANDOFF.md and docs/ORIGIN.md, then continue from the last HANDOFF entry.
Publish first if `git remote -v` shows no origin. Then add the offline test suite
described in HANDOFF.md. Append a dated HANDOFF entry when you stop.
