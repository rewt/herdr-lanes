# Project supervisor locator (private, ignored)

Canonical checkout:
Git common directory:
Approved supervisor working directory and permitted writes:
Applicable agent instructions: `AGENTS.md` and any inherited parent instructions
Default daily supervisor agent kind, model, and effort:
Current active queue owner and model (or none; verify on startup):
Allowed launch-time model override and source of operator decision:
Codex shared app-server and mobile Remote setup status, if used:
Allowed dispatch and review routes:

## Current goals

- Authoritative current plan or task source (exact path):
- Durable transition / closeout index (exact path or lookup rule):
- Additional required source, if any:

## Durable records

- Current plans and decisions (tracked path, if project policy requires one):
- Lane reports, measurements, and evidence (exact durable archive root):
- Independent review records (private and public destinations, as applicable):
- Private runtime artifacts (exact location; never treat as a public archive):
- Disposable scratch (exact location; preserve durable results before cleanup):
- Publication records, if applicable (separate review and authority gate):

The repository's own instructions control when to commit, promote, close, or
publish. Do not create an archive convention by analogy with another project.
This locator contains paths and policy pointers only, never secrets or raw
transcripts. Recheck source identities and current status at every new session.
